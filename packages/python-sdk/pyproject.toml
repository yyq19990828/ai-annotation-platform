[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-annotation-sdk"
version = "0.15.2"
description = "AI 标注平台官方 Python SDK: 同步 Client 覆盖 8 个稳定工作流, extras 提供 aap CLI / TUI"
requires-python = ">=3.11"
classifiers = [
    "Development Status :: 4 - Beta",
]
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.5",
]

[project.optional-dependencies]
cli = ["typer>=0.12", "rich>=13.0"]
# tui 隐含 cli (aap tui 子命令从 CLI 入口启动)
tui = ["textual>=0.80", "typer>=0.12", "rich>=13.0"]
test = [
    "pytest>=8.3",
    "respx>=0.21",
    "typer>=0.12",
    "rich>=13.0",
    "textual>=0.80",
    "pytest-asyncio>=0.24",
]

[project.scripts]
aap = "ai_annotation.cli.main:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
