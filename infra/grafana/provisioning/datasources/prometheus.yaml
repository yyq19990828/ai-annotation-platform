# v0.8.8 · Grafana auto-provision datasource。
# Grafana 启动时自动注册，无需 admin 手动创建。
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
