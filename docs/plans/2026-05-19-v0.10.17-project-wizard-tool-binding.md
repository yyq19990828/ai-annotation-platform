# v0.10.17 · 项目向导通用化 + 工具维度类别属性 + Magic Box + 模板编辑器

> 日期: 2026-05-19 · 版本: v0.10.17
> 落地后请重命名为 `2026-05-19-v0.10.17-project-wizard-tool-binding.md`(plan 模式仅允许此默认路径)

## Context

ROADMAP 反馈四件相关事一起做:

1. **新建项目向导过窄**: 当前 `CreateProjectWizard` 把"项目类型"枚举死 7 种(image-det / image-seg / image-kp / lidar / video-mm / video-track / mm),其实只有 image / video / lidar 三种**数据载体**,具体能做什么由"工具集"决定。强行用 `type_key` 区分让 UI 死板、客户场景只要不是平台预设的组合就要再加 type_key。
2. **类别和属性与工具脱钩**: 现在 `classes_config` 是项目级扁平 dict,所有工具共享一份。客户提的是"bbox 工具标行人车辆,polygon 工具标可行驶区/天空,AI 交互工具又有自己的类",一张大表混着塞强行兼容不了。需要把绑定颗粒度提到"工具单位"。
3. **Magic Box (粗框 → SAM 收紧到对象紧凑外接矩形)**: SmartBoxTool + sam3-backend 的 bbox→mask 链路已就绪,只差最后一步从 mask 取紧凑 bbox 回写。同窗口落地零额外后端。
4. **TemplateEditModal 复杂字段 UI**: 后端 `ProjectTemplate` schema 已完整支持 `classes_config / attribute_schema / rendering_config`,只是前端 Modal 没暴露,被迫"先从已有项目导出再做模板"。本次工具维度改造完成后,这三个字段必须按新结构暴露,顺手补齐。

**已对齐的关键决策**(由用户在 plan 阶段确认):

- **类别绑定颗粒度 = 工具独占**(强隔离)。每个工具单位独立持有 classes + attribute_schema,可同名不同色。
- **工具集呈现 = 三组**:基础几何 (bbox + polyline) / 区域 (polygon + mask 打包) / AI 交互 (smart-point + smart-box + text-prompt + exemplar 打包)。其中 polyline 当前还没有,本版 v0.10.17 **不实现** polyline 工具,但 schema 留好位置。
- **Magic Box 仅复用 SAM 链路**,不引入 Canny/Sobel snap-to-edge(留 v0.11+)。
- **后端 schema 重写为嵌套结构 + alembic backfill**,旧扁平 `classes_config` 按 `type_key` 推断映射到对应 tool_unit。

---

## 核心数据模型变更

### tool_unit 枚举(后端 + 前端共享)

新增"工具单位"概念,作为类别/属性绑定的 key:

| tool_unit_id | 包含工具 | type 限制 |
|---|---|---|
| `bbox` | BboxTool | image, video |
| `polyline` | (本版无实现) | image, video (留位) |
| `region` | PolygonTool + MaskTool(打包) | image |
| `ai_interactive` | SmartPointTool + SmartBoxTool + TextPromptTool + ExemplarTool + MagicBoxTool(打包) | image |
| `lidar_box_3d` | (本版无实现) | lidar (留位) |

### Project 模型新字段

新增 `tool_bindings: JSONB`,形状:

```json
{
  "bbox": {
    "enabled": true,
    "classes": [{ "name": "person", "color": "#f00", "alias": "人", "order": 0 }],
    "attribute_schema": { "fields": [...] }
  },
  "region": {
    "enabled": true,
    "classes": [...],
    "attribute_schema": {...}
  },
  "ai_interactive": {
    "enabled": false,
    "classes": [],
    "attribute_schema": { "fields": [] }
  }
}
```

**新 schema 是唯一真相**。alembic 迁移把旧 `classes_config` + `attribute_schema` backfill 到对应 tool_unit:
- `type_key in (image-det, video-track, video-mm, mm)` → 写到 `bbox`
- `type_key = image-seg` → 写到 `region`
- 其它(image-kp / lidar) → 写到 `bbox`(占位,后续手工调)

旧字段 `projects.classes_config` 与 `projects.attribute_schema` **保留作 v0.10.17 的过渡视图**:运行期由 trigger 或 service 层从 `tool_bindings` 派生(union 合并),供未迁移的导出/聚合查询继续读;v0.10.18 起删除。

### Annotation 关联工具单位

`annotations` 表加 `tool_unit_id: String(30) NOT NULL DEFAULT 'bbox'`。alembic backfill 按 `annotation_type` 反推:
- `bbox` → `bbox`
- `polygon` / `mask` → `region`
- `point` / `line` → (留待 v0.10.18 加 polyline/keypoint 时填,本版统一记 `bbox`)

`predictions` 表同样加 `tool_unit_id`。

`class_name` 字段保留,但语义改为"在 `tool_bindings[tool_unit_id].classes` 内"。新增标注/预测时,服务层校验 `class_name ∈ tool_bindings[tool_unit_id].classes`(空白单位放行,兼容历史);校验失败 422。

---

## 实施切片

### Slice 1 · 后端 schema + 迁移(P0,~1.5d)

**关键文件**

- `apps/api/app/db/models/project.py` — 加 `tool_bindings: JSONB`
- `apps/api/app/db/models/annotation.py` — 加 `tool_unit_id: String(30)`
- `apps/api/app/db/models/prediction.py` — 加 `tool_unit_id: String(30)`
- `apps/api/app/schemas/_jsonb_types.py` — 新增 `ToolBinding`, `ToolBindings`, `ToolUnitId` Literal
- `apps/api/app/schemas/project.py` — `ProjectCreate / Update / Out` 新增 `tool_bindings`;旧 `classes_config / attribute_schema` 暂保留(派生)
- `apps/api/app/schemas/annotation.py` / `prediction.py` — 加 `tool_unit_id` 必填字段
- `apps/api/alembic/versions/0072_project_tool_bindings.py` — 新 migration:
  - DDL: ADD COLUMN `projects.tool_bindings JSONB NOT NULL DEFAULT '{}'::jsonb`
  - DDL: ADD COLUMN `annotations.tool_unit_id`, `predictions.tool_unit_id`
  - Data: 按上述映射规则把现有 `classes_config + attribute_schema` 写入 `tool_bindings`
  - Data: 按 `annotation_type` backfill `annotations.tool_unit_id`
- `apps/api/app/services/project.py`(若无则新建) — `derive_legacy_classes_config(tool_bindings)` helper,**合并**所有 enabled 单位的 classes 给老接口降级使用

**验收**

- `pnpm api:test` 全绿;`alembic upgrade head` 后 `SELECT classes_config FROM projects` 仍返回原数据形状(派生)。
- 新 `tool_bindings` 字段在 GET /projects/{id} 返回正确。

### Slice 2 · 标注/导出/Prediction 链路适配(P0,~1.5d)

**标注校验**

- `apps/api/app/services/annotation.py` 创建/更新 annotation 时校验 `class_name ∈ tool_bindings[tool_unit_id].classes`;空集合放行。
- `apps/api/app/api/v1/tasks.py:810-849` `create_annotation` 必须收 `tool_unit_id`;前端工作台按当前工具传。

**导出协议**

- `apps/api/app/services/export.py:309-339` COCO categories 改为:每个 tool_unit 各贡献一段,`category.id` 全局唯一编号,`category.supercategory = tool_unit_id`,`category.name` 保留(同名不去重)。`cat_map` 改为 `(tool_unit_id, class_name) → category_id`,ann 查询用 `(ann.tool_unit_id, ann.class_name)` 查。
- `apps/api/app/schemas/aap_json.py` AAP JSON envelope `project.tool_bindings` 整段嵌入(替代旧 `classes_config / attribute_schema`);schema_version 升到 `1.1`。annotations/predictions 数组里每条加 `tool_unit_id`。**保留** envelope `project.classes_config` 派生字段作向后兼容(读端老 SDK 仍能解析扁平类)。
- v0.10.15 `predictions_import` 服务层(`apps/api/app/services/predictions_import.py`)对外部格式 COCO/YOLO/AAP/LS:
  - COCO/YOLO import 默认写到 `bbox` tool_unit
  - LS import 按 result.type 反推(`rectanglelabels` → bbox,`polygonlabels` → region)
  - AAP JSON 1.1 直接读 envelope tool_unit_id

**Prediction 写入**

- `apps/api/app/services/prediction.py` `to_internal_shape()` 出参增加 `tool_unit_id`(按 result.type 推断);worker `apps/api/app/workers/tasks.py:392-411` 写入 `Prediction.tool_unit_id`。

**验收**

- 导出现有项目 COCO 文件 categories 段含 `supercategory` 字段、按 tool_unit 分组。
- 老 prediction 经过 worker 重算后,tool_unit_id 字段非空。
- AAP JSON 导入 1.0 仍可读(只是不带 tool_unit_id,默认填 bbox)。

### Slice 3 · 新建项目向导改造(P1,~2d)

**类型简化**

- `apps/web/src/constants/projectTypes.ts` — 顶层 `PROJECT_DATA_TYPES = [image, video, lidar]`;旧 7 种作 `LEGACY_PROJECT_TYPE_KEYS` 保留只读引用(用于打开旧项目时回填 type_key)。
- `apps/web/src/components/projects/CreateProjectWizard.tsx`(当前 1130 行,本次拆分):
  - Step 1: 数据类型(三选一)+ 名称
  - **Step 2 [NEW]: 工具集**。每个 tool_unit 一个 card(标题 + 包含的工具列表 + 一个总开关)。AI 交互 / region 是打包总开关,basic_geometry 内部 bbox / polyline 可单独勾(polyline 本版禁用置灰)。`tool_bindings[unit_id].enabled` 由此决定。
  - Step 3: 类别 + 属性。按 Step 2 勾选的 tool_unit 生成 N 个折叠面板,**每个面板内部用 `<ClassEditor value={...} onChange={...} /> + <AttributeSchemaEditor value={...} onChange={...} />`** 各自维护一份。复用现有受控组件(`apps/web/src/pages/Projects/sections/ClassEditor.tsx`、`AttributeSchemaEditor.tsx`),无需解耦。
  - Step 4-6: 沿用 AI 接入 / 数据 / 成员。
- 把 `CreateProjectWizard` 1130 行拆出几个 step 子组件:`steps/DataTypeStep.tsx`、`steps/ToolsetStep.tsx`、`steps/ClassAttributeStep.tsx`(给后续模板复用)。

**ProjectSettings sections 改造**

- `apps/web/src/pages/Projects/sections/ClassesSection.tsx` + `AttributesSection.tsx` — 改为按 `tool_bindings` 遍历渲染分块;每块内 ClassEditor 仍 value/onChange,顶层一个 useUpdateProject hook 合并 PATCH。
- 旧 `classes_config / attribute_schema` 顶层编辑入口删除(派生只读)。

**验收**

- 用 Playwright/手动: 新建 image 项目 → 勾 bbox + region → 给 bbox 加类"人"红色、region 加类"路"蓝色 → 创建成功;打开项目设置看到两个分块,类不串。
- v0.10.16 之前创建的旧项目打开向导编辑(走 ProjectSettings 而非 wizard)时,所有旧类正确出现在 `bbox` 块(image-det)或 `region` 块(image-seg)。

### Slice 4 · 工作台 schema 读取适配(P1,~1d)

**新增 hook**

- `apps/web/src/pages/Workbench/state/useToolBindings.ts` — 给定 `project.tool_bindings` + 当前 `activeToolId` → 反查 tool_unit_id → 返回 `{ classes, attributeSchema, toolUnitId }`。
- 工具 → tool_unit 映射常量放 `apps/web/src/pages/Workbench/stage/tools/toolUnits.ts`(与后端 enum 字符串严格一致)。

**消费点**

- `apps/web/src/pages/Workbench/shell/ClassPalette.tsx` — 读 `useToolBindings().classes` 而非 `project.classes_config`。
- `apps/web/src/pages/Workbench/shell/AIInspectorPanel.tsx` — `attribute_schema.fields` 改读 `useToolBindings().attributeSchema.fields`。
- `apps/web/src/pages/Workbench/state/useAnnotations.ts`(创建/更新分支)— POST 时携带 `tool_unit_id` 字段。
- 切换工具时若新工具的 tool_unit 与当前激活 class 不在同一 binding,自动清空 activeClass(避免错位)。

**验收**

- 切到 bbox 工具,左侧调色板只显示 `bbox.classes`;切到 polygon 工具,只显示 `region.classes`。
- 新建标注成功落库,`tool_unit_id` 列非空。

### Slice 5 · Magic Box 工具(P2,~0.5d)

**前端**

- `apps/web/src/pages/Workbench/stage/tools/MagicBoxTool.ts` 新增:
  - `requiredPrompt: "bbox"`(走与 SmartBoxTool 同一能力开关)
  - `onPointerDown` 复用 SmartBoxTool 拖框逻辑派发 `samProbe { mode: "bbox" }`
  - **区别**: 在 `useInteractiveAI` 回调里,新增一个 mode 旗标"tighten",拿到 polygon 后调用 `apps/web/src/pages/Workbench/stage/shared/geometry/bbox.ts:tightenBboxFromPolygon(points) → [x1, y1, x2, y2]`(新增 helper,取 min/max x/y)写回 bbox 标注,而不是落 polygon。
- `apps/web/src/pages/Workbench/state/useInteractiveAI.ts` — 加一个 `outputKind: "polygon" | "bbox-tight"` 入参,bbox-tight 时把 polygon 跑 helper 转 bbox。
- `apps/web/src/pages/Workbench/stage/tools/index.ts` — 加 ToolId `"magic-box"`,注册到 `TOOL_REGISTRY` 与 `ALL_TOOLS`(AI 组里 ExemplarTool 之后);hotkey 取个未占用的 `G`。
- `toolUnits.ts` 映射: `magic-box → ai_interactive`(与其他 SAM 类工具同 unit,共享类别)。
- `apps/web/src/pages/Workbench/shell/ToolDock.tsx` — TOOL_DESCRIPTORS 加 magic-box 描述与图标。
- `apps/web/src/pages/Workbench/stage/tools/MagicBoxTool.test.ts` — 单测 helper `tightenBboxFromPolygon` 正确性 + outputKind 路由。

**后端**: 无改动(完全复用 sam3-backend bbox prompt → polygon 协议)。

**验收**

- 工作台 G 键 切到 Magic Box → 在图上拖出粗框 → 等 SAM → 框收紧到对象紧凑外接矩形 → 落 bbox 标注(annotation_type=bbox, tool_unit_id=ai_interactive)。
- AI backend 未就绪时按钮 disable + tooltip 提示(沿用 SmartBoxTool 已有 `useMLCapabilities` 机制)。

### Slice 6 · TemplateEditModal 复杂字段 UI(P3,~1d)

依赖 Slice 3 拆出的 `ClassAttributeStep` 组件。

**改造**

- `apps/web/src/pages/ProjectTemplates/TemplateEditModal.tsx` — 移除 classes CSV 输入框;新增三个 tab(基础信息 / 工具与类别 / 渲染配置):
  - tab1: name / description / scope / type_data(image/video/lidar)+ annotation_guide(沿用现有 Markdown 编辑器组件)
  - tab2: 复用 `<ToolsetStep>` 与 `<ClassAttributeStep>` 同样的 UI,value=template.tool_bindings
  - tab3: 复用 `apps/web/src/pages/Projects/sections/RenderingConfigSection.tsx` 的纯 UI 部分(snapToGrid / smoothImage / cssImageFilter / controlPointsSize)— 如该 Section 当前耦合 useProject,先抽出受控视图 `RenderingConfigEditor`(value/onChange),Section 仅做保存外壳
- `apps/api/app/schemas/project_template.py` — 已支持 classes_config / attribute_schema / rendering_config,本次只需新增 `tool_bindings` 字段(与 Project 对齐)。alembic migration `0073_template_tool_bindings.py` backfill 同 Slice 1 规则。
- `apps/api/app/services/project_clone.py` `CLONEABLE_PROJECT_FIELDS` 加 `tool_bindings`,删旧 `classes_config / attribute_schema`(派生)。

**验收**

- TemplateEditModal 内可直接编辑模板的工具集 / 类别 / 属性 / 渲染配置,无需"先建项目再导出"。
- 应用模板创建新项目时,tool_bindings 正确深拷贝。

---

## 受影响 / 复用的关键文件清单

**复用(无需改造)**
- [ClassEditor.tsx](apps/web/src/pages/Projects/sections/ClassEditor.tsx) — 已是纯受控
- [AttributeSchemaEditor.tsx](apps/web/src/pages/Projects/sections/AttributeSchemaEditor.tsx) — 已是纯受控
- [SmartBoxTool.ts](apps/web/src/pages/Workbench/stage/tools/SmartBoxTool.ts) — Magic Box 复用拖框
- [useInteractiveAI.ts](apps/web/src/pages/Workbench/state/useInteractiveAI.ts) — 加 outputKind 参数
- [sam3-backend/schemas.py](apps/sam3-backend/schemas.py) — Magic Box 不动后端协议

**需新建**
- [Workbench/stage/tools/MagicBoxTool.ts](apps/web/src/pages/Workbench/stage/tools/MagicBoxTool.ts)
- [Workbench/stage/tools/toolUnits.ts](apps/web/src/pages/Workbench/stage/tools/toolUnits.ts)
- [Workbench/stage/shared/geometry/bbox.ts](apps/web/src/pages/Workbench/stage/shared/geometry/bbox.ts) — 新增 `tightenBboxFromPolygon`
- [Workbench/state/useToolBindings.ts](apps/web/src/pages/Workbench/state/useToolBindings.ts)
- [components/projects/CreateProjectWizard/steps/DataTypeStep.tsx](apps/web/src/components/projects/CreateProjectWizard/steps/DataTypeStep.tsx)
- [components/projects/CreateProjectWizard/steps/ToolsetStep.tsx](apps/web/src/components/projects/CreateProjectWizard/steps/ToolsetStep.tsx)
- [components/projects/CreateProjectWizard/steps/ClassAttributeStep.tsx](apps/web/src/components/projects/CreateProjectWizard/steps/ClassAttributeStep.tsx)
- [alembic/versions/0072_project_tool_bindings.py](apps/api/alembic/versions/0072_project_tool_bindings.py)
- [alembic/versions/0073_template_tool_bindings.py](apps/api/alembic/versions/0073_template_tool_bindings.py)

**需改写**
- [project.py](apps/api/app/db/models/project.py)、[annotation.py](apps/api/app/db/models/annotation.py)、[prediction.py](apps/api/app/db/models/prediction.py)
- [_jsonb_types.py](apps/api/app/schemas/_jsonb_types.py)、[project.py 的 schema](apps/api/app/schemas/project.py)、[annotation.py 的 schema](apps/api/app/schemas/annotation.py)、[prediction.py 的 schema](apps/api/app/schemas/prediction.py)、[project_template.py 的 schema](apps/api/app/schemas/project_template.py)
- [services/export.py](apps/api/app/services/export.py)(COCO categories 改造)
- [services/prediction.py](apps/api/app/services/prediction.py)(to_internal_shape 返回 tool_unit_id)
- [services/annotation.py](apps/api/app/services/annotation.py)(class_name 校验)
- [services/predictions_import.py](apps/api/app/services/predictions_import.py)(import 时填 tool_unit_id)
- [services/project_clone.py](apps/api/app/services/project_clone.py)
- [api/v1/tasks.py](apps/api/app/api/v1/tasks.py)(create_annotation 接 tool_unit_id)
- [schemas/aap_json.py](apps/api/app/schemas/aap_json.py)(schema_version → 1.1)
- [CreateProjectWizard.tsx](apps/web/src/components/projects/CreateProjectWizard.tsx)(1130 行拆分)
- [ClassesSection.tsx](apps/web/src/pages/Projects/sections/ClassesSection.tsx)、[AttributesSection.tsx](apps/web/src/pages/Projects/sections/AttributesSection.tsx)
- [ClassPalette.tsx](apps/web/src/pages/Workbench/shell/ClassPalette.tsx)、[AIInspectorPanel.tsx](apps/web/src/pages/Workbench/shell/AIInspectorPanel.tsx)、[ToolDock.tsx](apps/web/src/pages/Workbench/shell/ToolDock.tsx)
- [TemplateEditModal.tsx](apps/web/src/pages/ProjectTemplates/TemplateEditModal.tsx)
- [tools/index.ts](apps/web/src/pages/Workbench/stage/tools/index.ts)(加 magic-box)
- [constants/projectTypes.ts](apps/web/src/constants/projectTypes.ts)

---

## 风险与不做的事

- **不做** polyline / keypoint / lidar 工具实现(留 Slice schema 位但置灰)。
- **不做** snap-to-edge(Canny/Sobel),Magic Box 仅 SAM bbox→mask→紧凑外接矩形。
- **不做** 跨 tool_unit 类别"软关联"(同名映射 / 别名复用)。强隔离意味着 bbox 的"人"和 region 的"人"是两条独立记录,导出 COCO 时通过 `supercategory` 区分。如果客户后续反馈"想共享",再加可选 alias_to(tool_unit, class_name)。
- **不做** v0.10.18+ 计划的:删除派生 `classes_config / attribute_schema` 字段、加 polyline 工具、AAP JSON 1.1 importer 完整支持 video_track tool_unit。
- **迁移风险**: alembic backfill 默认把所有 image-det 项目类塞 bbox unit;若客户实际混用 polygon 工具,需要事后到 ProjectSettings 把类**复制**到 region unit(强隔离,不能共享)。这条要在 CHANGELOG 与 docs-site 标注。

---

## 验证(端到端)

1. **后端**
   - `cd apps/api && uv run alembic upgrade head` 成功
   - `uv run pytest tests/` 全绿,新增 migration 测试 + tool_unit 校验测试
   - 老接口 `GET /projects/{id}` 仍返回派生 `classes_config / attribute_schema`(降级兼容)
2. **新建向导**
   - 起 dev 环境(`docker compose up -d` + `pnpm web:dev`)
   - 新建 image 项目 → 选 bbox + region → 给两组各加 2 个类(同名 person 不同色) → 创建成功
   - 检查 DB:`SELECT tool_bindings FROM projects WHERE display_id='...'` 结构正确
3. **工作台**
   - 进入该项目,bbox 工具调色板只显示 bbox 的类
   - 切到 polygon 工具,调色板切到 region 的类
   - 落一个 bbox + 一个 polygon → 数据库 `SELECT tool_unit_id, class_name FROM annotations` 字段分离
4. **Magic Box**
   - sam3-backend 起来后,G 键切到 Magic Box,粗框框住目标 → 收紧到紧凑外接矩形 → 落 bbox 标注,tool_unit_id=ai_interactive
   - 检查 `useMLCapabilities` 不支持 bbox prompt 时 G 键工具置灰
5. **导出**
   - `POST /projects/{id}/exports?format=coco` → 拉下 zip → categories 段含 `supercategory` 字段,bbox 与 region 类分组清晰
   - AAP JSON 导出 schema_version=1.1,tool_bindings 整段嵌入
6. **TemplateEditModal**
   - `/project-templates` → 编辑一个模板 → tab2 编辑 tool_bindings(加类别 + 属性) → 保存 → 应用模板新建项目 → 项目继承全部字段
7. **回归**
   - 打开 v0.10.16 之前创建的老项目: classes 派生显示,标注工作台旧框可读;新建标注按当前工具走对应 tool_unit_id

---

## 估时

| Slice | 内容 | 估时 |
|---|---|---|
| 1 | 后端 schema + 迁移 | 1.5d |
| 2 | 标注/导出/Prediction 链路 | 1.5d |
| 3 | 新建向导改造 + ProjectSettings sections | 2d |
| 4 | 工作台 schema 读取 | 1d |
| 5 | Magic Box | 0.5d |
| 6 | TemplateEditModal | 1d |
| **合计** | | **~7.5d** |

Slice 1+2 必须先做(打地基);Slice 3-4 接力;Slice 5+6 可与 3-4 部分并行(不依赖向导改造,Magic Box 只需 Slice 1 的 tool_unit_id schema 就够)。
