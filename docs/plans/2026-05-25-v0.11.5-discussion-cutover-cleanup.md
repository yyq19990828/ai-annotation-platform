# v0.11.5 · AIInspectorPanel 瘦身 + 去 flag + 删旧 CommentsPanel

> [epic 索引](2026-05-25-v0.11-annotation-feedback-convergence.md) · B 组（前端）· 依赖：v0.11.2 / v0.11.3 / v0.11.4 全部就绪

## 目标

三个 tab 验证无回归后，把 DiscussionPanel 转正：去 feature flag，删除旧 `CommentsPanel` 路径，`AIInspectorPanel` 瘦身。

## 前端改动

- **`AIInspectorPanel.tsx` 瘦身**：删除 `CommentsPanel` 引用与已下放的 props（`backgroundUrl` / `enableCanvasDrawing` / `liveCanvas` / `commentAnchor` / `onSeekFrame`），只保留 BoxList / AttributeForm / videoTrackPanel。
- **删 `CommentsPanel.tsx`** 老路径。
- **去 `flags.discussionPanel`**：两段布局成为默认。
- 清理本组改动产生的孤儿 import/props（仅限本组自己造成的，不动无关代码）。

## 验证

- `pnpm --filter web typecheck` + `pnpm --filter web lint`。
- 全工作台 e2e 不回归（评论 / issue / history 三条链 + AIInspectorPanel 检查器功能）。
- grep 确认无残留 `CommentsPanel` / `flags.discussionPanel` 引用。
- dark mode 走查。

## 回退

本切片删旧路径 + 去 flag，回退较重（需还原 CommentsPanel + flag）。**因此前置条件是 v0.11.2/3/4 已充分验证**。建议本切片单独成 PR，便于必要时整体 revert。

## 备注

至此 ADR-0027「统一前端组件」收益（D3）落地。`bug` / `reject` 仍走各自专用入口（BugReportDrawer / 审核流），符合 B 组边界澄清。
