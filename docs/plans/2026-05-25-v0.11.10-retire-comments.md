# v0.11.10 · comment 切单源（退役 annotation_comments）

> [epic 索引](2026-05-25-v0.11-annotation-feedback-convergence.md) · D 组（切单源）· 依赖：v0.11.9（撤 bug/reject 双写）+ v0.11.8 ADR
>
> ⚠️ ADR 门控。`mentions`/`canvas_drawing`/`is_resolved` 落位以 v0.11.8 决策为准（refined-C 下 comment 完全并入统一表，这些字段作为统一表列 / JSONB）。

## 定位：refined-C 下 D 组唯一的实质迁移

bug/reject 不迁（v0.11.9 已撤其双写），issue 天生单源。**comment 是唯一有旧表、且确实该单源进 `annotation_feedbacks` 的 kind**——所以这是整个 D 组真正动数据的一版。

## 难点

有子结构（`mentions` / `attachments` / `canvas_drawing`）+ 状态用 `is_resolved` 布尔（非 status 枚举）+ **6 处生产读路径**。

## 步骤（同一版本内顺序推进）

1. **backfill**：alembic 把 `annotation_comments.is_active=true` 行灌入 `annotation_feedbacks`（kind=comment）；`is_resolved`→`status`（`true→resolved`/`false→open`）；`mentions`/`canvas_drawing` 按 v0.11.8 决策落位。
2. **切源**：前端 `useTaskCommentsInfinite` / `useAnnotationComments`（及 v0.11.2 已合并的 DiscussionPanel comments tab）切到统一 API；逐一核对 6 处读路径全部迁移。
3. **停双写**：停 `annotation_comments.py:287` 的 `mirror_annotation_comment` 调用 + 删 `feedback.py` 的该 helper；旧表保留只读（drop 留 v0.11.11 收尾）。

> 同 reject：步骤 1↔3 之间靠对账 cron 确认 comments 源 drift=0 再停双写。

## 验证

- 对账 comments 源 drift=0；抽样比对 mentions/canvas 数据无损。
- 评论 + canvas 批注 + mentions e2e 不回归；DiscussionPanel comments tab 全功能正常。

## 回退

三步各自可回退。切源前 backfill 已验证；停双写可恢复 mirror。

## 备注

`canvas_drawing` 与 v0.11.6 笔画 timeline 的 JSONB 结构相关——若 v0.11.6 已落，backfill 需保留 stroke 的 `id`/`started_at`/`ended_at`。
