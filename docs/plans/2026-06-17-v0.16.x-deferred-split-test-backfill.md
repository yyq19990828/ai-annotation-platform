# v0.16.x · 缓拆迁移「补测试再拆」计划(解锁巨石文件拆分 Epic 的遗留项)

> 规划日期 2026-06-17 · 本文是 [`2026-06-17-v0.16.x-large-file-component-split-epic.md`](./2026-06-17-v0.16.x-large-file-component-split-epic.md) 的续作。
>
> Epic §8 把若干子模块标为 🟡「缓拆」,**首要原因是「测试覆盖不足以守护行为不变」**(红线 §2.2)。本文回答:**对这些缓拆迁移,如何先补齐守护测试、再安全开拆。**
>
> 全程仍守 Epic §2 三条红线:行为零变化、测试不回退、各刀独立可回滚。

## 1. 核心判断:缓拆原因分两类,补测试只解锁第一类

摸底(2026-06-17,三路只读探查后端 pytest 基建 / 前端 vitest 基建 / 缓拆代码结构)后,把缓拆项按「补测试能否解锁」分成两类:

| 类别 | 缓拆的真实原因 | 补测试能否解锁 |
|---|---|---|
| **A. 纯逻辑外圈** | 单纯测试覆盖不足,不敢动 | ✅ **能** —— 补表征测试(characterization test)后即可放心拆 |
| **B. 渲染时序 / 竞态内核** | jsdom 无 WebGL;rAF/paint 时机测不到;多 ref 竞态脆 | ❌ **补单测也解锁不了** —— 只能靠 Playwright 视觉基线 + 字段契约 + 人工值守 |

**诚实前提:不要指望「补齐测试」能把 B 类(`usePsrEditor`、`useStageViewport`、`usePredictionPropagation`)变成后台代理可自动拆的对象。它们的内核本质上单测不到。** 本文把 A、B 分开排,A 类可放心(甚至后台代理)做,B 类必须留人工值守。

## 2. 通用方法:每刀拆成「两个独立 commit」

这把 Epic §2.2(测试先绿、后绿)落地,并优雅绕开「红线 2 不准新增/修改断言」的表面冲突 —— 关键是把「补测试」和「拆分」分成两次提交:

1. **Commit ①(补测试)**:针对**当前未拆**代码写新表征测试 → 对现状必须全绿。本身就是覆盖率提升,独立有价值、独立可回滚。
2. **Commit ②(拆分)**:搬代码 + 改 import / monkeypatch target → **同一套测试一字不改仍全绿**。

> 这就是 Feathers《Working Effectively with Legacy Code》的 golden-master/characterization-test 守护重构标准姿势:先把网补上,再动结构。红线 2「不新增/不删除/不修改断言」约束的是 Commit ②(拆分本身不动断言);新增守护测试是 Commit ① 的前置动作,二者分提交即无冲突。

## 3. 第 1 批 · 后端三刀(A 类,补测试 100% 解锁,可后台代理安全做)

基建:`uv run pytest`(memory `feedback_use_uv`)。三刀按 ROI / 风险从高到低排。

| 顺序 | 目标 | Commit ① 补什么测试 | Commit ② 拆什么 | 守护证据 |
|---|---|---|---|---|
| 1️⃣ 最稳 | `services/batch.py` → `services/batch_permissions.py` | 新增 `tests/test_batch_permissions.py`(~80 行):把 `VALID_TRANSITIONS` 20+ 条边 × 3 角色参数化铺满,直测 `_is_owner`/`_is_reviewer`/`_is_annotator_assigned`/`assert_can_transition` | 4 个纯函数整体搬迁;`batch.py`(经 `batch/__init__.py` re-export)`from app.services.batch import ...` 旧入口一字不改 | 现有 44 个 `test_batch_lifecycle.py`(`TestTransitionAuth`)+ 新单测**双守护** |
| 2️⃣ | `services/annotation.py` → `services/annotation_propagation.py` | 新增 `tests/test_annotation_propagate_helpers.py`(~30 行):表征测 `_clean_bbox_geometry`/`_composition_keyframe`/`_track_visible_keyframes`/`_clip_outside_ranges`/`_new_track_id` | 抽纯函数 + 协作类 `AnnotationPropagationService`,主类 `__init__` 组合持有并委派 `propagate`/`propagate_batch` | 现有 24 个 `test_annotation_propagate.py` 守护编排,新单测守护纯函数搬迁 |
| 3️⃣ | `workers/media.py` → `media_chunks.py` / `media_frames.py` | ⚠️ 此刀「补测试」≠ 补行为(现有 monkeypatch 已覆盖主路径),而是 **patch target 迁移核对**:先 grep 锁定全部 patch 点,再随搬迁同步改;可选补 2 个 `subprocess.run` mock 的边界单测 | 搬 `extract_video_chunk`/`extract_video_chunk_smart_copy`/`extract_video_frame_image`;`media.py` 冗余别名 re-export(同 v0.16.12 ① 手法) | 4 处字符串 patch 同步改新模块路径;**漏改 = ffmpeg 真跑 → 测试响亮报错**(自验证) |

### 3.1 目标 1️⃣ `batch_permissions.py` — 行号 / 测试矩阵

- 搬迁成员(均纯判定、零 DB 依赖):`_is_owner`(`batch.py` 73–74)/`_is_reviewer`(77–78)/`_is_annotator_assigned`(81–85)/`assert_can_transition`(88–170)。
- 测试矩阵思路:提取 `VALID_TRANSITIONS` 全部 (src, dst) 边,每条 × {SUPER_ADMIN, REVIEWER, ANNOTATOR} 参数化,断言放行 / 抛 403。关键样例:
  - `(ANNOTATOR, ANNOTATING→REVIEWING)` 放行;`(REVIEWER, 同)` 403。
  - `(SUPER_ADMIN, DRAFT→ACTIVE)` 放行;`(REVIEWER/ANNOTATOR, 同)` 403(owner-only)。
  - 逆向转移 `(*, APPROVED→REVIEWING)` owner-only。
- 难度:**最简**。纯函数 + 参数化,搬迁零副作用风险。

### 3.2 目标 2️⃣ `annotation_propagation.py` — 行号 / 协作类设计

- 纯函数(模块级,可改 `@staticmethod` 或保留模块级):`_PropagateContext`(39–59)/`_new_track_id`(62–63)/`_clean_bbox_geometry`(66–72)/`_composition_keyframe`(75–83)/`_track_visible_keyframes`(86–91)/`_clip_outside_ranges`(94–115)。后两者依赖已 import 的 `app.services.video_tracks`(`sorted_keyframes`/`frame_is_outside`/`normalize_outside_ranges`),随簇搬迁。
- 类内方法(依赖 `self.db` + 其它 service):`_resolve_propagate_context`(574–628)/`_ensure_box3d_context`(630–642)/`_propagate_one`(644–721)/`propagate_batch`(723–855)。
- 协作类设计:`AnnotationPropagationService(db)` 持公共 `propagate`/`propagate_batch` + 内部 `_resolve_*`/`_propagate_one`;`AnnotationService.__init__` 内 `self._propagation = AnnotationPropagationService(db)`,公共方法委派调用 —— 不对外暴露 `_resolve_propagate_context` 等复杂方法。
- 表征测试样例(各 5–10 行):`_clean_bbox_geometry({"x":"1","w":None})` → `{"x":1.0,"w":0.0,...}`;`_clip_outside_ranges(..., start=3, end=7)` 边界裁剪;`_track_visible_keyframes` 过滤 outside 区间的 keyframe。

### 3.3 目标 3️⃣ `media_chunks.py`/`media_frames.py` — patch target 迁移清单

- 搬迁成员:`extract_video_chunk`(`media.py` 117–160)/`extract_video_chunk_smart_copy`(161–194)/`extract_video_frame_image`(195–223)。调用方 `_store_video_chunk`/`_store_frame_cache_image` 留在 `media.py`。
- **必须同步改的 4 处字符串 patch**(`tests/test_video_workbench.py`):
  - 行 235 `"app.workers.media.extract_video_frame_image"` → `"app.workers.media_frames.extract_video_frame_image"`
  - 行 385 / 441 / 494 `"app.workers.media.extract_video_chunk"` → `"app.workers.media_chunks.extract_video_chunk"`
- **不动**:`tests/test_media_chunk_samples.py` 行 175/192 在 `media` 模块对象上 patch `probe_chunk_samples`/`_extract_decoder_config` —— 这俩已于 v0.16.12 ① re-export,`media.<name>` 仍存在,patch 继续生效。
- 开拆前先用绝对路径 grep 复核全部 patch 点(memory `rtk-grep-garbles-output`:用 `/usr/bin/grep`):
  ```bash
  /usr/bin/grep -rn 'app.workers.media\.\(extract_video\|probe\|_extract_decoder\)' apps/api/tests
  ```
- ⚠️ 改了 worker import 结构,dev 环境**必须 `docker restart` celery worker**(CLAUDE.md §8:Celery 无 `--reload`,跑旧代码会静默报错)。

## 4. 第 2 批 · 前端「纯逻辑外圈」(A 类,补纯函数测试解锁,中风险)

补完纯函数单测后,把对应**纯逻辑**安全抽出;**hook 壳保留为编排**,对外返回字段集用 `Object.keys` 前后比对守护(Epic §3 v0.16.10 已定的契约手法)。

| 目标文件 | 先抽并补单测的纯函数(来源行段) | 解锁的拆分 |
|---|---|---|
| `state/useWorkbenchShellModel.tsx` | `resolvePinViewport`(图钉→视口变换,`useIssuePins` 段 765–771);`buildPredictParams` 已抽已在 `*.helpers.ts` | `useIssuePins` 整簇(732–772)低风险可拆 |
| `stages/three-d/ThreeDWorkbench.tsx` | 新增 `validatePsrField`/`clampPsrValue`/`degreesToRadians`/`radiansToDegrees`(从 `handleFieldBlur` 抽)、`clampDragPosition`(面板拖拽);`psrToForm`/`boxGeometryFromPsr` 已在 `*.helpers.ts` | `useCameraPanels`(565–601)/`usePsrFloatingPanel`(1582–1783)低耦合可拆 |
| `Workbench/stage/ImageStage.tsx` | 新增 `normalizeImageCoordinate`(`toImg` 378)/`resolveSnapMatch`(`findSnapMatch` 388) | 纯几何外圈;renderHook 不需要,纯函数单测即可 |

基建就绪度:vitest 2.1.9 + jsdom + `renderHook`(已含于 `@testing-library/react` 16)+ `src/test/konvaMock.tsx`。样板:`hooks/useTheme.test.ts`、`state/__tests__/useCanvasDraftPersistence.test.ts`、`stage/ImageStageShapes.konva.test.tsx`。

## 5. 第 3 批 · 前端「渲染时序 / 竞态内核」(B 类,补单测解锁不了 → 人工值守)

**不进后台代理、不靠单测守护**,诚实标注守护手段:

| 目标 | 为何单测守不住 | 唯一可行守护 |
|---|---|---|
| `ThreeDWorkbench` 的 `usePsrEditor`(621–1260)/`usePointCloudSelection`(1437–1700)/`usePointMask`(455–1493) | jsdom 无 WebGL,Three.js scene/raycaster 整体跑不起来(`vite.config.ts` 已把 `stages/three-d/*.tsx` 排除出 coverage) | 人工回归:落框 / 改 PSR / 框选 / 点掩膜;鼠标键位口径先确认(memory `feedback_button_terminology`) |
| `ImageStage` 的 `useStageViewport` | render-time setState 比 effect 早一帧;双 `useLayoutEffect`(fit→draw)在 paint 前的执行**顺序**;`draw()` 同步刷 react-konva 的 rAF batchDraw —— 这些时机 jsdom 测不到(翻页首帧 jank 的修法核心,见 `ImageStage.tsx` 426–450 注释) | **Playwright 视觉基线 `pnpm screenshots`** + 手动翻页验证无 jank |
| `useWorkbenchShellModel` 的 `usePredictionPropagation`(795–1044)/`useAiConfigPanel`(1073–1397) | 3 ref(`pendingCrossFrameSelectRef`/`crossFrameInFlightRef`/`motionCompWarnedRef`)跨 4 callback + 2 effect 的竞态,renderHook 能勉强测但脆且高成本;`useAiConfigPanel` 与共享 `preCfg` hook(`usePreannotateConfig`)深耦合 | 人工冒烟:连按 Shift+→ 验证不重复建标注(同 group_id);字段契约比对 |

> 若将来要给 B 类加守护,正确投资方向是 **Playwright 端到端 / 视觉回归**(能覆盖 paint 时机与画布像素),不是 jsdom 单测 —— 但那属于 Epic §7 非目标(测试基建建设),不在「补测试再拆」范围,需另行立项。

## 6. 统一验证口径(沿用 Epic §4,补一条覆盖率注意)

每刀 PR 必须满足:

1. 后端:`uv run pytest`(目标子集 + 回归)全绿 + **OpenAPI 快照零 diff**(本批后端三刀均不碰路由,快照不应变);前端:`pnpm build`(tsc)+ `pnpm lint`(含 `check-css-tokens`)+ `pnpm test:coverage` 全绿。
2. **行为不变证据**:后端 = OpenAPI 快照零 diff + 现有测试不动断言仍绿;前端 = hook 返回字段集 / 组件 props 前后一致(`Object.keys` 对比)。
3. **覆盖率门槛注意**(`vite.config.ts`:lines/statements/functions 45%、branches 70%,当前实测 ~47.64% 仅剩 ~2.6pt 缓冲):第 2 批补纯函数测试会**抬高**覆盖率(好事);但拆 hook 时注意别把逻辑挪进已被 coverage **排除**的 `stages/three-d/*` 而平白丢覆盖。
4. 每刀 `cloc <file>` 实测前后 SLOC,PR 描述贴「拆前 N → 拆后 M + 新增 k 个子文件各 x 行」。
5. 按 memory `feedback_changelog_bugfix`:内部重构可不进 CHANGELOG,但同步受影响的 docs(若 `docs-site/dev/concepts/` 有引用这些文件路径/结构)。

## 7. 顺序与风险

```
第 1 批(后端 A 类,可后台代理):
  1️⃣ batch_permissions   → 最稳,纯函数+参数化,双守护,先做立信心
  2️⃣ annotation_propagation → 补 5 纯函数单测 → 抽协作类
  3️⃣ media_chunks/frames  → patch target 迁移核对为主;改完重启 worker

第 2 批(前端 A 类,中风险,靠纯函数单测 + 字段契约):
  useIssuePins / useCameraPanels / usePsrFloatingPanel / ImageStage 纯几何外圈

第 3 批(前端 B 类,补单测解锁不了,人工值守):
  usePsrEditor / usePointMask / useStageViewport / usePredictionPropagation
  —— 留有人值守时手动回归;不在后台代理可安全验证范围
```

- **不并行**:均触碰工作台 / worker 热路径,串行做便于隔离回归。
- **A 类每刀严格两段式 commit**(先测试后拆),任一可独立 revert。
- **若 A 类某刀拆到一半发现职责其实纠缠**(无法在不改行为前提下干净切分):停下,记入 issue,该刀缩小范围或退回 B 类缓拆,绝不为「拆干净」引入行为变化(Epic §5)。
- **子代理纪律**:若用 `Agent` 派子代理改代码,按 CLAUDE.md「Parallel Subagent Worktree Rule」加 `isolation: "worktree"` 并要求其提交到 `worktree-agent-*` 分支;验证(`uv run pytest` / `pnpm`)在主进程合并后跑(worktree 无运行环境)。

## 8. 执行进度(2026-06-17 开工)

> 第 1 批后端三刀全部完成,严格两段式 commit(纯函数刀:先测试后拆)/ 单 commit(media:现有测试守护)。各刀独立可回滚。**全量后端测试 exit 0**。

### 第 1 批 ✅ 全部完成

1. **batch_permissions ✅**(commit `c20529ae` 补测试 + `645d8ff1` 拆分):
   - 新增 `tests/test_batch_permissions.py`(**88 passed / 1 skipped**):纯单元测,直接构造 User/Project/TaskBatch ORM 实例(无 DB),参数化覆盖 `VALID_TRANSITIONS` 全部合法边 × 三角色 + `REVERSE_TRANSITIONS` owner-only。
   - 抽 `_is_owner` / `_is_reviewer` / `_is_annotator_assigned` / `assert_can_transition` + `REVERSE_TRANSITIONS` → `batch_permissions.py`(`X as X` re-export)。`batches.py` 端点 + 测试 import 零改动;新模块不 import batch → 无循环。
   - `batch.py` 物理行 ~1522 → **1419** + `batch_permissions.py` 129。证据:权限单测 88 + `test_batch_lifecycle` 44 拆分前后全绿。

2. **annotation_propagation ✅**(commit `0651637b` 补测试 + `8b118fe6` 拆分):
   - 新增 `tests/test_annotation_propagate_helpers.py`(**17 passed**):表征测 5 个纯函数(喂 dict 断言输出)。
   - 抽 `_PropagateContext` + `_new_track_id` / `_clean_bbox_geometry` / `_composition_keyframe` / `_track_visible_keyframes` / `_clip_outside_ranges` → `annotation_propagation.py`。annotation.py `X as X` import 回自用 + re-export。⚠️ **踩坑**:`_clean_bbox_geometry` 仅供测试 re-export(类方法不直接调),`ruff --fix` 会把它当孤儿剪掉 → 必须用 `as` 别名显式 re-export。
   - 证据:helpers 17 + `test_annotation_propagate` 24 拆分前后全绿。
   - **按 §6.1 缓拆**:协作类(`propagate` / `propagate_batch` / `_resolve_propagate_context` / `_propagate_one` 等 `self.db` 方法)依赖穿透深,本刀只抽纯逻辑簇。

3. **media_chunks / media_frames ✅**(commit `31152b7d`,现有测试守护,单 commit):
   - 抽 `extract_video_chunk` / `extract_video_chunk_smart_copy`(+`FFMPEG_CHUNK_TIMEOUT_SECONDS`)→ `media_chunks.py`;`extract_video_frame_image`(+`FFMPEG_FRAME_TIMEOUT_SECONDS`)→ `media_frames.py`。三者是无依赖纯 ffmpeg 封装,新模块不 import media → 无循环。
   - **§3.3 修正(实测更干净)**:调用方 `_store_*` 留 media.py 以裸名调用、走 media 模块全局 + media.py re-export → **monkeypatch target 与 test import 全部不变,0 处 patch 改动**(§3.3 预想要改 4 处字符串,实测 re-export 方案无需)。`test_video_workbench` / `test_media_chunk_samples` 零改动。
   - `media.py` 1164 → **1069** + `media_chunks.py` 93 + `media_frames.py` 45。证据:media 两套 **61 测试** + 全量 exit 0,patch 未失效(未真跑 ffmpeg)。⚠️ dev 需 `docker restart` celery worker(CLAUDE.md §8)。

### 第 2、3 批 ⏸ 未开工

- 第 2 批(前端 A 类):需前端 vitest 环境跑 `pnpm test` 守护纯函数,本轮后端无人值守未触及;可后续认领。
- 第 3 批(前端 B 类):按设计需人工值守 + Playwright,不在后台代理可安全验证范围。
