# 第二批：内部易用性、管理效率与超管系统设置

> 状态：方案草案，未实施。面向内部团队，包含用户补充的高频 `.env` 配置可视化管理需求。
> 调研基线：2026-09-09，Git `25313227`。配置默认值取自源码和 `.env.example`，未读取或修改实际部署的 `.env`。

## 目标与独立交付边界

让员工更快找到工作，让管理员更少重复操作，并能在后台调整常用业务设置。
本批直接扩展当前系统，不以[第一批](1788952434_internal-launch-readiness.md)全部完成为前提；生命周期操作继续使用实施时已经存在的接口，本批不重复实现离职交接。
[第三批](backlog/1788952434_internal-identity-governance.md)负责有明确需求后再接入的企业身份、跨项目角色和高级运行策略。

按工作包分别合并。超管系统设置是本批独立的优先工作包，可先于邀请向导、培训提示和批量操作交付。
本批预计超过 8 个文件；不将所有 UI、业务配置和批量管理改动捆成一次上线。

## 工作包 A：超管系统设置优化

### A1. 当前基础与选择

当前 `SystemSettingsService` 已支持 DB 覆盖部署默认值，采用 30 秒进程内 TTL 缓存。已经开放注册开关、邀请有效期、前端基础地址和 SMTP；存储连接器主机白名单已有专门的设置组件和接口。

代码锚点：

- `apps/api/app/config.py`：部署默认值。
- `apps/api/app/services/system_settings_service.py`：白名单、读取、写入、重置、审计脱敏。
- `apps/api/app/api/v1/system_settings.py`、`apps/api/app/schemas/me.py`：系统设置契约。
- `apps/web/src/pages/Settings/SettingsPage.tsx`、`apps/web/src/api/settings.ts`、`apps/web/src/hooks/useSystemSettings.ts`：当前设置界面与调用。

选择扩展现有设置服务和表。UI 保存数据库覆盖值，保留 `.env` 作为部署默认，不写服务器上的 `.env`，不增加配置中心、常驻服务或必装插件。
不把整个 Settings 类自动暴露为通用编辑器：只有下表明确列出的运营配置及现有设置可写。

### A2. 第一轮开放的六项配置

表中“默认值”是当前代码默认，不代表生产实际值。页面必须显示当前部署有效值及来源，不得用表中数值覆盖已存在的部署配置。

| 页面名称 / 分组                   | 环境变量 / 设置键                                                   | 当前代码默认                   | 计划校验与含义                                                                        | 消费者与生效时机                                                                                       |
| --------------------------------- | ------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 每位管理员邀请上限 / 成员与邀请   | `MAX_INVITATIONS_PER_DAY` / `max_invitations_per_day`               | 30                             | 整数 1–1000；按邀请人、滚动 24 小时计数，不是自然日或全平台总量；0 不代表无限         | `services/invitation.py`；后续创建邀请按有效值判断，API 跨进程传播不超过 30 秒缓存周期                 |
| 用户离线判定时间 / 成员与邀请     | `OFFLINE_THRESHOLD_MINUTES` / `offline_threshold_minutes`           | 5 分钟                         | 整数 2–60；只影响在线状态显示，不强制退出登录                                         | `workers/presence.py`；下一轮扫描采用有效值，现有 Beat 每 2 分钟扫描，页面说明该额外延迟               |
| 单次连接器导入文件数 / 数据导入   | `DATASET_IMPORT_MAX_FILES` / `dataset_import_max_files`             | 50,000                         | 正整数；首轮 UI 上限为代码默认与该部署环境基线的较大值，不因新增 UI 扩大容量承诺      | `api/v1/datasets.py` 受理时固定快照，`workers/dataset_import.py` 按快照枚举并提前拒绝超限              |
| 单次连接器导入总量 / 数据导入     | `DATASET_IMPORT_MAX_TOTAL_BYTES` / `dataset_import_max_total_bytes` | 214748364800 bytes，即 200 GiB | 正整数 bytes；UI 用 GiB 输入并精确换算，允许保留既有非整 GiB 值；上限原则同文件数     | 与文件数在同一快照读取；仅是连接器枚举预算，不宣称限制所有上传或实际网络字节                           |
| 转后台建任务的数量阈值 / 数据导入 | `TASK_CREATE_SYNC_THRESHOLD` / `task_create_sync_threshold`         | 2000                           | 整数 0 到代码默认与部署环境基线的较大值；0 表示所有非空数据集异步；数量等于阈值仍同步 | `services/dataset.py` 的关联数据集路径；下一次关联请求决定分支，不改已排队任务                         |
| 视频向后预热块数 / 视频体验       | `VIDEO_CHUNK_WARMUP_LOOKAHEAD` / `video_chunk_warmup_lookahead`     | 1                              | 整数 0 到代码默认与部署环境基线的较大值；0 关闭额外预热                               | `services/video_frame_service.py` 的邻块预热决策；只影响新请求，不取消已排队任务、不改变现有视频块布局 |

邀请和离线阈值虽可通过环境变量配置，但目前没有完整展示在 `.env.example`；实施时补充示例和后台覆盖说明。
若现有部署基线或历史覆盖超出新增校验范围，界面保留原值并标明范围冲突，允许显式恢复默认；启动和读取不静默截断。提高导入容量、同步事务数量或预热并行规模前先完成针对该部署的容量验收，再调整环境基线。

### A3. 页面行为

- 使用成员与邀请、邮件与访问地址、数据导入、视频体验四组表单；现有连接器白名单仍由专属组件管理，不复制一份通用文本框。
- 每项展示有效值、来源（部署默认或后台覆盖）、单位、作用范围、修改人/时间和生效时机；技术变量名可在说明中查看，主要标签使用业务名称。
- 支持搜索设置、按组保存、取消未保存修改和逐项恢复部署默认。保存前展示本组变更摘要，离开页面时处理未保存修改。
- 保存失败保留输入；后台刷新不覆盖正在编辑的表单；保存成功展示实际读回的有效值，不能只显示提交值。
- SMTP 密码始终只返回是否设置；“保留密码”“更换密码”“清除密码”含义明确，测试邮件明确使用已保存配置。修改未保存时先保存后测试。
- 恢复默认指删除 DB 覆盖并回到本部署环境值，不等于恢复开发机地址，也不等于清空字段。

### A4. API、类型和读写语义

- 保留 `GET/PATCH /api/v1/settings/system` 及已有字段；GET 增量返回新值、非敏感来源元数据和版本标识，兼容当前调用者。
- 增加 `POST /api/v1/settings/system/reset`，接收明确的 key 列表，在一笔事务内删除对应覆盖并记录审计。现有服务 `reset()` 可复用，当前 PATCH 的 `null` 忽略行为不能假装成重置。
- 新页面写入带读取时版本标识；服务端在事务内核对并串行化设置写入，冲突返回 409，页面保留草稿并展示最新值。旧 PATCH 调用保留只更新显式字段的兼容行为。
- 白名单字段定义集中维护类型、范围、单位和生效说明；API schema 与前端通过生成契约校对，避免只在表单里增加字段。
- 拒绝未知字段、非法类型、非整数计数和超范围值；不能用 `bool("false")` 一类强转接受错误输入。
- 读取合法的 `false`、`0`、空字符串时不使用 `value or default`。SMTP 的显式清除存为可辨别的空值；重置覆盖与清除邮件密码是不同操作。历史 NULL 覆盖维持继承环境值的兼容语义。
- 仅超管可读写全局设置。审计记录普通配置前后值、操作者及恢复默认操作；敏感字段只记录是否变更。

### A5. 真正生效的消费链路

DB 覆盖不会自动改变进程中的 `config.settings`。必须同时修改消费者，不能只增加输入框、schema 和白名单。

```text
超管设置页 → 系统设置 API → SystemSettingsService → system_settings + 审计
业务 API   → 有效配置读取 → 邀请判断 / 同步建任务 / 视频预热
导入 API   → 配额快照 → AsyncJob.payload → Celery → 同步枚举器
Beat       → presence worker → 有效配置读取 → 在线状态更新
```

1. 保留最多 30 秒的跨进程缓存传播合同。管理页面读回使用已提交数据；事务提交后再失效当前进程缓存，失败事务不能污染有效配置。
2. 需要一起使用的键以同一次查询形成快照，特别是两项导入预算，避免逐键缓存形成混合版本。
3. 新连接器导入在 API 受理时把有效文件数、字节数与配置版本写入 `AsyncJob.payload` 的有界配置快照；不接受客户端自行提供更宽松的预算。
4. Worker 将快照作为显式参数传入同步 `_collect_within_limits()`，不能在逐文件循环中查 DB。运行中和已经排队的任务继续使用自己的快照；修改全局配置只影响后来受理的任务。
5. 升级前已经排队且缺少快照的历史任务，在第一次执行前读取一次有效值并持久化，之后重试复用该值。同一任务重试不换预算；重新创建导入任务才使用新值。
6. 邀请创建、数据集关联和视频预热分别在其业务决策点读取配置；在线状态扫描在该轮执行时读取，维持原有扫描调度。
7. 初次上线代码需要刷新受影响 API/Worker，并确认实际运行的入口已更新；此后上述六项调参不要求重启进程。
8. 数据库读取失败不能悄悄退回更宽松配额。导入等预算操作使用同一操作已取得的快照，尚未取得快照时按已有请求/作业失败机制返回可重试错误。可选视频预热读取失败则本次不派额外块并记录诊断，保留主视频请求正常返回，不能把辅助预热变成播放成功的必要条件。

### A6. 本轮不开放的配置与后续边界

| 配置类别                                                            | 安排与原因                                                                                                                                                                     |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ML_PREDICT_TIMEOUT=100` 秒、`ML_HEALTH_TIMEOUT=10` 秒（代码默认）  | 放入第三批高级运行策略。推理超时还影响 GPU runtime 硬期限及恢复窗口；探活受 worker 总预算约束。完整统一 API、共享客户端、worker 与期限计算后才开放编辑，不能只更改 HTTP client |
| `ML_BACKEND_DEFAULT_URL`                                            | 保留模型市场负责后端配置。代码默认空串，示例值只是部署提示；该字段还参与观测地址回退，不能作为“默认模型服务地址”随意热改                                                       |
| 视频块大小、分段大小、模型追踪窗口、派生资源版本                    | 涉及已存数据布局、结果或容量，需要各自业务域验证；预热数量的可编辑不代表这些字段也可热改                                                                                       |
| 审计保留期、缓存清理天数、对象桶 lifecycle                          | 影响归档/清理结果，应独立规划影响预览与恢复边界，不作为普通高频输入框                                                                                                          |
| 数据库/Redis/对象存储连接与密钥、签名密钥、环境模式、迁移和种子开关 | 留在部署配置和凭据管理                                                                                                                                                         |
| Celery 队列、并发、启动时任务期限、GPU 仲裁和路由模式               | 留在部署及专属运行控制面；本批不提供重启容器按钮                                                                                                                               |

### A7. 系统设置验收

- 超管可读写；项目管理员及普通员工被服务端拒绝。直接构造非法 key、类型和越界值均被拒绝。
- API A 保存 → API B 在缓存合同内读取新值；重启后保持 DB 覆盖；恢复默认后回到当前部署环境值。
- 邀请按两位管理员分别计数，滚动 24 小时边界正确；在线阈值仅改在线状态，不能停用账号或改变登录会话。
- 连接器导入数量等于预算成功，超过一个文件或一个字节提前失败；失败不留下本次部分业务导入。
- 导入排队后修改设置，旧任务与重试保持原快照，新任务使用新快照；文件枚举循环不增加逐文件配置查询。
- 同步阈值两侧分支正确，0 不被回退值吞掉；预热为 0 时不派额外块，既有缓存及主视频请求正常。
- 两位超管并发修改、保存失败、恢复默认、SMTP 清除/保留/更换、表单脏值刷新均按约定工作。
- 验证实际行为而非仅 GET 回显；记录 API、worker 的有效值及非敏感版本，不记录秘密。

## 工作包 B：邀请直接进入目标项目

- 在现有邀请表单增加可选目标项目，复用已有用户组选择器与内置角色。项目成员角色遵守当前模型，不提前实现第三批的跨项目混合角色。
- 新用户接受邀请后建立账号、用户组与目标项目成员关系；已有账号先登录并确认加入项目，不重复注册、不按邮箱默默合并账号。
- 接受时重新校验邀请人仍有管理资格、项目仍存在、角色仍可分配；失败保持可解释状态，避免只建账号却留下不明原因的半完成入职。
- 复制邀请链接仍是完整可用路径；邮件作为已有 SMTP 能力上的可选发送方式，不要求公司先配置邮件系统。
- 接受后显示开始任务或等待分派及负责人；不承诺仅加入项目就已经有激活批次。
- 数据上为邀请增加可选项目关联，旧邀请继续有效；账号创建与正式成员写入使用一致事务。按现有邀请路由增量扩展，不另建第二套邀请系统。

主要文件：`apps/api/app/schemas/invitation.py`、`apps/api/app/db/models/user_invitation.py`、`apps/api/app/services/invitation.py`、邀请相关 API、`apps/web/src/components/users/InviteUserModal.tsx`、邀请接受页面。
验收覆盖新/旧账号、重复接受、过期撤销、项目删除/转让、邀请人被停用，以及注册后确实可见目标项目。

## 工作包 C：新人和项目的开工清单

- 员工首页展示阅读项目规范、打开分派任务、完成首条标注、查看送审结果；步骤由实际业务状态决定，支持跳过与重新打开。
- 复用已有项目指南、创建模板和向导草稿，不增加独立培训服务；练习可先用管理员维护的小样本项目完成。
- 项目设置持续显示类别规范、有效数据、建任务进度、批次、标注员、审核员和激活状态；未完成项直达对应设置。
- 首页优先展示继续上次工作、退回待处理、即将到期；审核员展示待复核和返修重提。统计仍保留，开发占位文案退出产品界面。

主要文件：`apps/web/src/pages/Dashboard/`、`apps/web/src/pages/Workbench/sidebar/GuidePanel.tsx`、`apps/web/src/components/projects/steps/Step7Success.tsx`、`apps/web/src/pages/Projects/ProjectSettingsPage.tsx`。
验收：5 位首次使用的内部同事中，至少 4 位无需口头指导，在 10 分钟内完成一条简单标注并送审；管理员能从清单准确判断成员为什么尚未开工。这是拟定目标，不是当前测量结果。

## 工作包 D：通知和后台任务直达结果

- 任务退回通知进入具体任务并展示最新退回理由；首页退回入口应用对应筛选，不只跳到总列表。
- 复用既有工作台 URL 和上下文恢复入口；检查当前用户权限和目标状态，目标被删除或转派时给出说明，不误开另一条任务。
- 后台作业条目包含项目/数据集、阶段、结果计数和具体详情入口；失败原因可展开。重试只提供给已经具备安全重试语义的作业，避免重复导入或重复预标。
- 保留现有通知偏好、未读同步和历史分页，不另建独立消息中心。

主要文件：`apps/web/src/components/shell/NotificationsPopover.tsx`、`JobsBell.tsx`、相关 dashboard、既有任务/作业详情入口；仅在通知缺少必要定位字段时扩展其 payload。
验收：退回通知一次点击到任务与理由；长导入离开页面后可回来查看该次结果；失去权限、目标不存在和导出链接过期都有可操作说明。

## 工作包 E：成员管理与派发效率

- 用户管理增加紧凑表格，按项目、组、角色、状态筛选；统计、列表和导出使用相同授权范围和筛选口径。
- 新增分页查询契约时保留现有数组接口供选人调用者使用，不能直接把所有用户列表返回改成分页对象。列表与选人搜索分别核对实际调用者。
- 批量邀请/分组操作显示预览及逐项成功/失败结果，重试只处理失败项。项目成员多选已经存在，直接复用。
- 派发前展示逐批次变更、每人新增任务及已有待办；默认只处理未分派项，覆盖已有分派必须明确显示影响。
- 先显示任务数负载，不把不同媒体、难度和工时简单混成自动评分；智能分派在有可靠业务数据后另评估。
- 用户详情展示平台角色、项目身份和可操作范围；改角色前预览跨项目影响。本包做解释与预检，不改变角色架构。
- 设置页接入已有 `useLogoutAll`，明确“退出其他设备”，当前设备继续使用；提示个人 API Key 是独立入口，不将最后活跃时间伪装成设备会话列表。

主要文件：`apps/api/app/api/v1/users.py`、`groups.py`、`invitations_admin.py`、`apps/web/src/pages/Users/UsersPage.tsx`、`apps/web/src/components/projects/ProjectDistributeBatchesModal.tsx`、`BatchAssignmentModal.tsx`、`apps/web/src/hooks/useAuth.ts`。
验收：筛选后统计与导出一致；1000 条成员/邀请数据可分页检索；批量部分失败有准确结果；派发前后映射一致；第二浏览器失效而当前浏览器保留会话。

## 验证、正式文档与运行环境

实施时只运行相关工作包检查，Python 使用当前 checkout 虚拟环境，数据库测试必须指向已核实的一次性测试库。

- 系统设置 API 起点（在 `apps/api`）：`uv run pytest -q tests/test_system_settings_patch.py tests/test_invitations.py tests/test_presence.py tests/test_dataset_import.py tests/test_create_tasks_worker.py tests/test_video_frame_service.py`。补跨进程读取、旧作业快照、显式 reset 和合法 0 的回归。
- Web 起点：`pnpm --filter @anno/web test src/pages/Settings/SettingsPage.test.tsx src/components/shell/NotificationsPopover.test.tsx src/hooks/useAuth.test.tsx`；补各工作包行为测试。
- 执行 `pnpm --filter @anno/web typecheck`，样式变更执行 `pnpm --filter @anno/web lint:css-tokens`；API 变更同步 `pnpm openapi:export`、`pnpm codegen`、`pnpm openapi:check`。
- 系统设置上线需按 `.agents/skills/aap-runtime/SKILL.md` 确认 API 和相关 Celery worker 的实际挂载/镜像，并在刷新后验证新读取路径。仅改配置 UI 不算验收。
- 文档同步 `docs-site/user-guide/reference/settings.md`、用户管理、快速开始、通知、数据导入、批次派发及视频播放指南；相关 API 文档、`README.md`、`DEV.md`、`.env.example` 和 `CHANGELOG.md` Unreleased 一并更新。
- 环境变量说明新增后台覆盖、生效范围和恢复默认语义，运行 `pnpm docs:gen-env-vars`，检查生成文件 `docs-site/dev/reference/env-vars.md` 的变更。
- 如系统设置的跨进程读取合同成为长期约定，在 `docs-site/dev/concepts/` 持久记录；必要时新增 ADR，不能只把约定留在本计划。
- 每次验收结束清理本次测试数据、导入对象、邮件、报告、截图、构建缓存和自建测试进程；不清理原有产物或业务资源。最后执行 `git diff --check`。

## 回退、成本与完成条件

最小交付是工作包 A 的六项设置及真实消费者接线，其他工作包可独立排期；整体规模为较大范围，单个普通入口改善为小至中等范围。
设置扩展不新增部署服务、第三方账号或环境变量控制开关；邮件可继续使用已有公司 SMTP，未配置时保留复制链接。

- 恢复单项配置通过删除 DB 覆盖完成，不要求回滚整个应用或改写 `.env`。
- 代码回退前列出旧代码不识别的新设置键，恢复这些键的部署默认。先排空使用新快照的排队、运行及重试任务，或保留兼容 worker 处理完毕，再回退消费者代码；旧 worker 不识别新快照，不能仅恢复配置就承诺旧任务预算不变，也不能靠调低全局值中断它们。
- 邀请和派发已产生的成员/负责人变化需显式撤销或反向操作，不能用 UI 回滚替代业务回退。
- 最大风险是假热更新、多个消费者读到不同预算，以及低频部署参数被误做成日常设置。以实际 API/worker 行为、快照和作用范围说明验收。
- 完成每个工作包后更新正式文档；全部范围交付后追加 `## Outcome`，准确记录提交、验证和剩余范围。
