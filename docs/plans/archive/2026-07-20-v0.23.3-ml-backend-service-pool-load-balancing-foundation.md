# v0.23.3 · ML Backend 服务池与真实请求负载均衡地基计划

> Status: P0 Accepted（ADR-0050 + inventory + 冻结合同 + fixture 规格 + v0.23.2 baseline 已落地，见附录 §A–§D）· 2026-07-20 · P1–P7 待逐 PR 推进；ADR 被接受前不得合并生产 schema 或请求切换
>
> 发布基线：[v0.23.2 旧 Service 导入路径彻底清零](2026-07-17-v0.23.2-legacy-service-import-elimination.md)
>
> 架构前序：[v0.19.0 全局 ML Backend 注册表](2026-06-25-v0.19.0-global-ml-backend-registry-draft.md) · [v0.22.4 跨 Backend GPU 显存仲裁](2026-07-14-v0.22.4-cross-backend-gpu-memory-arbitration.md) · [ADR-0044](../../adr/archive/0044-global-ml-backend-registry-and-project-enablement.md) · [ADR-0049](../../adr/archive/0049-cross-backend-gpu-memory-arbitration.md)
>
> 后续计划：[v0.23.4 模型市场注册管理与运行时观测结构化重设计](2026-07-20-v0.23.4-model-market-registry-runtime-observability-redesign.md)
>
> 盘点基线：分支 `feat/26-07-18`，commit `f7549b09`。当前基线没有服务池、请求选择器、路由 lease、实例权重、traffic drain、池级项目绑定或路由指标；GPU 仲裁不承担实例选择。

## 1. 结论

v0.23.3 新增一等 **ML Backend 服务池**，把“项目选择一个逻辑能力”和“平台选择一个物理实例”拆成两个明确步骤：

```text
项目 / pipeline stage 请求 service_pool_id
                  │
                  ▼
      MLBackendRouter 原子选择实例
      ├── pool / member 配置与能力兼容
      ├── active / draining / disabled
      ├── health / circuit / max_concurrency
      └── Redis route lease + 加权调度
                  │
                  ▼
       selected registry instance_id
                  │
                  ▼
   既有 MLBackendClient + GPU arbitration
                  │
                  ▼
 Prediction / FailedPrediction / AsyncJob / audit
 同时记录 requested_pool_id + selected_instance_id
```

本版本必须交付**真实请求路由**，不是前端把若干 URL 分组，也不是把 GPU 仲裁改名：

1. `ml_backend_registry` 继续代表一个物理实例，ID、URL、鉴权、health、GPU claim 和 GPU lifecycle identity 不变。
2. 新增服务池、唯一成员关系和项目池绑定；项目、pipeline 和用户偏好以 pool id 为配置真值。
3. 每次推理先由跨进程一致的 router 选择实例并获得 route lease，再进入现有实例 transport 与 GPU 仲裁。
4. 默认只实现一种生产策略：**平滑加权轮询（smooth weighted round robin）+ per-instance 并发上限**。
5. route lease、traffic draining 和被动熔断独立于 GPU request lease、GPU eviction drain 和模型 residency。
6. 不做透明跨实例重试，避免预测重复、状态会话破坏和非幂等副作用；失败只影响后续选择。
7. 同步交付 topology、runtime snapshot、drain / resume 和路由指标合同，作为 v0.23.4 的硬前置。

v0.23.3 是后端架构与第一方调用链切换版本。模型市场「注册管理 / 运行时观测」的完整池化 UI 留给 v0.23.4；本版本只做保证项目选择、创建向导和现有页面不失效所需的最小前端兼容。

## 2. 当前基线审计

### 2.1 当前对象层级

| 当前对象                               | 当前语义                                           | 负载均衡缺口                        |
| -------------------------------------- | -------------------------------------------------- | ----------------------------------- |
| `MLBackendRegistry`                    | 一行 = 一个唯一 URL、能力快照、并发配置、GPU claim | 没有逻辑服务池或成员关系            |
| `ProjectMLBackend`                     | 项目直接启用 registry 实例                         | 项目无法绑定一个多实例能力          |
| `Project.ml_backend_id`                | 项目主实例                                         | 主配置与物理实例耦合                |
| `preannotate_pipeline[].ml_backend_id` | 每阶段精确实例                                     | stage 不能在等价副本间选择          |
| 用户 AI 偏好中的 backend id            | 变体、模型、交互后端按实例分桶                     | 副本扩容会复制偏好或漂移            |
| `Prediction.ml_backend_id`             | 实际生成结果的物理实例                             | 缺少 requested pool 溯源            |
| `FailedPrediction.ml_backend_id`       | 失败发生的物理实例                                 | 无法区分池请求和实例失败            |
| `AsyncJob.payload.ml_backend_id`       | 请求时指定的实例                                   | 没有 pool / selected instance 双 ID |

### 2.2 当前请求选择

- `get_project_backend()` 优先 `Project.ml_backend_id`，否则取第一个 connected interactive 实例。
- tracker 能力匹配后返回显式绑定实例或 `supporting[0]`。
- 同步 API 直接按 backend id 构造 `MLBackendClient`。
- batch worker 在 job 启动时 `db.get(registry_id)`，源阶段整批复用同一实例；pipeline 下游阶段也按 `ml_backend_id` 逐一加载实例。
- 创建项目、项目启用、AIPreAnnotate、保存 pipeline、用户偏好和重试都传递 registry id。

这些路径没有原子实例选择、权重、公平性、跨进程 inflight、traffic drain 或 circuit breaker。

### 2.3 GPU 仲裁不是负载均衡

ADR-0049 和 v0.22.4 明确不做同一 backend 多副本、负载均衡和 scale-out。GPU arbitration 的职责是：在**实例已经确定之后**，按 `backend_registry_id + gpu_resource_id` 管理显存预算、request lease、卡级队列、驻留和驱逐。

因此：

- route lease 不能复用只在 GPU enforce 下完整生效的 GPU request lease。
- CPU 实例、GPU off / observe 模式和无 GPU claim 的实例也必须参与路由并发控制。
- traffic draining 只阻止新请求选中实例；GPU residency draining 是显存驱逐生命周期，二者不得共享状态字段或 ACK。
- 最终 selected registry id 必须原样进入 GPU dispatch，不能把 pool id 当 GPU backend identity。

### 2.4 当前观测缺口

- `/admin/ml-integrations/observe` 逐 URL 探测 health / setup，没有 pool、weight、routable、selection 或 route inflight。
- `ml_backend_request_duration_seconds` 只有 backend id / outcome，无法还原池级分布。
- `extra_params.max_concurrency` 在 off / observe 下主要是进程内 semaphore，不是跨 API / Celery 的全局路由容量。
- GPU `/gpu-resources` 的 backend / card queue 只表达 GPU 准入，不是路由队列。

## 3. 目标、非目标与不变量

### 3.1 目标

- 建立 service pool → registry instance 的一对多模型，一个实例只能属于一个服务池。
- 迁移项目启用、主 backend、pipeline stage 和用户偏好到 pool id。
- 建立跨进程原子选择与 route lease，统一约束 API、Celery 和视频 / pipeline 调用。
- 支持成员权重、per-instance max concurrency、active / draining / disabled 和新鲜 health 门控。
- 支持 transport 失败的被动熔断，使后续请求绕开暂时不可用实例。
- 保留实例级 GPU 仲裁、能力快照、鉴权、生命周期和历史 prediction 溯源。
- 所有新请求记录 requested pool 和 selected instance 双 ID。
- 提供 pool CRUD、成员管理、项目池启用、drain / resume、topology 和 runtime snapshot API。
- 建立 off / observe / enforce 灰度模式和 fail-closed enforce 语义。
- 保持旧实例 ID 请求的有限兼容，使 SDK / 旧 Web 在迁移窗口内可解析到对应 pool。
- 同步 OpenAPI、正式文档、环境变量文档、CHANGELOG 和版本源。

### 3.2 明确不做

- 不做 autoscaling、自动注册副本、服务发现、跨节点编排或 Kubernetes controller。
- 不做区域 / 可用区感知、数据本地性、成本路由、延迟预测或自适应权重。
- 不同时提供多种调度算法；首版只实现 smooth weighted round robin。
- 不做全局路由等待队列；全部可用实例达到并发上限时直接返回结构化 503。
- 不做自动跨实例 retry、hedged request 或 speculative execution。
- 不迁移正在运行的 tracker / session；有状态执行单元在选定实例上保持 pin。
- 不把不同 capability、模型目录或参数 schema 的实例强行放进同一池。
- 不把 warmup、reload、unload、health check 等实例生命周期动作自动负载均衡。
- 不删除现有 registry CRUD、实例 health / unload API 或旧请求字段；清理另行排期。
- 不重构模型市场完整管理 / 观测 UI；该工作属于 v0.23.4。
- 不修改 Backend `/health`、`/setup`、`/predict` wire protocol 来承载平台内部路由状态。

### 3.3 冻结不变量

1. pool id 是逻辑请求身份；registry id 是物理执行身份，二者永不互换。
2. 一个 registry instance 同时最多属于一个 service pool。
3. 一个 service pool 的全部 active 成员必须通过 routing capability fingerprint 校验。
4. 项目只能请求已启用的 pool；router 不能越过项目绑定选择实例。
5. 只有 `traffic_state=active` 的成员可接收新 route lease。
6. draining 成员保留既有 lease，不接收新 lease；inflight 归零后才能 disable、移出或普通 unload。
7. route lease acquire / heartbeat / finish / cancel 使用 Redis 原子合同；进程退出最终由 TTL 回收。
8. Redis 在 enforce 下不可用时 fail closed；不能静默直连 `legacy_instance_id`。
9. GPU dispatch 始终使用 selected registry id，现有 generation / fence / membership 不池化。
10. 单阶段 Prediction / FailedPrediction 的 `ml_backend_id` 表示 selected instance，新增字段表示 requested pool；多阶段聚合必须另存每阶段 / 调用的双 ID，不能把 root instance 冒充全部 shape 的生产者。
11. 没有真实指标时返回 unknown / null，不把缺失值解释成 0 或健康。
12. 被动熔断只作用于后续请求，不重放已经失败的非幂等预测。
13. 非空且 enabled 的 pool 必须有 `legacy_instance_id`，且它必须是本池成员；它只服务 off / observe 兼容，不参与 enforce 优先级。空 pool 只能 disabled。
14. pool / member / weight / traffic state 任何变更都单调增加 `routing_generation`，旧 generation 不得 acquire。

## 4. 架构决策与 ADR

P0 新增 `docs/adr/0050-ml-backend-service-pools-and-request-routing.md`，至少冻结：

- 为什么在 ADR-0044 的全局实例注册表之上增加服务池，而不是改变 registry 行语义。
- 为什么 pool / instance 使用双 ID，prediction 为什么保留实例溯源。
- 为什么一个实例首版只能属于一个 pool。
- 为什么首版选择 smooth weighted round robin，而不是随机、least latency 或多策略插件。
- 为什么 routing ledger 与 GPU arbitration ledger 分离。
- 为什么不做透明 retry。
- 为什么项目绑定迁移到 pool，生命周期控制仍定位 instance。
- migration 的 forward-only 条件和旧 ID 兼容窗口。
- v0.23.4 如何只消费 topology / runtime contract，不复制 router 判定。

ADR 被接受之前可以完成 inventory、fixture 和实验脚本，但不得合并生产 schema 或请求切换。

## 5. 目标数据模型

### 5.1 `ml_backend_service_pools`

| 字段                      | 约束与语义                                                                                                                         |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `id`                      | UUID PK，逻辑请求稳定身份                                                                                                          |
| `name`                    | 显示名；不作为 join key                                                                                                            |
| `enabled`                 | 全局接流开关；显式创建的空 pool 默认 false，singleton backfill / 自动创建按既有状态设置                                            |
| `routing_policy`          | 首版只允许 `smooth_weighted_round_robin`                                                                                           |
| `legacy_instance_id`      | nullable FK → registry；非空 enabled pool 必须指向本池成员，仅供 off / observe 实际 dispatch 与受控 downgrade，不影响 enforce 权重 |
| `routing_generation`      | 正整数、单调递增；pool / member / weight / traffic state 变更时同事务推进                                                          |
| `capability_fingerprint`  | 路由相关能力规范化摘要；无可信快照时为 null 且池不可路由                                                                           |
| `capability_snapshot`     | 池级 canonical capability，只保存路由相关字段                                                                                      |
| `created_at / updated_at` | 审计与 topology 新鲜度                                                                                                             |

不在池表保存 URL、auth token、GPU claim、model residency 或实例 health。

### 5.2 `ml_backend_pool_members`

| 字段                      | 约束与语义                                          |
| ------------------------- | --------------------------------------------------- |
| `id`                      | UUID PK                                             |
| `pool_id`                 | FK → service pool；`(pool_id, registry_id)` unique  |
| `registry_id`             | FK → registry；单列 unique，保证实例只属于一个 pool |
| `traffic_state`           | `active / draining / disabled`，DB CHECK            |
| `weight`                  | 1..100，默认 1；仅影响分配比例，不突破实例并发上限  |
| `created_at / updated_at` | 配置变更时间                                        |

删除 registry 前必须先完成 drain、inflight=0、GPU retirement 和成员移除；若它是 `legacy_instance_id`，还必须先显式选择另一个 quiescent 成员接替，或把空 pool 原子置 disabled 并清空该指针。FK 使用 RESTRICT，不能靠 cascade 绕过控制线。

### 5.3 项目绑定

将 `project_ml_backend` 迁移为 `project_ml_backend_pool`：

- `registry_id` 改为 `pool_id`，唯一约束变为 `(project_id, pool_id)`。
- `enabled` 和项目级 `default_variants` 保留，但覆盖语义改为 pool 级。
- `Project.ml_backend_id` 迁移并重命名为 `ml_backend_pool_id`，成为项目主服务池。
- 项目启用和主池必须一致；禁用主池时显式清空或要求先换主池，不能留悬空引用。
- disabled、空成员或无 capability fingerprint 的 pool 可以被超管预配置，但不能被项目启用。

### 5.4 请求与结果溯源

- `Prediction.ml_backend_id`：单阶段保存 selected registry instance；多阶段聚合行保存 root stage selected instance，并明确列语义不是全部 shape producer。
- `Prediction.ml_backend_pool_id`：新增；单阶段保存 requested pool，多阶段保存 root stage pool。
- `PredictionMeta.extra.pipeline.selections[]`：保存 stage / invocation 的 pool id、selected instance id、operation 与 shape / result 关联；同一 Prediction 聚合多个实例结果时以此为完整 lineage。
- `FailedPrediction` 保存失败所在 stage / pool + selected instance；若选择前失败，instance id 可为 null，不能一律写 root instance。
- `AsyncJob.payload / result`：新任务写 `ml_backend_pool_id`、每阶段 pool id，以及 selection 汇总；实例级失败记录 selected instance。
- audit：触发事件写 pool id；实际调用 / 失败 / drain 事件写 pool + instance。
- 结构化日志统一字段 `service_pool_id`、`backend_registry_id`、`route_lease_id`、`routing_outcome`。

### 5.5 JSONB 和用户偏好

必须盘点并迁移：

- `projects.preannotate_pipeline[].ml_backend_id` → `ml_backend_pool_id`。
- `projects.default_variants` 的 backend key → pool key。
- `users.preferences.ai.params_by_backend`。
- `users.preferences.ai.model_by_backend`。
- `users.preferences.ai.interactive_backend_by_project`。
- `users.preferences.ai.secondary_by_model` 内所有 backend 引用。
- AsyncJob 历史 payload 中可确定映射的 `ml_backend_id` 补 `ml_backend_pool_id`，原字段保留为历史实例证据。

所有 JSONB 迁移必须以 machine-readable registry → singleton pool 映射表驱动，不用名称或 URL 重算。

## 6. Alembic 迁移与兼容

### 6.1 一对一安全 backfill

迁移建议使用下一 revision（当前最新为 `0131`），分阶段执行：

1. 新建 pool / member 表和约束。
2. 为每条现有 registry 建一个 singleton pool，pool 名称取 registry 名称、`legacy_instance_id` 指向该 registry；生成并持久保存 exact registry → pool 映射。
3. 每个实例成为对应 pool 的 active、weight=1 成员。
4. 迁移 `project_ml_backend` 到 pool 关联，保留原关联行的 id、enabled、default_variants 和时间。
5. 增加并回填 `Project.ml_backend_pool_id`。
6. 增加 prediction / failed prediction 的 pool FK，并在父表 / 分区上验证。
7. 迁移 pipeline 与用户偏好 JSONB。
8. 为历史 AsyncJob 补可确定的 pool id，不改审计日志历史事实。
9. 建索引、FK、CHECK 后执行 orphan / cardinality / JSON shape 守卫。

backfill 完成后，每个原请求仍落到唯一原实例，行为必须与 v0.23.2 完全一致。只有管理员显式把新实例加入既有 pool 并切换 rollout mode 后，才产生多实例分配。

### 6.2 兼容边界

- 新 API 和第一方 Web / worker 使用 `ml_backend_pool_id`。
- 旧请求中的 `ml_backend_id` 先尝试解析为 legacy registry id，再解析其唯一 pool；响应增加弃用提示，但本版本不删除字段。
- lifecycle API 的 `{registry_id}` 永远继续表示实例，不套用上述别名。
- 旧 `/admin/ml-integrations/all` 保持实例列表，供 v0.23.3 现有模型市场页面继续工作。
- 项目设置和创建向导做最小切换，展示 pool 名称 / 成员数，提交 pool id；完整池管理留给 v0.23.4。
- SDK 若暴露推理 / 项目配置写路径，同步增加 pool 参数；旧 instance 参数走相同 resolver。

### 6.3 downgrade

在所有 pool 都是 migration 生成的 singleton、且没有新 pool / 多成员 / pool-only 配置时，可以按保存映射逆迁移。

一旦创建多成员 pool、变更 / 删除 legacy instance 或写入 pool-only pipeline / preference，downgrade 不再无损，迁移必须 fail closed 并提示 forward-only。不能把多成员配置静默压回任意实例。

## 7. Capability 等价合同

服务池表示可互换的逻辑能力，成员加入前必须执行新鲜 `/setup` 探测并计算 canonical fingerprint。

### 7.1 fingerprint 包含

- capability protocol version。
- model ids、task、modality、infra。
- model / weights version 或等价 build revision；首版不支持版本混跑 / canary。
- supported prompts / inputs / outputs / trackers。
- parameter JSON schema 中影响请求合法性的字段。
- variant axes、枚举和默认值。
- stateful / batchable / warmup 等路由相关能力。

### 7.2 fingerprint 排除

- URL、实例名称、auth、health 时间。
- GPU UUID、显存占用、temperature、compute 实时状态。
- model residency、cache、builder / borrower。
- 纯展示描述和不影响请求兼容的实例元数据。

### 7.3 变化处理

- 创建 pool 时以 seed instance 生成 pool snapshot / fingerprint。
- 加成员必须 exact match；不匹配返回 409 `ml_backend_pool_capability_mismatch` 和结构化 diff。
- active 成员后续 health 刷新发现 fingerprint 漂移时，原子改为 disabled，记录诊断和审计；不能继续路由后再由请求暴露错误。
- pool capability 变更必须在全部成员 disabled / quiescent 后显式执行，不自动取多数派。

fingerprint canonicalization、排序、缺省值和 SHA 算法必须有 golden fixture，前后端不重复实现。

## 8. 路由领域与依赖边界

新增 `app/services/ml_routing/`：

```text
ml_routing/
├── contracts.py       # pool / selection / lease / error 合同
├── capability.py      # canonical snapshot 与 fingerprint
├── policy.py          # eligibility 与 smooth weighted round robin 输入
├── ledger.py          # Redis keys、Lua acquire / heartbeat / finish / cancel
├── circuit.py         # 被动失败分类与 ejection 状态
├── router.py          # DB topology + ledger 编排，返回 RouteSelection
├── diagnostics.py     # topology / runtime 诊断
└── metrics.py         # pool / instance 路由指标
```

边界：

- `MLBackendRouter` 返回 selected `MLBackendRegistry` 与 route lease，不发 HTTP 请求。
- 既有 `MLBackendClient` 仍只负责一个实例的 transport、auth、local semaphore 和 GPU dispatch。
- router 可以依赖 registry model、routing ledger 和 capability；不得导入 API router 或 worker。
- `MLBackendClient` 不反向导入高层 router，避免新的循环依赖。
- 调用方使用 context manager / `try/finally` 保证 finish / cancel，并显式上报 outcome；同一 lease 只能提交一个终态。
- GPU arbitration 不导入 ml_routing；route selection 完成后单向调用实例 client。

禁止把全部实现塞回现有 `ml_backend.py` 或 `ml_client.py`，也不新增泛化 BaseRouter / repository 框架。

router 的候选构建顺序固定为：先验证项目已启用 pool 与 pool enabled，再从 DB 读取同一 `routing_generation` 下的 active member；registry 必须 connected、health 未过期、capability fingerprint exact match。Redis acquire 再原子排除 circuit open 与达到 max concurrency 的成员。任何一步不确定都返回结构化 reason，不回落到名称、URL 或 `legacy_instance_id` 猜测。

## 9. Redis routing ledger

### 9.1 原子合同

acquire Lua 在一个原子步骤完成：

1. 校验 pool / candidate generation。
2. 清理过期 route lease。
3. 校验调用方传入的 active / fresh / capability-compatible candidates，并排除 circuit open 和达到 max concurrency 的实例。
4. 对剩余候选执行 smooth weighted round robin。
5. 写入 exact lease id / pool / instance / owner / operation / expiry。
6. 更新 current weight、selection counter 和运行时 bucket。
7. 返回 selected instance 或结构化 rejection reason。

heartbeat 只能延长 exact 未过期 lease，不能改变 pool / instance / owner。正常终态使用 `finish(lease_id, outcome, duration)` 原子记录 metrics、更新 passive circuit 并释放；caller cancel 使用不计 transport failure 的 `cancel`。两者都必须 exact、幂等，重复终态返回同一稳定结果，不能重复累计指标。

### 9.2 推荐 key namespace

```text
ml-router:v1:pool:{pool_id}:state
ml-router:v1:pool:{pool_id}:member:{instance_id}:leases
ml-router:v1:lease:{lease_id}
ml-router:v1:pool:{pool_id}:member:{instance_id}:circuit
ml-router:v1:pool:{pool_id}:metrics:{bucket}
```

key、KEYS / ARGV 顺序、TTL、返回码和 Lua SHA-256 在 P1 冻结 golden；不能与 `gpu-arbiter:*` namespace 交叉。

`metrics:{bucket}` 使用固定分钟桶和有限 histogram 边界，至少保存 selection、success、error、rejection 和 duration buckets；bucket 过期自动回收，不保存 request / lease 级高基数明细。runtime snapshot 聚合最近窗口并返回 request count、error rate 和近似 P95，同时明确窗口起止和精度。

### 9.3 并发语义

- `extra_params.max_concurrency` 继续是实例配置，router 在所有 rollout mode 的 enforce 路径以 route lease 做跨进程上限。
- local semaphore 和 GPU backend lease 暂时保留为 defense-in-depth，不在本版本删除。
- route inflight 包含已选中后等待 local / GPU 准入的请求，表达实例承担的全部未完成需求。
- 首版没有 router queue；所有候选达到上限时返回 503 `ml_backend_pool_saturated` 和 `Retry-After`。
- route lease TTL 必须大于正常 transport timeout，并由长任务 heartbeat；固定 TTL 不因普通轮询无限续命。

### 9.4 故障语义

- Redis acquire 不可用：observe 只记录诊断并继续 legacy dispatch；enforce 返回 503 `ml_backend_router_unavailable`。
- 无 active / healthy 成员：503 `ml_backend_pool_unavailable`。
- 全部达到并发上限：503 `ml_backend_pool_saturated`。
- capability mismatch：成员不进入候选，不在运行时临时降级请求 schema。
- caller cancel 走 cancel，成功 / 可分类失败走 finish；无法分类的异常在 finally 保守 cancel，进程 crash 由 TTL 清理。

## 10. 调度、熔断与 draining

### 10.1 Smooth weighted round robin

首版只支持一个 policy：

- 每轮 eligible 成员 `current_weight += configured_weight`。
- 选择 current weight 最大者；并列按稳定 instance UUID 排序。
- 选中者 `current_weight -= eligible_total_weight`。
- 达到 max concurrency 的成员先排除，不参与当轮权重推进。
- membership / weight generation 改变时，旧 current weight 按新集合安全重置。

该策略提供确定、可测试的权重比例；不宣称根据 latency 或实时 inflight 最优。后续若需要 least-request，单独新增 ADR / feature，不在本版本预留插件框架。

### 10.2 被动熔断

- 只把连接拒绝、connect/read timeout、无 HTTP 响应和受控 gateway unavailable 计为 transport failure。
- 业务 4xx、模型校验失败、GPU capacity 503、主动 cancel 不触发实例 ejection。
- 连续失败达到配置阈值后在 Redis 写 `open_until`，后续 selection 排除该实例。
- 新鲜 health success 或 cooldown 后进入一次 half-open probe；成功关闭，失败重新 open。
- 不自动重试当前失败请求，避免重复 prediction 和 stateful session 损坏。

### 10.3 Traffic draining

状态机：

```text
active --drain--> draining --inflight=0--> disabled
   ^                    |
   └------ resume ------┘
```

- drain mutation 先持久化 membership state，再使新 acquire 不可选。
- runtime snapshot 显示 exact inflight 和最老 lease deadline。
- draining 时可以 resume；disabled 后 resume 需重新验证 health / capability。
- 移除成员、删除实例和普通 unload 要求 disabled + route inflight=0。
- traffic drain 完成不证明 GPU residency 已释放；卸载仍走现有 GPU / backend lifecycle proof。
- `traffic_state` 是 configured state；runtime 另返回由 rollout mode 决定的 `effective_routing_state`。
- off / observe 下可以预配置成员状态，但实际 legacy dispatch 不受它门控，drain 响应必须标 `effective=false`，也不能用 shadow inflight 证明安全卸载。
- 只有 enforce 下的 draining + 正式 route inflight=0 才形成 traffic quiescence proof；v0.23.4 不得在其它 mode 显示“已停流”。

## 11. 路由选择作用域

| 调用类型                         | 选择 / lease 作用域        | 说明                                         |
| -------------------------------- | -------------------------- | -------------------------------------------- |
| 同步 interactive / predict       | 单 HTTP 业务请求           | 请求完成走 finish，主动取消走 cancel         |
| batch 单阶段                     | 每个 task / execution unit | 不把整批永久压在一个实例                     |
| pipeline                         | 每个 stage invocation      | stage 保存 pool id；不同调用可选不同等价实例 |
| video tracker / stateful session | 整个 job / session pin     | heartbeat 保活，不能中途换实例               |
| retry failed prediction          | 新的显式请求               | 可以选择不同实例，但写新 selection 证据      |
| health / setup                   | 精确实例                   | 不经过 pool router                           |
| warmup / reload / unload         | 精确实例                   | 管理员明确选择目标，禁止静默挑副本           |

同一 stateful job 的 affinity 只在 job 内部显式保存 selected instance，不实现通用 cookie / user sticky session。

## 12. API 合同与权限

### 12.1 Super Admin pool / member API

推荐并在 P0 冻结：

```http
GET    /admin/ml-integrations/service-pools
POST   /admin/ml-integrations/service-pools
GET    /admin/ml-integrations/service-pools/:pool_id
PATCH  /admin/ml-integrations/service-pools/:pool_id
DELETE /admin/ml-integrations/service-pools/:pool_id

PUT    /admin/ml-integrations/service-pools/:pool_id/members/:registry_id
DELETE /admin/ml-integrations/service-pools/:pool_id/members/:registry_id
POST   /admin/ml-integrations/service-pools/:pool_id/members/:registry_id/drain
POST   /admin/ml-integrations/service-pools/:pool_id/members/:registry_id/resume
```

创建 pool 可以先得到一个 disabled 空池；加入第一个成员时设置 `legacy_instance_id`，完成新鲜 capability 校验后才能 enabled。注册新实例时 `service_pool_id` 可选；缺省在同一事务创建 singleton pool。首版不做任意 pool merge；新增副本应在注册时直接指定目标 pool。

### 12.2 项目 pool API

```http
GET /projects/:project_id/ml-backend-pools/available
PUT /projects/:project_id/ml-backend-pools/:pool_id/enablement
```

Project Admin 只能修改自己有管理权限的项目绑定，不能改 pool 成员、权重或 traffic state。

### 12.3 v0.23.4 读模型

```http
GET /admin/ml-integrations/topology
GET /admin/ml-integrations/runtime-snapshot
```

- topology 允许 Project Admin / Super Admin，但按角色服务端裁剪。
- Project Admin 只得到当前 `/all` 已允许的基础 pool / instance 摘要。
- runtime snapshot 只允许 Super Admin，返回 pool、member、router metrics、health、GPU 摘要、residency、sources freshness 和 diagnostics。
- 两个端点按稳定 ID join；不能让 v0.23.4 继续按 URL 拼 `/all + /overview + /observe`。

### 12.4 错误码

至少冻结：

- `ml_backend_pool_not_enabled`
- `ml_backend_pool_unavailable`
- `ml_backend_pool_saturated`
- `ml_backend_router_unavailable`
- `ml_backend_pool_capability_mismatch`
- `ml_backend_member_draining`
- `ml_backend_member_not_quiescent`
- `ml_backend_legacy_instance_unmapped`

错误 body、HTTP status、Retry-After、日志 reason 和 OpenAPI schema 必须一致。

## 13. 第一方调用链切换

P0 建 machine-readable inventory，至少覆盖：

- 项目创建 / 克隆、项目设置启用和主 backend。
- CreateProjectWizard、项目 ML 模型选择器。
- interactive annotation 全部入口。
- AIPreAnnotate 单阶段、多阶段、逐帧和 retry。
- batch worker、pipeline stage client、fan-out child。
- video tracker 能力选择与 job pin。
- warmup / reload / unload / smoke-test 的实例作用域守卫。
- SDK、CLI、测试 fixture、截图 seed 和示例。
- AsyncJob / Prediction / FailedPrediction / audit / notification / WebSocket 字段。

切换后调用形态统一：

```text
resolve project + pool permission
→ router.acquire(pool_id, operation, owner)
→ selected registry instance
→ MLBackendClient(instance)
→ GPU dispatch + HTTP
→ record pool / instance / outcome
→ router.finish() / cancel() exactly once
```

任何第一方生产路径直接 `db.get(MLBackendRegistry, requested_id)` 并发起推理，都属于未迁移；实例 lifecycle / health 路径除外。

## 14. Rollout 模式

新增：

```env
ML_BACKEND_ROUTER_MODE=off
ML_BACKEND_ROUTER_HEALTH_MAX_AGE_SECONDS=90
ML_BACKEND_ROUTER_LEASE_TTL_SECONDS=120
ML_BACKEND_ROUTER_HEARTBEAT_INTERVAL_SECONDS=15
ML_BACKEND_ROUTER_PASSIVE_FAILURE_THRESHOLD=3
ML_BACKEND_ROUTER_EJECT_SECONDS=30
```

最终名称和范围在 P0 按 typed settings 冻结；同步 `.env.example`，运行 `pnpm docs:gen-env-vars` 并检查 `DEV.md`。

模式语义：

| mode      | 实际实例                                                          | routing ledger                                       | 失败处理                              |
| --------- | ----------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------- |
| `off`     | 请求携带的 legacy instance；新 pool 请求使用 `legacy_instance_id` | 不 acquire                                           | 保持 v0.23.2 行为                     |
| `observe` | 请求携带的 legacy instance；新 pool 请求使用 `legacy_instance_id` | shadow namespace 计算 would-select、记录诊断，不门控 | ledger 失败不阻断实际请求             |
| `enforce` | router selected instance                                          | 正式 acquire / heartbeat / finish / cancel           | Redis / topology 不确定时 fail closed |

进入 enforce 前必须满足：

- migration / backfill 零 orphan。
- 全部第一方调用方已传 pool id。
- observe 窗口中 actual / would-select capability 一致，无未知实例映射。
- route lease cleanup、进程 crash TTL、Redis 故障和 worker cancel 演练通过。
- 至少一个真实双实例 pool 完成比例、饱和、drain、ejection 和 GPU 交互验收。

本版本不引入复杂 durable rollout state；环境 mode 是单一部署级开关。API / worker / beat 必须读取相同配置，部署时整体重建 / 重启，不能混跑不同 mode。

## 15. 观测与指标

新增指标时保留现有 metric，不原地改变 label contract：

- `ml_backend_router_selections_total{pool_id,instance_id,outcome}`。
- `ml_backend_router_rejections_total{pool_id,reason}`。
- `ml_backend_router_ejections_total{pool_id,instance_id,reason}`。
- `ml_backend_routed_request_duration_seconds{pool_id,instance_id,outcome}`。
- route inflight / lease deadline 从 Redis snapshot 聚合，避免多进程 Gauge 各写各的。

runtime snapshot 至少返回：

- router desired / effective mode 与 runtime readiness。
- pool enabled / policy / total / routable。
- member configured traffic state、effective routing state、weight、health、routable reason。
- inflight / max、selection window、rejection、last selected。
- circuit state / open until。
- route snapshot、health、GPU、residency 各自 observed_at / stale / error。
- diagnostics 使用 `code + subject_type + subject_id` 稳定去重键。

Prometheus label 只用稳定 UUID / 受控 outcome，不放 URL、错误文本、lease id 或用户输入，避免基数爆炸。

## 16. 实施阶段与 PR 边界

### P0 · ADR、inventory 与 golden fixture

1. 新增并接受 ADR-0050。
2. 冻结 pool / instance / lease / drain / circuit 术语和不变量。
3. 生成所有 registry-id consumer、JSONB 引用和 lifecycle 例外 inventory。
4. 冻结 schema、migration 映射、API、错误码、Redis key 和 Lua I/O。
5. 准备 singleton、双实例、能力不匹配、饱和、draining、circuit、Redis failure fixture。
6. 保存 v0.23.2 行为 baseline 和 OpenAPI snapshot diff 预期。

验收：每个实例 ID 使用点都分类为“迁 pool / 保实例 / 双 ID”，没有未分类调用方。

### P1 · 数据模型与一对一迁移

1. 落 service pool / member 和项目 pool binding 模型。
2. 执行 singleton backfill、项目主池、prediction / failed pool id 和 JSONB 迁移。
3. 更新 ORM、schema 和最小 CRUD，不切实际请求选择。
4. 增加 upgrade / conditional downgrade、分区 prediction、orphan 和 mapping 测试。

验收：off mode 下全量行为与 v0.23.2 一致；每个 registry 恰有一个 singleton pool。

### P2 · Routing ledger 与 capability contract

1. 实现 capability canonicalization / fingerprint / diff。
2. 实现 Redis acquire / heartbeat / finish / cancel、SWRR、capacity 和 snapshot。
3. 实现被动熔断原语和 shadow / enforce namespace。
4. 建 Lua digest、key、TTL、并发、cancel、crash 和 replay golden。

验收：多进程竞争不超 max，权重长期比例符合 fixture，lease 无泄漏，GPU key 零变化。

### P3 · Router domain 与 observe 接线

1. 实现 `MLBackendRouter` 和 legacy instance → pool resolver。
2. 在全部第一方推理路径前接 route context，但 mode 保持 off / observe。
3. 写 pool / instance 双 ID 到 job、prediction、failure、audit 和 logs。
4. stateful tracker / session 实现 job-scope pin 与 heartbeat。
5. 对比 actual / would-select，建立 unknown / mismatch diagnostics。

验收：observe 不改变实际实例或结果；所有推理路径都有 would-select 证据，lifecycle 路径不误入 router。

### P4 · 项目合同与第一方 Web 兼容

1. 项目启用、主 pool、创建向导、pipeline、用户偏好切到 pool id。
2. 增加 pool availability / enablement API 和 legacy request alias。
3. 现有项目设置做最小 pool 选择展示；模型市场大改留给 v0.23.4。
4. SDK / CLI / OpenAPI generated types 同步。
5. 迁移旧 bookmark / request / stored JSON fixture。

验收：项目只能选择已启用 pool；前端不把 selected instance 写回项目配置。

### P5 · Enforce、drain、circuit 与管理快照

1. enforce 使用 selected instance 发请求并 fail closed。
2. 落成员 drain / resume / disable / remove 安全门。
3. 接 passive ejection 和 half-open probe。
4. 落 topology / runtime snapshot、sources freshness 和 diagnostics。
5. 落 Prometheus metrics、审计和运维诊断。

验收：真实双实例池通过权重、并发、故障绕行、drain、恢复和 GPU 交互测试。

### P6 · 正式文档、灰度与稳定窗口

1. 更新 ADR、开发概念、协议中的平台注册层、用户指南和 observability / runbook。
2. 更新 `.env.example` 并重新生成 env 文档。
3. 在目标环境依次 off → observe → enforce，保存每阶段起止、指标、错误和批准人。
4. 运行真实 API / Celery / tracker / pipeline smoke。
5. 为 v0.23.4 回填 release commit、schema、endpoints、metrics、drain 和测试证据。

验收：enforce 稳定窗口无未知映射、route lease 泄漏、错误率回归或能力错路由。

### P7 · Release 与 Outcome

1. 全部 required checks 为 0 失败。
2. 更新 CHANGELOG `Unreleased`，按 Added / Changed / Fixed 分组。
3. 发布时同步 API config / pyproject、Web package、uv.lock 和 OpenAPI version。
4. 依环境变量、依赖和镜像内代码变化执行 Docker rebuild / recreate，确认运行态 `/health`。
5. 在本文末追加真实 `## Outcome`，列出 ADR、migration、release commit、测试、灰度和残余风险。
6. 将同一份证据链接进 v0.23.4 的进入门，再允许其 implementation ready。

## 17. 验证矩阵

### 17.1 Schema 与迁移

- 0 registry、单 registry、多 registry、多项目 fixture。
- singleton backfill 数量、映射唯一、FK、CHECK、索引和时间保留。
- project binding / main pool / default variants / pipeline / user preferences 精确迁移。
- partitioned predictions 与 failed predictions 双 ID。
- historical AsyncJob 可确定映射补齐，不篡改 audit 历史。
- upgrade 幂等防护；singleton downgrade 成功，多成员 downgrade fail closed。
- env sync 新建 registry 时 singleton pool 原子创建，重启不重复。

### 17.2 Capability

- canonical field order / list order / missing defaults 不影响 fingerprint。
- task / model / input / output / param / variant 不兼容产生精确 diff。
- health refresh capability 漂移使成员退出路由。
- 无 capability snapshot 的成员不可 active。
- 同名 / 同 URL 前缀不会自动合池。

### 17.3 Routing ledger

- weight 1:1、1:2、1:10 的长期分布。
- max concurrency 在 API + 多 Celery 进程竞争下不超限。
- saturated / unavailable / Redis unavailable reason 稳定。
- heartbeat、正常 finish、重复 finish、finish / cancel 竞争、timeout、进程 crash TTL。
- membership / weight change generation 与 SWRR state reset。
- circuit open / half-open / close，非 transport 错误不误熔断。
- off / observe / enforce namespace 和行为完全隔离。

### 17.4 请求链路

- interactive、batch、pipeline、frame fan-out、tracker、retry。
- project 未启用 pool、pool disabled、member draining、全部饱和。
- pipeline 每 stage pool 验证，selected instance 精确进入 client / GPU dispatch。
- 多阶段聚合 Prediction 的 root 双 ID 与 `PredictionMeta.extra.pipeline.selections[]` 完整，stage failure 不再错误归因 root instance。
- tracker 整 job pin，heartbeat 中断和 cancel 清理。
- 不透明重试：一次业务调用最多一次实例 transport。
- lifecycle / health / warmup / unload 仍精确实例，不经过 router。

### 17.5 GPU 交互

- selected registry id 与 GPU membership / token / lease identity 一致。
- route lease 等待 GPU queue 时计入 route inflight，最终全部释放。
- GPU off / observe / enforce 三种模式都能 route。
- CPU 和无 GPU claim 实例可 route，不触发 GPU ledger。
- traffic drain 与 GPU drain 可同时出现且状态、日志、诊断不混淆。
- 既有 GPU Redis key、Lua digest、generation、fence 和 rollout 测试不回归。

### 17.6 API、权限与观测

- Super Admin pool / member CRUD 和 drain / resume。
- Project Admin 只管理本项目 pool binding。
- topology 双角色服务端裁剪；runtime snapshot Project Admin 403。
- pool / instance 双 ID 在 response、audit、logs、metrics 一致。
- metrics label 基数守卫，URL / lease / error text 不进 label。
- source stale / partial / unknown 不被聚合成 healthy / 0。
- 无项目 × pool × instance N+1 SQL / HTTP。

### 17.7 共同门禁

- API 定向 pytest、完整 pytest、ruff / format、OpenAPI snapshot。
- Web 定向 test、完整 test、typecheck、lint、CSS token lint、build / size。
- docs build、dead links、snippets、env vars generation 和 docs impact。
- Redis / PostgreSQL / API / worker / GPU worker / beat 的真实 smoke。
- 测试结束清理本轮 coverage、临时数据库、Redis namespace、截图、下载和构建中间产物。

## 18. 正式文档与 CHANGELOG

实施时至少同步：

| 内容                              | 正式落点                                                                 |
| --------------------------------- | ------------------------------------------------------------------------ |
| 服务池 / 实例 / 项目绑定架构      | `docs/adr/0050-*`、`docs-site/dev/concepts/backend-layers.md`            |
| 平台注册层和双 ID                 | `docs-site/dev/reference/ml-backend-protocol.md`                         |
| 项目启用服务池                    | `docs-site/user-guide/projects/ml-backends.md`                           |
| 超管 pool API 与实例生命周期      | `docs-site/user-guide/superadmin/ml-backend-registry.md`                 |
| Router mode / Redis / 指标 / 告警 | `docs-site/ops/observability/index.md`、部署与 runbook                   |
| API                               | OpenAPI / `docs-site/api/`                                               |
| 环境变量                          | `.env.example` → `pnpm docs:gen-env-vars` → env reference，检查 `DEV.md` |
| 用户可见变化                      | `CHANGELOG.md` 的 `Unreleased`                                           |

正式文档只描述完成后的当前状态，不写用户可见的版本沿革。ADR 和本 plan 保留演进原因。

## 19. 停止条件与回滚

出现任一情况立即停止：

- pool / instance ID 在任一调用链中无法区分。
- 项目仍以实例为 canonical binding，却开始在池内任选实例。
- capability fingerprint 无法证明成员请求兼容。
- router acquire 不能跨进程原子执行或可能突破 max concurrency。
- enforce 遇 Redis 不确定时会静默直连。
- route lease 无法覆盖 cancel / timeout / worker crash。
- tracker / stateful session 会在执行中换实例。
- Prediction / FailedPrediction / AsyncJob / GPU dispatch 丢失 selected instance。
- traffic drain 与 GPU drain 共用状态或错误执行 lifecycle。
- migration 无法完整迁移 pipeline / preference，或 downgrade 会静默丢多成员配置。
- observe 有未知映射、would-select capability mismatch 或高比例 router rejection。

回滚按层执行：

- P1 schema 在 singleton 条件下可逆；多成员配置后 forward-only。
- P2 ledger 是独立 namespace，可停止调用但不删除 GPU 数据。
- P3 / P4 先退到 router `off`，legacy alias 恢复原实例行为；不回滚已写双 ID 历史。
- P5 enforce 故障先切 observe / off，保留 topology 和诊断证据。
- 禁止通过删除 pool 表、清 Redis 全库或把 pool id 当 registry id 进行紧急回滚。

## 20. Release gate 与成功标准

以下全部满足才允许发布并解锁 v0.23.4：

- ADR-0050 Accepted，本文 P0–P6 全部完成。
- 现有 registry 全部完成 singleton backfill，无 orphan / duplicate membership。
- 项目、pipeline、偏好以 pool id 为 canonical 配置，旧 instance id 只走明确 alias。
- 全部第一方推理先 route、后 instance client、再 GPU dispatch。
- Redis acquire / heartbeat / finish / cancel、SWRR、capacity、circuit 和 drain 金标通过。
- 双实例真实环境完成权重、饱和、故障、恢复、drain 和 stateful pin 验收。
- route / GPU 两套 lease 和 draining 语义独立且可同时观测。
- Prediction、failure、job、audit、logs、metrics 具备 pool + instance 双 ID。
- topology / runtime snapshot、权限裁剪、freshness 和 diagnostics 合同冻结。
- off → observe → enforce 稳定窗口完成，无未知映射或错误率回归。
- API、Web、OpenAPI、docs、env 和 required CI 全绿。
- CHANGELOG、版本源、镜像和运行态版本一致。
- 本文 Outcome 和 v0.23.4 前置证据表已回填。

完成后系统应能明确回答：项目请求了哪个服务池、router 为什么选择哪个实例、该实例是否接流 / 饱和 / 熔断、请求最终由谁执行、GPU 仲裁发生在哪个物理实例，以及维护时何时可以安全停止接流和卸载。

---

## 附录 A · P0 Inventory（实例 ID 使用点分类）

> 基于 commit `f7549b09`（分支 `feat/26-07-18`）的全量盘点。每个使用点都分类为「迁 pool」「保实例」或「双 ID」，对应计划 §16 P0 验收标准。

### A.1 第一方预测调用链（必须迁 router，§13）

| 调用类型                                        | 站点                                                                                           | 当前实例来源                |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------- |
| 同步 interactive image predict                  | `apps/api/app/api/v1/ml_backends.py:719`（client `:757`）                                      | 路径参数 `backend_id`       |
| 同步 predict-frame（video 单帧）                | `ml_backends.py:777`（client `:859`）                                                          | 路径参数 `backend_id`       |
| 同步 interactive-annotating-frame               | `ml_backends.py:891`（client `:959`）                                                          | 路径参数 `backend_id`       |
| predict-test（manager smoke）                   | `ml_backends.py:678`（client `:703`）                                                          | 路径参数 `backend_id`       |
| preannotate trigger（单 / 多阶段入口）          | `apps/api/app/api/v1/projects.py:1560`（`body.ml_backend_id`，验证 `:1574`）                   | body 字段                   |
| secondary inference（单框二次推理）             | `apps/api/app/api/v1/tasks/annotations.py:264`（client `services/secondary_inference.py:217`） | `data.ml_backend_id`        |
| batch source stage（job 启动）                  | `apps/api/app/workers/tasks.py:585`（`db.get(MLBackend, ml_backend_id)`，client `:866`）       | job 参数                    |
| batch downstream pipeline stage                 | `tasks.py:877`（`db.get(MLBackend, s["ml_backend_id"])`）                                      | 每 stage JSONB              |
| frame fan-out（逐帧）                           | `apps/api/app/workers/frame_preannotate.py:379`（client `:384`）                               | job 参数                    |
| failed-prediction retry                         | `apps/api/app/workers/predictions_retry.py:181`（client `:204`）                               | `fp.ml_backend_id`          |
| video tracker job pin（worker 端选择）          | `apps/api/app/services/video_tracking/runner.py:921`（`get_tracker_backend_for_capabilities`） | capability 匹配             |
| video tracker per-window predict                | `apps/api/app/services/video_tracking/adapters.py:161`（client 后 `predict_interactive`）      | `ctx.ml_backend`（job pin） |
| tracker job-create 422 gate（route 端能力校验） | `apps/api/app/services/video_tracking/jobs.py:227`                                             | capability 匹配             |

### A.2 Resolver 函数（router 必须前置）

| Resolver                                                | 位置                                      | 当前行为                                                               |
| ------------------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------- |
| `MLBackendService.get_project_backend`                  | `apps/api/app/services/ml_backend.py:601` | 优先 `Project.ml_backend_id`，否则取第一个 connected interactive 实例  |
| `MLBackendService.get_interactive_backend`              | `ml_backend.py:582`                       | 返回第一个 connected、enabled、`is_interactive` 实例                   |
| `MLBackendService.get_tracker_backend_for_capabilities` | `ml_backend.py:618`                       | capable 列表中返回显式绑定或 `supporting[0]`                           |
| `MLBackendService.get`                                  | `ml_backend.py:131`                       | registry-id 解析器，被所有 explicit-`backend_id` 路由与 lifecycle 复用 |

### A.3 Lifecycle / 管理路径（保实例，不经 router，§13）

`/health`（`ml_backends.py:631`）、`/setup`（`:555`，cached `_fetch_setup_cached` `:544`）、`/capabilities`（`:579`）、`/capabilities/refresh`（`:606`）、`/unload`（`:347` → `ml_backend.py:331`）、`/reload`（`:395` → `:343`）、`/warmup`（`:458` → `:362`）、smoke-test（`admin_ml_integrations.py:1246`）、项目克隆 capability 提取（`projects.py:631`）、GPU retirement（`gpu_arbitration/retirement.py:231,271`）、GPU membership activation（`gpu_arbitration/membership_activation.py:219`）、GPU rollout control（`gpu_arbitration/rollout_control.py:265`）、GPU eviction drain（`gpu_arbitration/dispatch.py:1690,2152`）、GPU policy requester lookup（`gpu_arbitration/policy.py:647`）、周期 stats publisher（`workers/ml_health.py:1154`）、screenshot seed catalog（`services/screenshot_seed_backends.py:157`）、registry CRUD（`admin_ml_integrations.py:814,879,943`）、admin preannotate summary resolver（`admin_preannotate.py:381`）、validation script（`scripts/validate_gpu_arbitration.py:805`）。

### A.4 数据模型（§5 迁移目标，当前状态）

| 对象                             | 当前字段 / 位置                                                                                                                                                                                       | 迁移动作                                                                                              |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `MLBackendRegistry`              | `apps/api/app/db/models/ml_backend_registry.py:18`；`id`/`url`(unique)/`state`/`is_interactive`/`auth_*`/`extra_params`/`gpu_resource_id`/`vram_budget_mb`/`eviction_priority`/`health_meta`/`source` | 不变（pool 是其上叠加层）                                                                             |
| `ProjectMLBackend`               | `ml_backend_registry.py:84`；`(project_id, registry_id)` unique，`enabled`/`default_variants`                                                                                                         | 改名 `ProjectMLBackendPool`，`registry_id`→`pool_id`，unique 变 `(project_id, pool_id)`               |
| `Project.ml_backend_id`          | `apps/api/app/db/models/project.py:43`（plain UUID，DB FK ON DELETE SET NULL 由 mig 0108 加）                                                                                                         | 改名 `ml_backend_pool_id`，FK 重指 pool                                                               |
| `Project.preannotate_pipeline`   | `project.py:53`（JSONB，stage dict 内 `ml_backend_id` str）                                                                                                                                           | stage 字段 `ml_backend_id`→`ml_backend_pool_id`                                                       |
| `Project.default_variants`       | `project.py:48`（JSONB，backend-keyed）                                                                                                                                                               | key 重映射 registry→pool                                                                              |
| `Prediction.ml_backend_id`       | `apps/api/app/db/models/prediction.py:18`（按月 RANGE 分区，composite PK `(id, created_at)`）                                                                                                         | 保留 = selected instance；新增 `ml_backend_pool_id`（父表 + 各分区同步）                              |
| `FailedPrediction.ml_backend_id` | `prediction.py:101`（未分区）                                                                                                                                                                         | 保留 = 失败 instance；新增 `ml_backend_pool_id`                                                       |
| `AsyncJob.payload / result`      | `apps/api/app/db/models/async_job.py:73`（JSONB）                                                                                                                                                     | 新任务写 `ml_backend_pool_id` + 每 stage pool + selection 汇总；旧 `ml_backend_id` 保留为历史实例证据 |
| `User.preferences.ai.*`          | `apps/api/app/db/models/user.py:60`；schema `apps/api/app/schemas/user.py:307-316`                                                                                                                    | 4 个子键按映射重 key                                                                                  |

### A.5 JSONB 引用（§5.5 必须 machine-readable 映射迁移）

| JSONB 路径                                            | key / value 语义                                                                    | schema 位置                                                                         |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `projects.preannotate_pipeline[].ml_backend_id`       | stage 内实例 id                                                                     | `apps/api/app/api/v1/projects.py:1413`（`_validate_saved_pipeline`）                |
| `projects.default_variants`                           | `{<backend_uuid>: {<axis>: <value>}}`                                               | `apps/api/app/schemas/project.py:138`                                               |
| `users.preferences.ai.params_by_backend`              | `{<backend_id_str>: <setup params dict>}`                                           | `apps/api/app/schemas/user.py:307`                                                  |
| `users.preferences.ai.model_by_backend`               | `{<backend_id_str>: <model_id_str>}`                                                | `user.py:308`                                                                       |
| `users.preferences.ai.interactive_backend_by_project` | `{<project_id_str>: <backend_id_str>}`（key 是 project，value 是 backend）          | `user.py:312`                                                                       |
| `users.preferences.ai.secondary_by_model`             | `{<backendId:modelId>: {params, variants}}`（复合 key）                             | `user.py:316`                                                                       |
| `AsyncJob.payload.ml_backend_id` / `.ml_backend_name` | text；被 `async_jobs.py:113`（filter）与 `services/ml_backend.py:221`（删除保护）读 | `apps/api/app/workers/tasks.py:240,382,470,651,798`、`predictions_retry.py:126,140` |

### A.6 前端 / SDK（§6.2 兼容边界）

- TS 类型：`apps/web/src/api/{adminMlIntegrations.ts:240, adminPreannotate.ts:48, dashboard.ts:202, failed-predictions.ts:12, mlCapabilities.ts:118,140, projects.ts:87, tasks.ts:19, ml-backends.ts}.ts`、`apps/web/src/hooks/{useMLBackends.ts, usePreannotation.ts}`、`apps/web/src/pages/AIPreAnnotate/**`（StageCard / usePreannotateConfig / GlobalPipelineLibraryPage / ProjectDetailPanel 用 `${backend_id}::${model_id}` 复合 key）、`apps/web/src/components/projects/CreateProjectWizard.tsx` + `steps/Step4Ai.tsx`、`apps/web/src/components/PerfHud/useMLBackendStats.ts`。
- 代码gen：`apps/web/src/api/generated/types.gen.ts`（`ml_backend_id` ~11 处、`backend_id` 多处），schema 重生后自动刷新。
- `/admin/ml-integrations/*` 客户端：`apps/web/src/api/adminMlIntegrations.ts:296`（`adminMlIntegrationsApi`：`overview/probe/runtimeHints/listAll/gpuResources/observe/observeSmokeTest/createRegistry/updateRegistry/deleteRegistry/registryHealth/registryUnload`）。
- Python SDK：`packages/python-sdk/`（`models.py:212` `backend_id`、`client.py:397` `MLBackends.get`、`cli/ml_backends.py`、`tui/app.py:815`）。无 TS SDK。

### A.7 Alembic 基线

head = `0131`（`apps/api/alembic/versions/0131_gpu_collector_fence_delete_guard.py`），链 `0108→…→0130→0131` 严格线性。pool 迁移从 `0131` 分叉为 `0132`。`predictions` 按月 RANGE 分区（ADR-0006），FK / 索引须父表 + 各分区同步（ADR-0044 先例）。GPU 子系统用 `backend_registry_id`（UUID），tombstone 表（`gpu_backend_memberships` / `gpu_backend_cancel_intents`）**不**有 FK 到 registry，是 registry 删除后的 tombstone。

### A.8 GPU / Redis 边界（避免冲突）

- GPU namespace：`gpu-arbiter:v1`（`apps/api/app/services/gpu_arbitration/ledger/types.py:15`）。
- routing namespace：`ml-router:v1`（本文冻结，零交叉）。
- `max_concurrency` 当前唯一存在于 `GPUBackendMembership.max_concurrency`（`apps/api/app/db/models/gpu_backend_membership.py:138`，`1..10000`，default 4）；`MLBackendRegistry` **无** `max_concurrency` / `connected` 列（`state` 字段 default `"disconnected"` 是最接近的 status）。pool 成员并发上限来源在 P1 决定（读 off `GPUBackendMembership` 或新增列），路由 lease 在所有 rollout mode 下都执行跨进程上限。

---

## 附录 B · P0 冻结合同

> 以下合同在 ADR-0050 被接受时冻结，P1–P5 实施必须逐字实现，不得偏离；偏离需新 ADR 修订。

### B.1 Schema 冻结

**`ml_backend_service_pools`**（新增，UUID PK）：

| 列                        | 类型 / 约束                                                                                                             |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `id`                      | `UUID` PK，default `gen_random_uuid()`                                                                                  |
| `name`                    | `VARCHAR(255)` NOT NULL                                                                                                 |
| `enabled`                 | `BOOLEAN` NOT NULL，default `false`（显式创建的空 pool）；singleton backfill / env 自动创建按既有 registry `state` 设置 |
| `routing_policy`          | `VARCHAR(40)` NOT NULL，default `'smooth_weighted_round_robin'`；DB CHECK 首版仅此一值                                  |
| `legacy_instance_id`      | `UUID` NULL，FK→`ml_backend_registry.id` ON DELETE RESTRICT；非空 enabled pool 必须指向本池 active 成员                 |
| `routing_generation`      | `BIGINT` NOT NULL，default `1`，单调递增（pool / member / weight / traffic_state 变更同事务 `+1`）                      |
| `capability_fingerprint`  | `VARCHAR(64)` NULL（SHA-256 hex）；null 时池不可路由                                                                    |
| `capability_snapshot`     | `JSONB` NULL（canonical 路由相关能力）                                                                                  |
| `created_at / updated_at` | `TIMESTAMPTZ`                                                                                                           |

CHECK：`(enabled = false) OR (legacy_instance_id IS NOT NULL)`（非空 enabled pool 必须有 legacy 成员）。

**`ml_backend_pool_members`**（新增，UUID PK）：

| 列                        | 类型 / 约束                                                                                               |
| ------------------------- | --------------------------------------------------------------------------------------------------------- |
| `id`                      | `UUID` PK                                                                                                 |
| `pool_id`                 | `UUID` NOT NULL，FK→`ml_backend_service_pools.id` ON DELETE CASCADE                                       |
| `registry_id`             | `UUID` NOT NULL，FK→`ml_backend_registry.id` ON DELETE RESTRICT；**单列 UNIQUE**（一实例只属于一个 pool） |
| `traffic_state`           | `VARCHAR(16)` NOT NULL，default `'active'`；DB CHECK IN (`active`, `draining`, `disabled`)                |
| `weight`                  | `INTEGER` NOT NULL，default `1`；CHECK `1 <= weight <= 100`                                               |
| `created_at / updated_at` | `TIMESTAMPTZ`                                                                                             |

UNIQUE：`(pool_id, registry_id)`。

**`project_ml_backend` → `project_ml_backend_pool`**（重命名 + 重指向）：

- `registry_id UUID` → `pool_id UUID`，FK→`ml_backend_service_pools.id` ON DELETE CASCADE。
- UNIQUE `(project_id, registry_id)` → `(project_id, pool_id)`。
- 保留 `enabled`、`default_variants`、`id`、时间戳；`default_variants` 覆盖语义改 pool 级。

**`Project.ml_backend_id` → `ml_backend_pool_id`**：

- 列重命名 + FK 重指 `ml_backend_service_pools.id` ON DELETE SET NULL。
- ORM 仍是 plain UUID（与现状一致），DB FK 由迁移加。

**`Prediction` / `FailedPrediction`**：新增 `ml_backend_pool_id UUID NULL`（FK→pool ON DELETE SET NULL）。`predictions` 父表 + 各月分区同步加列与 FK（ADR-0006 先例）。

### B.2 API 冻结

```http
# Super Admin pool / member
GET    /admin/ml-integrations/service-pools
POST   /admin/ml-integrations/service-pools
GET    /admin/ml-integrations/service-pools/:pool_id
PATCH  /admin/ml-integrations/service-pools/:pool_id
DELETE /admin/ml-integrations/service-pools/:pool_id
PUT    /admin/ml-integrations/service-pools/:pool_id/members/:registry_id
DELETE /admin/ml-integrations/service-pools/:pool_id/members/:registry_id
POST   /admin/ml-integrations/service-pools/:pool_id/members/:registry_id/drain
POST   /admin/ml-integrations/service-pools/:pool_id/members/:registry_id/resume

# Project Admin pool binding
GET /projects/:project_id/ml-backend-pools/available
PUT /projects/:project_id/ml-backend-pools/:pool_id/enablement

# v0.23.4 读模型（P5 落地，合同 P0 冻结）
GET /admin/ml-integrations/topology
GET /admin/ml-integrations/runtime-snapshot
```

权限：Super Admin 管池 / 成员 / 权重 / traffic；Project Admin 仅管本项目 pool binding；topology 双角色服务端裁剪，runtime-snapshot 仅 Super Admin（Project Admin 403）。

### B.3 错误码冻结

| code                                  | HTTP | 触发                                         |
| ------------------------------------- | ---- | -------------------------------------------- |
| `ml_backend_pool_not_enabled`         | 403  | 项目请求了未启用的 pool                      |
| `ml_backend_pool_unavailable`         | 503  | 无 active / healthy 成员                     |
| `ml_backend_pool_saturated`           | 503  | 全部候选达并发上限（带 `Retry-After`）       |
| `ml_backend_router_unavailable`       | 503  | enforce 下 Redis acquire 不可用              |
| `ml_backend_pool_capability_mismatch` | 409  | 加成员 fingerprint 不 match（带结构化 diff） |
| `ml_backend_member_draining`          | 409  | 对 draining 成员发新 lease                   |
| `ml_backend_member_not_quiescent`     | 409  | inflight > 0 时 disable / remove / unload    |
| `ml_backend_legacy_instance_unmapped` | 404  | 旧 instance id 无法解析到任何 pool           |

错误 body / HTTP status / `Retry-After` / 日志 reason / OpenAPI schema 必须一致。

### B.4 Redis key / Lua I/O 冻结

namespace：`ml-router:v1`（与 `gpu-arbiter:v1` 零交叉）。

```text
ml-router:v1:pool:{pool_id}:state                          # SWRR current_weight / generation / counters
ml-router:v1:pool:{pool_id}:member:{instance_id}:leases    # sorted set: lease_id -> expiry
ml-router:v1:lease:{lease_id}                              # hash: pool / instance / owner / operation / expiry / generation
ml-router:v1:pool:{pool_id}:member:{instance_id}:circuit   # hash: open_until / consecutive_failures / state
ml-router:v1:pool:{pool_id}:metrics:{bucket}               # hash: selection/success/error/rejection + duration buckets
```

**acquire Lua**（KEYS = `[pool:state, pool:member:{i}:leases..., lease:* 占位]`，ARGV 顺序冻结）：

1. `pool_id`, `candidate_generation`, `caller_active_candidates_json`（含 instance_id / weight / max_concurrency / fingerprint_ok / health_fresh）, `owner`, `operation`, `lease_ttl_ms`, `now_ms`, `lease_id`。
2. 校验 `pool:state.generation == candidate_generation`，否则返回 `{ok=0, reason="generation_mismatch"}`。
3. 清理 `pool:member:{i}:leases` 中过期 entry（`ZREMRANGEBYSCORE`）。
4. 对每个 active candidate：若 circuit `open_until > now` 或 `ZCARD(leases) >= max_concurrency`，标记 excluded。
5. 剩余非空候选执行 SWRR：`state.current_weight[i] += weight[i]`，选 max（并列按 instance UUID 字典序），`state.current_weight[winner] -= total_eligible_weight`。
6. 写 `lease:{lease_id}` hash + `ZADD pool:member:{winner}:leases now+ttl lease_id` + `state.selection_counter[winner] += 1` + `metrics:{bucket}.selection += 1`。
7. 返回 `{ok=1, instance_id=winner, lease_id, expiry}` 或 `{ok=0, reason}`（`pool_unavailable` / `pool_saturated` / `all_circuits_open`）。

**heartbeat Lua**：`EXPIRE` / `ZADD` 更新 `lease:{lease_id}` + `pool:member:{i}:leases` 的 score，仅当 lease 未过期且 pool/instance/owner exact match，否则 noop。**不**改 pool / instance / owner。

**finish Lua**（ARGV: `lease_id, pool_id, instance_id, owner, outcome, duration_ms, now_ms, bucket`）：

1. 校验 lease 存在且 exact match；已 finish 返回同一稳定结果（幂等）。
2. `ZREM pool:member:{i}:leases lease_id`、`DEL lease:{lease_id}`。
3. outcome=`success` → `metrics:{bucket}.success += 1` + circuit `consecutive_failures = 0`、`state = closed`。
4. outcome∈`{transport_timeout, connect_refused, no_response, gateway_unavailable}` → `metrics:{bucket}.error += 1` + circuit `consecutive_failures += 1`，达阈值则 `open_until = now + eject_ms`、`state = open`。
5. 其他 outcome（业务 4xx / cancel / gpu_capacity_503）→ 仅 `metrics:{bucket}.{outcome} += 1`，**不**触发 circuit。

**cancel Lua**：与 finish 同结构但**不**计 transport failure、**不**更新 circuit；仅释放 lease + `metrics:{bucket}.cancel += 1`。

TTL：`lease` 默认 `ML_BACKEND_ROUTER_LEASE_TTL_SECONDS`（120s），必须 > 正常 transport timeout；长任务由 heartbeat（`ML_BACKEND_ROUTER_HEARTBEAT_INTERVAL_SECONDS`=15s）续命。`metrics:{bucket}` 桶过期自动回收（窗口外）。key 顺序 / ARGV 顺序 / 返回码 / Lua SHA-256 在 P2 冻结 golden 测试。

### B.5 Capability fingerprint 冻结

canonical snapshot 字段（**排序固定，前后端不重复实现**）：

1. `protocol_version`（capability 协议版本）
2. `model_ids`（sorted list）
3. `task` / `modality` / `infra`
4. `model_version` / `weights_version`（或等价 build revision；首版不支持版本混跑）
5. `supported_prompts` / `supported_inputs` / `supported_outputs` / `supported_trackers`（sorted）
6. `parameter_schema`（仅影响请求合法性的字段）
7. `variant_axes`（sorted）+ 枚举 + 默认值
8. `stateful` / `batchable` / `warmup`（路由相关布尔）

排除：URL、实例名、auth、health 时间、GPU UUID、显存、residency、cache、纯展示描述。

canonicalization：JSON with `sort_keys=True` + list sorted + missing defaults filled；SHA-256 hex（64 chars）。golden fixture 覆盖 field order / list order / missing defaults 不影响 fingerprint；task / model / input / output / param / variant 不兼容产生精确 diff（`409 ml_backend_pool_capability_mismatch`）。

### B.6 Rollout 配置冻结

```env
ML_BACKEND_ROUTER_MODE=off                          # off | observe | enforce（部署级单一开关）
ML_BACKEND_ROUTER_HEALTH_MAX_AGE_SECONDS=90         # health 新鲜门控
ML_BACKEND_ROUTER_LEASE_TTL_SECONDS=120             # 必须 > transport timeout
ML_BACKEND_ROUTER_HEARTBEAT_INTERVAL_SECONDS=15     # 长任务 lease 续命
ML_BACKEND_ROUTER_PASSIVE_FAILURE_THRESHOLD=3       # 连续 transport failure 阈值
ML_BACKEND_ROUTER_EJECT_SECONDS=30                  # 熔断 open 持续
```

typed settings 在 P0 冻结；`.env.example` 同步 + `pnpm docs:gen-env-vars` + 检查 `DEV.md`。

---

## 附录 C · P0 Fixture 规格（P2 实现的金标测试）

> 这些 fixture 在 P2 落地时必须全部通过；P0 仅冻结预期行为。

### C.1 Capability golden

| Fixture                             | 输入                                         | 预期                           |
| ----------------------------------- | -------------------------------------------- | ------------------------------ |
| `fingerprint_stable_order`          | 同能力，字段 / list 顺序打乱                 | 同一 SHA-256                   |
| `fingerprint_missing_defaults`      | 缺省字段省略 vs 显式默认                     | 同一 SHA-256                   |
| `fingerprint_task_mismatch`         | task 不同                                    | 不同 fingerprint + 结构化 diff |
| `fingerprint_model_mismatch`        | model_ids 不同                               | 不同 fingerprint + diff        |
| `fingerprint_param_schema_mismatch` | parameter_schema 影响合法性的字段不同        | 不同 fingerprint + diff        |
| `fingerprint_excludes_runtime`      | URL / GPU UUID / 显存不同                    | 同一 SHA-256                   |
| `health_drift_disables_member`      | active 成员 health 刷新发现 fingerprint 漂移 | 原子 disabled + 诊断 + 审计    |

### C.2 Routing ledger golden

| Fixture                                 | 预期                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------ |
| `swrr_weight_1_1`                       | 长期 50/50                                                                     |
| `swrr_weight_1_2`                       | 长期 33/67                                                                     |
| `swrr_weight_1_10`                      | 长期 9/91                                                                      |
| `swrr_tie_break_uuid`                   | 并列按 instance UUID 字典序稳定                                                |
| `swrr_max_concurrency_excluded`         | 达上限成员不参与当轮权重推进                                                   |
| `swrr_generation_reset`                 | membership / weight 改变 generation，旧 current_weight 按新集合重置            |
| `max_concurrency_cross_process`         | API + 多 Celery 进程竞争下不超限                                               |
| `saturated_returns_503`                 | 全部达上限 → `ml_backend_pool_saturated` + `Retry-After`                       |
| `unavailable_returns_503`               | 无 active/healthy → `ml_backend_pool_unavailable`                              |
| `redis_unavailable_enforce_fail_closed` | enforce 下 Redis acquire 失败 → `ml_backend_router_unavailable`，不直连 legacy |
| `redis_unavailable_observe_continue`    | observe 下 Redis 失败 → 记录诊断，legacy dispatch 继续                         |
| `heartbeat_extends`                     | 未过期 exact lease 可延长；改 pool/instance/owner 拒绝                         |
| `finish_idempotent`                     | 重复 finish 返回同一稳定结果，不重复累计指标                                   |
| `finish_cancel_race`                    | finish / cancel 竞争仅一者生效                                                 |
| `crash_ttl_reclaim`                     | 进程 crash 后 lease TTL 到期回收，candidate 重新可选                           |
| `circuit_open_on_transport_failure`     | 连续 transport failure 达阈值 → open，后续排除                                 |
| `circuit_ignores_business_4xx`          | 业务 4xx / 模型校验失败 / GPU 503 / cancel 不触发 ejection                     |
| `circuit_half_open_probe`               | cooldown 后一次 half-open probe，成功关闭、失败重 open                         |
| `namespace_isolation`                   | `ml-router:v1` 与 `gpu-arbiter:v1` key 零交叉；GPU key 不变                    |

### C.3 迁移 golden

| Fixture                              | 预期                                                                              |
| ------------------------------------ | --------------------------------------------------------------------------------- |
| `singleton_backfill_zero_registry`   | 0 registry → 0 pool，无 orphan                                                    |
| `singleton_backfill_multi_registry`  | N registry → N singleton pool，每 pool 1 active 成员 weight=1                     |
| `mapping_unique`                     | registry→pool 映射唯一，FK / CHECK / 索引齐全                                     |
| `project_binding_migrated`           | `project_ml_backend` 行 id/enabled/default_variants/时间保留，registry_id→pool_id |
| `project_main_pool_backfilled`       | `Project.ml_backend_pool_id` 指向 singleton pool                                  |
| `prediction_dual_id_partitioned`     | 父表 + 各月分区都有 `ml_backend_pool_id` FK                                       |
| `pipeline_jsonb_migrated`            | `preannotate_pipeline[].ml_backend_id`→`ml_backend_pool_id`                       |
| `preferences_jsonb_migrated`         | 4 个 `ai.*` 子键按映射重 key                                                      |
| `asyncjob_payload_backfilled`        | 历史 payload 可确定映射补 `ml_backend_pool_id`，audit 历史不改                    |
| `downgrade_singleton_ok`             | 全 singleton + 无多成员 → 可逆                                                    |
| `downgrade_multi_member_fail_closed` | 有多成员 pool → fail-closed 提示 forward-only                                     |
| `env_sync_creates_singleton`         | 新建 registry 时 singleton pool 原子创建，重启不重复                              |

### C.4 请求链路 golden（observe 阶段，实际实例不变）

| Fixture                        | 预期                                                          |
| ------------------------------ | ------------------------------------------------------------- |
| `interactive_would_select`     | 路由后写 pool + selected instance 双 ID，actual instance 不变 |
| `batch_source_stage_per_task`  | 每个 task 走 router，不整批压一个实例                         |
| `pipeline_per_stage_pool`      | 每 stage pool 验证，selected instance 精确进 client/GPU       |
| `tracker_job_pin`              | 整 job pin，heartbeat 中断 / cancel 清理                      |
| `retry_explicit_new_selection` | retry 是新请求，写新 selection 证据                           |
| `lifecycle_bypasses_router`    | health/setup/warmup/reload/unload 仍精确实例                  |
| `no_transparent_retry`         | 一次业务调用最多一次实例 transport                            |

---

## 附录 D · v0.23.2 Baseline 证据

| 项                                          | 值 / 位置                                                                                                                                                                     |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 分支 / commit                               | `feat/26-07-18` / `f7549b09`                                                                                                                                                  |
| `app_version`                               | `0.23.2`（`apps/api/app/config.py:91`）                                                                                                                                       |
| `pyproject.toml` version                    | `0.23.2`（`apps/api/pyproject.toml:3`）                                                                                                                                       |
| `apps/web/package.json` version             | `0.23.2`（`:4`）                                                                                                                                                              |
| `openapi.snapshot.json` info.version        | `0.23.2`                                                                                                                                                                      |
| alembic head                                | `0131`（`apps/api/alembic/versions/0131_gpu_collector_fence_delete_guard.py`）                                                                                                |
| GPU Redis namespace                         | `gpu-arbiter:v1`（`apps/api/app/services/gpu_arbitration/ledger/types.py:15`）                                                                                                |
| `MLBackendClient` 构造点数                  | 12 prediction + ~10 lifecycle（见 §A.1 / §A.3）                                                                                                                               |
| `db.get(MLBackendRegistry, ...)` 预测路径点 | 5（batch source `tasks.py:585`、downstream `:877`、frame `frame_preannotate.py:379`、retry `predictions_retry.py:181`、secondary 经 `ml_svc.get` `tasks/annotations.py:309`） |
| JSONB 引用类                                | 7（`preannotate_pipeline` / `default_variants` / 4× `ai.*` / `AsyncJob.payload`）                                                                                             |
| OpenAPI `ml_backend_id` 出现次数            | ~11 schema + 多个 `__backend_id__` operationIds（`apps/api/openapi.snapshot.json`）                                                                                           |

**Baseline 不变量（off mode 必须保持）**：singleton backfill 后每个原请求仍落到唯一原实例，行为与 v0.23.2 完全一致；只有管理员显式把新实例加入既有 pool 并切 rollout mode 后才产生多实例分配。

---

## Outcome

落地版本：**v0.23.3**（commit `baf0b252` 起，至版本源同步 commit 止）。

### 落地内容（P0–P6 全部完成）

- **ADR**：`docs/adr/0050-ml-backend-service-pools-and-request-routing.md`（Accepted；冻结决策 D1–D18）。
- **数据模型 + 迁移**：`ml_backend_service_pools` + `ml_backend_pool_members` ORM（`apps/api/app/db/models/ml_backend_pool.py`）；`Project.ml_backend_id`→`ml_backend_pool_id`、`ProjectMLBackend`→`ProjectMLBackendPool`、`Prediction`/`FailedPrediction` += `ml_backend_pool_id`；alembic `0132_ml_backend_service_pools.py`（singleton backfill + 项目绑定改名 + 分区 prediction 双 ID + 7 类 JSONB 重映射 + forward-only downgrade 守卫）。
- **路由领域**（`apps/api/app/services/ml_routing/`）：`contracts.py`（双 ID / route lease / outcome / rejection / error code）、`capability.py`（canonical 指纹 + diff）、`policy.py`（SWRR 纯核）、`ledger.py`（Redis `ml-router:v1` 原子 acquire/heartbeat/finish/cancel Lua + 被动熔断）、`router.py`（`MLBackendRouter` off/observe/enforce + would-select + `route_context`）、`diagnostics.py`（topology/runtime-snapshot）、`metrics.py`（Prometheus）。
- **第一方调用链双 ID**：`PredictionService.create_from_ml_result` / `create_failed` += `ml_backend_pool_id`；6 个调用点（predict-frame / batch source+failed / frame fan-out / retry / secondary-inference audit）记录 pool + instance 双 ID；`AsyncJob.payload` += `ml_backend_pool_id`。
- **API**：项目池绑定（`/projects/:id/ml-backends/pools/available` + `/pools/:pool_id/enablement`）；超管 pool/member CRUD + drain/resume（`/admin/ml-integrations/service-pools/*`）；读模型（`/topology` 角色裁剪、`/runtime-snapshot` 仅超管）；legacy `ml_backend_id` alias 兼容。
- **配置 + 文档**：6 个 router 环境变量（`ML_BACKEND_ROUTER_MODE` 等）+ `.env.example` + `docs-site/dev/reference/env-vars.md`；`docs-site/dev/concepts/backend-layers.md` + `docs-site/dev/reference/ml-backend-protocol.md` 更新；`CHANGELOG.md` `[0.23.3]` 条目。

### 测试证据（全量绿，0 失败）

| 测试套件                                          | 数量 | 覆盖                                                                                   |
| ------------------------------------------------- | ---- | -------------------------------------------------------------------------------------- |
| `test_migration_0132_ml_backend_service_pools.py` | 12   | singleton backfill / 双 ID / 7 类 JSONB / downgrade fail-closed                        |
| `test_ml_routing_capability_and_policy.py`        | 20   | 指纹稳定性 / diff / SWRR 1:1 / 1:2 / 1:10 / tie-break                                  |
| `test_ml_routing_ledger.py`                       | 15   | acquire / 并发 / 饱和 / heartbeat / finish / cancel / 熔断 / TTL 回收 / namespace 隔离 |
| `test_ml_routing_router.py`                       | 8    | off legacy / observe would-select / enforce fail-closed / 项目启用 / stale health      |
| `test_ml_routing_dual_id.py`                      | 6    | Prediction / FailedPrediction 双 ID + predict-frame 端到端                             |
| `test_ml_routing_pool_api.py`                     | 5    | 项目池可用清单 + enablement + disabled pool 409                                        |
| `test_ml_routing_admin_pool_api.py`               | 7    | CRUD / 指纹不匹配 409 / drain/resume / enable-without-legacy 409 / 非 admin 403        |
| `test_ml_routing_topology.py`                     | 3    | topology 角色裁剪 / runtime-snapshot 仅超管                                            |
| `test_alembic_drift.py`                           | 1    | model ↔ migration schema 一致                                                          |
| 既有套件（17 文件 + ripple）                      | ~200 | off-mode 行为不回归                                                                    |

### v0.23.4 前置证据

- topology / runtime-snapshot 合同已冻结（`/admin/ml-integrations/{topology,runtime-snapshot}`），v0.23.4 模型市场 UI 直接消费，不复制 router 判定。
- pool/member CRUD + drain/resume API 已落地，v0.23.4 管理 UI 只做前端壳。
- 双 ID 溯源（Prediction / FailedPrediction / AsyncJob / audit）已写入，v0.23.4 观测 UI 直接读。
- 路由 ledger (`ml-router:v1`) 与 GPU 仲裁 (`gpu-arbiter:v1`) namespace 零交叉，独立演进。

### 残余风险 / 未尽事项

- **真实双实例 pool 验收已在 dev 栈执行**（见下方「灰度验收证据」）；生产部署的 observe → enforce 稳定窗口仍需运维在目标环境按 §14 流程执行并保存起止 / 批准人 / 错误率回归观测。
- **前端 / SDK**：公共 schema 仍接受 `ml_backend_id`（registry id）兼容旧前端；完整池管理 UI / SDK pool 参数属 v0.23.4。

### 灰度验收证据（dev 栈，2026-07-20）

在 dev 栈（真实 Docker：Postgres + Redis + 4 GPU backend 容器）启动第二个 grounded-sam2 实例（端口 8011，同 image / 同模型），与既有 gsam2:8001 组成真实双实例 pool（`gsam2-dual`，fingerprint `e75e9d98…` exact match）。依次执行：

| 验收项                   | 模式    | 结果                                                                                                     |
| ------------------------ | ------- | -------------------------------------------------------------------------------------------------------- |
| observe actual = legacy  | observe | 20/20 acquires，actual 全部 = legacy gsam2_1（行为 = v0.23.2），would_select 分布到两实例，无未知映射 ✅ |
| SWRR 权重分布            | enforce | 40 acquires → 精确 20/20（weight 1:1），两实例都被选中 ✅                                                |
| 并发饱和                 | enforce | 8 leases（4×2 实例）held，第 9 → `ml_backend_pool_saturated` (503) ✅                                    |
| crash TTL 回收           | enforce | 持有 1 lease 不 finish，等 TTL 过期 → 过期 lease 被 sweep，新 acquire 成功 ✅                            |
| 被动熔断                 | enforce | 同一实例连续 3 次 transport failure → circuit open，后续 acquire 排除该实例 ✅                           |
| drain 隔离               | enforce | 成员从候选排除后，全部 acquire 路由到另一成员 ✅                                                         |
| Redis 不可用 fail-closed | enforce | 指向不可达 Redis → `ml_backend_router_unavailable` (503)，不静默直连 ✅                                  |
| 端到端 transport         | enforce | router.acquire → selected instance → `MLBackendClient` → 真实 `/health` 探测两 backend 成功 ✅           |

验收后：dev 栈恢复 `ML_BACKEND_ROUTER_MODE=off`（安全默认），dev DB 回到 singleton pool 状态，第二 gsam2 容器停止。生产 observe → enforce 稳定窗口由运维按 §14 在目标部署执行。
