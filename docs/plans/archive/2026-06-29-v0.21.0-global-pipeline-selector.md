# v0.21.0 · 全局跨项目编排选择器

状态：草案(draft)。minor 版本——v0.19.2 WS0 的**正主消费方**。把现在逐项目、临时的编排搭建
升级为全局、可命名、可复用的跨项目流水线选择器。是这一系列 plan 的 capstone。

## 背景

- v0.19.2 WS0 让 `/instances` 透传 `supported_inputs` + `resource_profile`,计划里写明这是
  **「全局编排选择器的前置」**——前置已就位,选择器未建。
- 现状编排(`StageCard` / `ProjectDetailPanel`,`apps/web/src/pages/AIPreAnnotate/`)走**项目级**
  `/capabilities`,每个项目从零搭流水线,无法跨项目复用「检测→车辆属性」这类常用编排。
- v0.18.27 落地方案 A(`Project.preannotate_pipeline` 单列 JSONB,一项目一条,
  `apps/api/app/db/models/project.py:53-55`)。ROADMAP §75 明确升级触发条件是「≥2 例多命名
  或跨项目复用需求出现」,并警告「不要因『灵活性』提前建表」。

### 触发用例(承接 ROADMAP §75 触发线)

命名沿用 M-N 供后续跟踪:

- **M-1** 「detect → 车辆属性」(乘用车项目)
- **M-2** 「detect → OCR」(车牌/工牌项目)
- **M-3** 「detect → 属性 + tracking」(视频侧,下游 v0.21.1 消费)

三条已由业务侧提出,命中触发线,非「灵活性推测建表」。

## 目标

基于全局 `/instances` 建一个编排选择器:从全局池选 backend/model 搭流水线,
存为**命名可复用**的编排(三级 scope),跨项目套用。把 WS0 数据用起来。

## 沿用的既有决策(不新开 ADR)

### ADR-0023 项目模板 vs 克隆策略

本版编排模板全部沿用其骨架:

- **三级 scope**:`private` / `organization` / `public`(显式 enum 列,**不用 nullable 判语义**)。
- **copy-on-write**:套用即 `deepcopy`,模板改不追杀已实例(`docs/adr/archive/0023-project-template-vs-clone.md:29`,
  显式否决 by-reference,§Alternatives)。
- **权限判据**:`assert_template_visible` / `can_edit_template` / `assert_can_create_scope`
  (`apps/api/app/services/project_template.py:43-80`)直接抄成 pipeline 版。
- **写权限**:`public` 仅 super_admin;`organization` 组织成员;`private` 由创建者/超管。
- **usage_count 运营信号**:沿用。

### ADR-0044 全局注册表 + 项目启用

本版编排「未启用 backend」降级沿用其二层模式:

- **派发期硬拦**:404 backend 未启用(`apps/api/app/api/v1/projects.py:1557-1560`)。
- **保存期软提示**:能力软 warning(`_compute_pipeline_capability_warnings`,`projects.py:1490-1541`)。
- **套用期同姿态**:套用瞬间校验,缺则 422 + 提示去启用(不新决策)。

## 工作项

### WS0 · `supported_inputs` 词表化前置(独立抢跑)

独立第一个 PR,同时解锁下游 v0.21.1 detect-tracking(后者只 block 在此)。

**后端 SSOT**:

- `apps/api/app/services/capability_registry.py` 加第六张受控词表 `INPUTS`
  (值:`full_image` / `crop` / `bbox_prompt` / `point_prompt` + **`video` 占位**)。
- `apps/_shared/protocol_v2/src/aap_protocol_v2/vocab.py` 加 `INPUT_VALUES` 镜像。
- codegen 管道(v0.18.30 已建)自动出 `apps/web/src/api/generated/capabilityVocab.gen.ts:INPUT_IDS`。

**迁 hardcode(前端 4 处)**:

- `apps/web/src/pages/AIPreAnnotate/components/StageCard.tsx:443-444`
- `apps/web/src/pages/AIPreAnnotate/components/usePreannotateConfig.ts:47`
- `apps/web/src/pages/Workbench/state/useSecondaryInference.ts:61`
- `apps/web/src/pages/AIPreAnnotate/utils/pipelineGraph.ts:87,121-122`
- `apps/web/src/pages/ModelMarket/capability/labels.ts:81`(label 映射顺带迁)

**迁 hardcode(后端 3 处)**:

- `apps/api/app/api/v1/projects.py:1430-1444`(`_stage_supported_inputs`)
- `apps/api/app/api/v1/projects.py:1613,1622`(dispatch-time 可达性)

**兜底策略**:

- `_synthesize_supported_inputs`(`apps/api/app/services/ml_capabilities.py:48-64`)
  **加 `video` 到词表时不合成 video**——video 只能显式声明才有,保持向后兼容。

**协议增补**:

- `default_input_type: str | None`(可选,进 `ModelCapability` + `InstanceModelItem` schema)。
  未声明时按 `supported_inputs[0]` 兜底;选择器选 backend 进源阶段时用此决定默认输入类型。
- **不预建 video 源专属 UI**——那是 v0.21.1 WS4,避免投机性抽象。

### WS1 · 数据模型:命名编排表(复用 project_templates 骨架)

**新表 `project_pipelines`**:

- `id UUID PK`
- `scope enum('private','organization','public')` —— 显式列,不用 nullable 判语义
- `project_id UUID FK`(`scope=private` 非空,其他为空)
- `organization_id UUID FK`(`scope=organization` 非空,其他为空)
- `name String(255)`
- `stages JSONB`(同现 `PreannotateRequest.pipeline_stages` 结构)
- `is_default Boolean`(每项目至多一条 true,DB partial unique + service 原子切换)
- `created_by UUID FK`
- `usage_count Integer default 0`
- `created_at / updated_at`

**Alembic 迁移**:

- 建表 + 索引 + partial unique(`WHERE is_default = true`,按 project_id)。
- **Backfill**:`Project.preannotate_pipeline` 非空 → 建一条 `scope=private, is_default=true` 行。
  列**保留一版做读兼容**,下版删。
- **顺带清理 `Project.ml_backend_id` FK**:「项目主 backend」是冗余第二真值,改由 `is_default`
  命名编排的源阶段派生。同版删 FK + 调 dispatch wire——
  `PreannotateRequest` 顶层 `ml_backend_id` 与 `pipeline_stages[0].ml_backend_id` 强绑
  (`projects.py:1304-1305`)改为**自动从源阶段派生**。

**权限**:直接抄 `apps/api/app/services/project_template.py:43-80` 到
`apps/api/app/services/pipeline_template.py`(`assert_pipeline_visible` /
`can_edit_pipeline` / `assert_can_create_pipeline_scope`)。

**校验**:

- 结构 → `_validate_saved_pipeline`(v0.18.27,`projects.py:1398-1427`)搬到新端点。
- 能力 → `pipeline_validation.py`(v0.19.3)直接复用,不动。
- `is_default` 唯一性 → DB partial unique + service 原子切换。

**API**:

- `GET /v1/project-pipelines?scope=&project_id=&organization_id=`
- `POST /v1/project-pipelines`(body 带 scope)
- `PUT /v1/project-pipelines/{id}`
- `DELETE /v1/project-pipelines/{id}`
- `POST /v1/projects/{project_id}/pipelines/apply`(套用一条模板)
  - body:`{pipeline_id, set_default: bool}`
  - 语义:copy-on-write(`stages` deepcopy 到 `scope=private` 的新行);`usage_count += 1` 原子递增
  - 校验:源阶段 backend 未在项目启用 → 422,body 列出 `unenabled_backends`(UI 提示去启用)

### WS2 · 全局编排选择器 UI

- 新页/抽屉:左侧全局 backend/model 池,右侧画布搭 DAG。
- **数据源**:复用 `apps/web/src/api/mlCapabilities.ts:151 useCapabilityInstances`
  (唯一现成的全局能力池 hook,拉 `/ml-capabilities/instances`)。
- **`state="error"` backend**:展示但打「探测失败」标 + 禁用选择动作
  (与 ADR-0044 派发期硬拦语义一致;后端过滤只挡 `disconnected`,
  `apps/api/app/services/capability_instances.py:123`)。
- **画布**:复用 v0.18.16 `pipelineGraph.ts` + `PipelineGraphCanvas.tsx` + `stageWarning`
  (所有配置期校验判据保持不变)。
- **判据字面量**:全部走 WS0 的 `INPUT_IDS`;源阶段可达性按词表驱动
  (`video` 值 backend 落到源阶段时源类型标 `video`,`default_input_type` 决定默认输入)。
- **存为命名编排** → 「应用到当前项目」/「设为项目默认」两个动作都走 apply 端点。

### WS3 · 跨项目套用 + 「当前题 AI」集成

- 项目设置 / 编排面板:新加「从命名编排库选一条套用」入口(替代从零搭)。
  沿用 `ProjectDetailPanel.tsx:922-934` 保存按钮位置改造。
- popover「运行当前题(按项目编排)」(`useWorkbenchShellModel.tsx:1258`)
  数据源从 `currentProject.preannotate_pipeline` 迁到 `is_default` 命名编排;
  **载荷构造 `buildPipelineRunPayload`(`useWorkbenchShellModel.helpers.ts:142-164`)不动**
  (纯函数,新旧编排源通用)。

## 拆分与依赖

沿用 ADR-0023/0044 后,从「独立 ADR + 大爆炸」降为四个独立 PR:

| PR | 内容 | 依赖 | 下游解锁 |
|---|---|---|---|
| **PR-A**(WS0)| `INPUTS` 词表化 + `default_input_type` 协议增补 + 迁 hardcode + `_synthesize` 兜底 | 无 | **v0.21.1 detect-tracking 可开工** |
| **PR-B**(WS1)| `project_pipelines` 表 + 迁移 + `Project.ml_backend_id` 清理 + apply 端点 + 权限抄 | PR-A | — |
| **PR-C**(WS2)| 全局选择器 UI + `state=error` 展示态 | PR-B | — |
| **PR-D**(WS3)| 项目设置套用 + popover 数据源切 + `usage_count` | PR-C | — |

## 风险(收敛后)

- **YAGNI 挑战**:由 §触发用例三条命名需求承接(ROADMAP §75 触发线已达)。
- **失效降级**:沿用 ADR-0044 二层模式,无新机制;套用瞬间校验缺则 422 + 提示去启用。
- **老 backend 无 `supported_inputs`**:WS0 明确 `_synthesize` 不合成 video,兜底安全。
- **`Project.ml_backend_id` FK 清理**:与命名编排表迁移**同版**做,避免留两个真值漂移。
  dispatch 期强绑约束改为自动派生。
- **backend/model 下线**:`pipeline_validation` 已是 SSOT(v0.19.3),套用/编辑期跑判据即可;
  无新表。
- **权限**:抄 project_template 三个 assert;super_admin 建 public,无新维度。
- **跨端契约**:`apps/web/src/__fixtures__/capability-validation-cases.json` +
  `apps/api/tests/test_pipeline_validation.py` 双端 fixture 已就位;WS0 加 `INPUT_IDS` 时
  同步扩 case,新增词表判据只加 code,不改判据结构。

## 验收

- 从全局选择器搭一条「detect→classify」编排、命名保存为 `scope=public`,套用到两个不同项目均生效。
- 选择器内全程有 v0.19.2 配置期校验(可达 / class / batchable / 属性落点全部前移到画布)。
- 套用时引用未启用 backend → 422,body 列出 `unenabled_backends`,UI 明确提示去启用(不静默失败)。
- 全局池里 `state=error` backend 展示但打「探测失败」标 + 禁用选择。
- v0.18.27 现存 `Project.preannotate_pipeline` 数据经迁移变为 `scope=private, is_default=true`,
  popover 行为无回归。
- PR-A 完成后:v0.21.1 detect-tracking 可基于 `INPUT_IDS` + `default_input_type` 开工,无回改。

## 排期与依赖

- **强依赖 v0.19.2 WS0**(已落,`/instances` 全局字段就位)。
- **强依赖 ADR-0023、ADR-0044**(沿用,不新开)。
- 受益于 v0.19.3(`pipeline_validation` SSOT)、v0.19.4(徽标复用)、v0.18.16(DAG 画布)、
  v0.18.30(codegen 管道)。
- **下游 v0.21.1** 只依赖 PR-A(WS0),不 block 整条链——PR-A 单飞可先合。
- 四个 PR 顺序合并,无需单独 ADR。

## 不在范围

- 编排模板市场 / 跨组织共享:先项目 + 组织 + 超管全局三级 scope,SaaS 级共享另议。
- 加权 AB 路由等高级编排(ROADMAP 已单列)。
- video 源专属 UI(属于 v0.21.1 WS4)。

---

## 收尾优化 · 编排 UI 对齐(增补,2026-07-02)

WS0–WS3 已落地,但**全局编排页与项目编排面板的搭建能力对不齐**:全局页画布禁用了加子/改父、右列无 Inspector、只能保存 `public`、无库管理入口。走 rule-of-three 前的**方案 B(渐进补齐)**+ 一层轻量 refactor(不下沉整套 `<PipelineComposer>`)。

### 差距清单(SSOT,后续 PR 按此闭环)

- **DAG**:全局页 `PipelineGraphCanvas` 传 `onAddChild={() => undefined}` / `canReparentConn={() => false}` → 只支持"源 + 直挂 root 兄弟"扁平结构;项目页支持 `MAX_DEPTH=3` 受限树。
- **Stage 参数 Inspector**:全局页无右列检查器,`globalStagePayload` 硬编码 `roi.pad=0.05` / `input.mode` 按 `bbox_prompt` 自动切 / `write.target` 按 `isGeometryModel` 自动切,`class_filter` / `write.keys` / `label` / `model_variants` / `params` 全丢。
- **校验**:全局页无 `hasKeyConflict` 检测(下游 write.keys 不可配也检测不了);保存时不跑 `stageWarning` 汇总。
- **Scope**:全局页写死 `public` disabled;项目页只有 `private` / `public`——两边都缺 `organization`(后端 `pipeline_template.py` 已支持三级)。
- **命名库管理**:全局页只保存不管理,没"我的编排列表 → 编辑 / 删除 / 展示 usage_count / 套用到某项目"。
- **默认名**:全局"公共默认编排" vs 项目"项目默认编排",预填名冲突,应统一为 placeholder。

### 落地做法(4 步,不动 API/DB)

**为什么走方案 B**:`usePreannotateConfig` 深度依赖 `projectId`(拉 `useProject` / `aliasFrequency` / `mlBackendsApi.setup+capabilities` / `useUpdateProject`),让 `StageCard` 全套 project-agnostic 化=方案 A 级别改动,与"渐进补齐"矛盾。方案 B 只共用**状态机层**(纯计算),Inspector 分两版:项目侧继续用 `StageCard`(全能力),全局侧用新增精简版 `<GlobalStageInspector>` 只暴露编排本身参数。

1. **抽 `usePipelineComposer(context)` hook**(纯搬 state,不动 UI)
   - 位置:`apps/web/src/pages/AIPreAnnotate/hooks/usePipelineComposer.ts`
   - 搬 `ProjectDetailPanel` 里的 `stagesGraph` / `selectedSid` / `stagePayloadsRef` / `stageCapsRef` / `stageTick` / `addStage` / `removeStage` / `onReparent` / `canReparentConn` / `downstreamPayloads` / `payloadBySid` / `conflictInfo` / `allDownstreamReady` / `onStageChange` / `onStageCaps`。
   - context 参数:`{ availableBackendCount: number }`(决定 `canAddChild` 是否允许加子);其余 `sourceProducesGeometry` 恒 true(源恒出几何,与现状一致)。
   - 不搬:`stageRunState` / `stageStatByIndex` / `sourceDetected`(属于运行态,只项目侧要)、`graphNodes` 组装(节点渲染细节两页不同,由调用方拼)。
   - 两页只共用"编排结构 + 键冲突判据",不共用"运行态展示"。
2. **全局页启用完整 DAG + 精简 Inspector**
   - `GlobalPipelineLibraryPage` 用 `usePipelineComposer` 替换现在的扁平 `globalDownstreamKeys`。
   - 新增 `<GlobalStageInspector>`(`apps/web/src/pages/AIPreAnnotate/components/GlobalStageInspector.tsx`),接收 `{ pool: GlobalModelOption[], value: PipelineStagePayload | null, kind: "source"|"stage", onChange, onCaps }`,只暴露:
     - `ml_backend_id` + `model_id` 下拉(源 + 下游共用池;`state=error` 禁用)
     - 下游独有:`roi.mode` / `roi.pad` / `input.mode` / `write.target` / `write.keys`(逗号分隔自由输入) / `label` / `class_filter`(自由文本多选)
     - 变体 / params:v1 不做,依赖项目级 setup schema 拉取,全局池目前没这个入口;placeholder 说明"套用到项目后按项目补齐"。
   - 保存前跑 `stageWarning` 汇总 + `hasKeyConflict` 检测(键冲突同项目侧,写红字预警)。
3. **Scope 三级**
   - 全局页 scope 选择器:`public` / `organization` 二选一。`organization` 时占位提示"组织模板需在创建后 PATCH 指定 organization_id(首版未提供 UI 字段)",沿用 `TemplateEditModal.tsx:266-268` 现成做法。
   - 项目页 scope 选择器:`private` / `organization` / `public` 三选一,同上占位处理 organization。
4. **命名库管理 + 默认名 placeholder**
   - 全局页加"我的编排"卡片:列出 `scope in {public, organization}` 的编排,列 `名称 / scope / 阶段数 / usage_count`,行内按钮 `加载编辑`(把 stages 回填到画布) / `删除`。
   - "加载编辑"通过 `PUT /project-pipelines/{id}` 保存(而不是新建);若用户改了 scope,一起 PATCH。
   - "套用到某项目":下拉选项目 → 调 `POST /projects/{project_id}/pipelines/apply`。
   - 保存名 input 从预填 → placeholder("例如 detect → 车辆属性"),避免两边默认名冲突。

### PR 拆分(独立 PR-E,不阻塞 v0.21.1)

| PR | 内容 | 依赖 |
|---|---|---|
| **PR-E-1** | 抽 `usePipelineComposer` hook + 项目页切换 | PR-D(WS3)已落 |
| **PR-E-2** | 全局页启用完整 DAG + 挂 `<GlobalStageInspector>` + 保存前预校验 | PR-E-1 |
| **PR-E-3** | scope 三级(两页) + 命名库管理入口(全局页"我的编排"列表 + 编辑/删除/套用) + 默认名 placeholder | PR-E-2 |

### 验收

- 全局页可搭三层树("源 → 分类 A → 分类 A1")保存为公共编排。
- 全局页 Inspector 能改 roi.pad / input.mode / write.target / write.keys / label / class_filter,保存出来的 `stages[]` 载荷带这些字段。
- 全局页 scope=organization 时占位提示与 `TemplateEditModal` 语义一致。
- "我的编排"列表出现所有 public/organization 条目,能加载回画布编辑并保存,能删除,usage_count 有展示。
- 项目页 scope=organization 可选,行为同 template 侧。
- `usePipelineComposer` 单测覆盖 canAddChild / canReparent / 键冲突(承接项目侧现有覆盖)。

## 收尾优化 · 再修正(2026-07-02):三格候选与项目侧同源

上面「收尾优化 · 编排 UI 对齐」实现里,`GlobalStageInspector` 三格类别/属性键**误做成"逗号分隔自由文本 + 裸填 index"**,理由写成"全局池无项目类别表"。**这条理由读错了代码**:重看 `StageCard` / `usePreannotateConfig` / `PreannotateConfigForm` 后确认——项目侧 StageCard 里,三格的候选**真源都是 model / backend 自报**,项目类别 / 项目 attribute schema 只是**最后回落**候选,payload 层与项目类别**从不耦合**(存的是 model 原生 index 或纯字符串,`ChipMultiSelect allowFreeText` 允许任意值)。也就是说编排本来就是与项目类别解耦的,是这一系列 plan 的地基之一。

- 源阶段 `class_filter: number[]`:项目侧从 `geometricModel.classes`(model 自报原生类别表)勾选 index。**全局池 `CapabilityInstanceModel.classes` 已经带这份数据**,全局侧完全可以同 UX 勾选,不必让用户裸填数字。
- 下游 `parent_class_filter: string[]`:项目侧候选 = 上游 model 输出类名 > 项目类别回落 > `allowFreeText`。全局侧只缺"项目类别回落"这条(与解耦初衷一致,应保留缺失),其余两条都能对齐。
- 下游 `write.keys: string[]`:项目侧候选 = 下游 backend 自报 `output_attribute_schema.key` > 项目 attr keys 回落 > `allowFreeText`。全局侧同样只缺项目 attr keys 回落。

### 修正做法

1. 抽 `ChipMultiSelect` 到 `apps/web/src/pages/AIPreAnnotate/components/ChipMultiSelect.tsx`(原为 StageCard 内部 helper),项目侧 StageCard 改 import 复用;新增 `freeTextPlaceholder` 参数。
2. `GlobalStageInspector` 三格全部换成 `<ChipMultiSelect>`:
   - 源阶段类别白名单:`options` 来自 `selectedOption.model.classes.map(c => ({value: String(c.index), label: `[${c.index}] ${c.name}`}))`,`selected` 承载 index 字符串集合,组装 payload 时 `Number()` 转回;空集=不下发 `class_filter`。
   - 下游 `parent_class_filter`:`options` 来自容器传入的 `parentClasses`(见下),`allowFreeText`。
   - 下游 `write.keys`:`options` 来自 `selectedOption.model.output_attribute_schema`,`allowFreeText` + `conflictKeys`。
3. `GlobalPipelineLibraryPage` 计算 `classesForSid(parentSid) → string[]`(查父 payload 的 `model.classes.name`),下发到每张下游 Inspector 的 `parentClasses` prop。
4. 撤回上一版误导 hint("全局池无项目类别表, 白名单按 model 原生 class index 下发"改成正确措辞:"留空=全部;勾选 model 原生类别")。

### 验收(补)

- 全局页选中源 model 后,类别白名单出现 `[0] person / [1] bicycle / ...` chip 勾选,勾选后 payload `class_filter` 为 sorted 原生 index 数字数组。
- 全局页下游 Inspector 的"父框类别"chip 候选=上游选中 model 的 `classes.name`;自由文本仍可加不在候选里的类名。
- 全局页下游 Inspector 的"写回属性键"chip 候选=下游选中 model 的 `output_attribute_schema.key`;冲突键 chip 红色。
- 页面顶部/字段 hint 不再出现"全局池无项目类别表"字样;文档 block 同步改为"三格候选与项目侧同源"。

## 收尾优化 · 再修正 2(2026-07-02):补 model_variants,params 延后

深调 payload 全字段后发现,「三格 chip」只补了类别/属性键,**仍漏两个字段**——用户复核时点出:

| payload 字段 | 项目侧 | 全局侧(修前) | 数据源在全局池? | 处理 |
|---|---|---|---|---|
| `model_variants`(version/size/lang/series 轴) | VariantSelector | ❌ 完全没有 | ✅ `/instances` **已下发** `supported_variants`+`variant_combinations`+`default_variants`(`ml_capabilities.py:88-89,118-128` 注释「全局编排选择器需要」保留了它) | **本次补** |
| `params`(conf/iou 等推理阈值) | SchemaForm(`/setup.params`) | ❌ 完全没有 | ❌ `/instances` 按设计裁掉(schema 注释「不暴露 url / params 等运维信息」);只项目作用域 `/projects/{id}/ml-backends/{bid}/setup` 有 | **延后到套用时**——copy-on-write 本就落到项目,项目侧 StageCard 拉 `/setup` 配阈值;要全局配需改后端(把 params schema 透进 instances 或加全局 setup 端点),本版不做 |

### 变体修正做法

- `GlobalStageInspector` 挂 `VariantSelector`(源+下游共用一段):数据取 `selectedOption.model.supported_variants`(结构与 VariantSelector 要的 `MLBackendSupportedVariantGroup` 一致,直接传)、`variant_combinations`、`default_variants`。`VariantSelector` 路径 A 只吃 `supportedVariants`、不依赖 params schema,故无项目依赖。
- `variantValue` state 承载用户选择;payload 组装时每轴取值 `variantValue[k] ?? default_variants[k]`,只保留本 model 声明的轴(与项目侧 `StageCard` 过滤语义一致),空=不下发 `model_variants`(后端按启动默认解析)。
- 加载编辑回填:reverse-init 从 `value.model_variants` 灌 `variantValue`;不设「换 model 重置为 default」的 effect,避免与 load 回填互相 clobber(defaults 在 payload 组装期兜底即可)。

### 验收(补)

- 全局页选中带变体轴的 model(如 yolo size 轴)→ Inspector 出「模型变体」选择器;未手改时 payload `model_variants` 取 model `default_variants`;改选后取用户值。
- `params` 全局侧不出现;文档/hint 说明「阈值等推理参数在套用到项目后由项目侧配」。

## 收尾优化 · 再修正 3(2026-07-02):roi/input/write 改为内生派生

用户复核截图指出:`roi.mode` / `input.mode` / `write.target` 在项目侧 StageCard **是内生的**——选完下游 model 后由其 task 定死,**从不给手选下拉**。之前 GlobalStageInspector 把这三个做成手选 `<select>` 是错的:用户能选出 model 不支持的组合(如给分类模型手选 `write.target=geometry`)。

项目侧派生逻辑(`StageCard.tsx` payload 构造,逐条复刻):

| 下游 model | 判据 | roi.mode | input.mode | write.target |
|---|---|---|---|---|
| 检测 | `task=detection && !is_interactive` | crop | crop | geometry |
| 分割 | `task=segmentation && prompts⊇bbox && !is_interactive` | geometry | (省略) | geometry |
| 识别 | `task=ocr && composition≠composite && !is_interactive` | crop | (省略) | attributes |
| 分类 | 其余 | crop | (省略) | attributes |

### 修正做法

- 删掉 `roiMode` / `inputMode` / `writeTarget` 三个 state 与对应 `<select>`;新增 `deriveDownstreamShape(model)` 纯函数(判据同上表),返回 `{role, roiMode, inputMode?, writeTarget, isAttributes}`。
- payload 里 roi/input/write 全部取 `shape`(input 仅检测下游显式下发、其余省略交后端烘焙,与 StageCard 一致);`write.keys` / `label` 仅 `isAttributes`(分类/识别)时出字段。
- UI 加一行只读「阶段类型」展示 `role · ROI x · 投递 y · 写回 z`,让用户看清将发生什么但不能改。
- 反向初始化(load-edit)不再回填 roi.mode/input.mode/write.target(选中 model 后自动派生一致)。

至此**全局侧手配字段 = 项目侧手配字段**:模型 / 变体 / 类别(源白名单 + 下游父框)/ write.keys / label / ROI pad;内生字段两侧同派生;仅 `params` 延后到套用时(数据源不在全局池)。
