# v0.10.12 — CSP style-src sections 群续推

## 背景

ROADMAP §B 已在 v0.10.11 完成 `BatchesSection.tsx` 试点：inline style 迁到 CSS modules、文件级 `no-restricted-syntax` guard 防回潮，并写入迁移指南。当前 `style-src 'unsafe-inline'` 仍不能摘除，因为全站还存在大量 `style={{...}}`。

本阶段不改 CSP 头，继续按页面群分批迁移，先把 `pages/Projects/sections/` 这个高密度目录清掉，再扩到 Workbench / Dashboard。

## 本次切片

目标：清空 `apps/web/src/pages/Projects/sections/` 下所有 JSX inline style：

- 首批 section：`GeneralSection` / `DatasetsSection` / `ClassesSection` / `RenderingConfigSection` / `DangerSection` / `MlBackendsSection` / `MembersSection` / `OwnerSection`
- 收口批次：`BatchesSection` / `BatchesKanbanView` / `BatchAuditLogDrawer` / `AttributesSection` / `AttributeSchemaEditor` / `ClassEditor` / `BulkReassignModal` / `BulkRejectModal` / `RejectBatchModal` / `ResetBatchModal` / `AdminLockModal` / `ReverseTransitionModal`

验收：

- `pages/Projects/sections/*.tsx` 不再包含 JSX `style` 属性。
- 新增同名 `.module.css` 承接原静态布局和状态样式。
- `apps/web/eslint.config.js` 的 inline-style lint guard 覆盖 `src/pages/Projects/sections/*.tsx`。
- 既有 `GeneralSection.test.tsx` / `DatasetsSection.test.tsx` / `MlBackendsSection.limit.test.tsx` / `BatchesKanbanView.test.tsx` / `AttributeSchemaEditor.test.tsx` 通过。
- targeted lint 通过；全量 typecheck 留到批次收口或提交前单独跑。

## 分批策略

1. `pages/Projects/sections/`：本批清零，guard 已收口成 glob。
2. 共享 UI 组件：迁 `Button` / `Icon` / `Card` / `Modal` 等基础组件自身 inline style，摘掉外部覆盖类里的 `!important` 桥。
3. 高密度页面群：迁 `pages/Workbench/` 与 `pages/Dashboard/`。
4. 收尾 PR：全站 guard 扩为 glob，确认无 inline style 后，从 `security_headers.py` 移除 `style-src 'unsafe-inline'`，并补齐 style 标签 nonce 注入。
