# Epic · 视频追踪多目标化 + SAM 能力扩充 + AI 传播打通与 UI/UX

状态：**修订（2026-07-10；原草案 2026-07-07）· 版本锚点 v0.21.25**（首个版本切片 = 阶段 R；后续阶段顺延 v0.21.26+）。三条主线合一：
1. **多目标化**——把视频 tracker 链路从「单 annotation 单 track」升级为**一次追踪产出多条 track**，借此接线级扩充
   sam2_video 既有 vendor 能力，并接上 sam3.1_multiplex 多目标视频追踪。
2. **AI 传播打通（gating 依赖收口）**——修掉 2026-07-10 发现的**后端路由洞**：sam3_video 在平台链路里没被路由到
   sam3-backend，而是静默落到项目绑定的 grounded-sam2；这是 v0.21.19 明写「单列、先灰置」的那个 gating 依赖，一直没接。
3. **AI 传播前端 UI/UX 优化**——「AI 追踪」对话框的交互清晰度、默认值、多目标与耗时感知等打磨。

**跨前后端、多版本**，本文件定骨架 / 证据 / 分期 / 待决策，不是单版本可完结。

关联计划：
- [v0.21.19 sam3_video 文本驱动追踪](2026-07-05-v0.21.19-sam3-video-text-tracking.md)——text-driven 前哨。其
  **PR1/PR2（协议 text 贯通 + 前端 text UI + 能力灰置）、PR3（sam3.1_multiplex backend）均已交付**，sam3-backend 直连
  `/predict video_tracker` 可跑通（见下「2026-07-10 回归实测」）；**唯独 PR4 里「平台按 tracker 能力选 backend」这步没接**
  → 本 epic 阶段 R。
- [v0.21.20 多几何 track](2026-07-05-v0.21.20-multi-geometry-track.md)——**已交付**。mask/polygon 输出归它，本 epic 不重复。
- detect-then-track（memory `project_detection_tracking_epic`）——天生多目标，共享本 epic 的多目标底座（阶段 0）。

---

## 2026-07-10 回归实测 + 新发现（修订依据）

在双 RTX 3090(24GB) 上对 sam3.1_multiplex 做了直连回归 + 平台链路 E2E，结论直接重塑本 epic 的阶段边界：

**A. sam3-backend 直连（`curl localhost:8002/predict`，真 sam3.1_multiplex）——健康：**
- 前向跑通、输出键 `out_obj_ids`(N,)int64 / `out_binary_masks`(N,H,W)bool / `out_probs`(N,)float32 与解析假设吻合。
- 修掉一枚真 bug：`video_predictor.py` 的 `_pick_target_obj` 无种子/空 mask 兜底误读 `output_probs`（真键 `out_probs`），
  致 probs 兜底静默失效、退化取首个目标而非最高分（已修 + 单测 + 真模型验证）。
- **显存画像**：视频前向峰值随窗口帧数近似线性——基线权重 ~5.4GB，3 帧 9.4GB / 16 帧 18.9GB / **41 帧即 OOM@24GB**，
  约 0.85–1.3 GB/帧。主因是 tracker 把整窗每帧的 1008² backbone 特征 + 每目标 memory 常驻 GPU 做时序 memory-attention，
  再被 multiplex（≤16 目标槽）放大，**非权重本身**。→ 见阶段 B 显存约束。

**B. 平台链路 propagate（前端/API → runner → adapter）——发现路由洞：**
- 一个 `model_key="sam3_video"` 的 job，celery-worker-gpu 日志显示 3 个 `/predict` **全打到 `172.17.0.1:8001`
  （grounded-sam2）**，不是 sam3-backend(8002)。grounded-sam2 收到 sam3_video context 后按 **seed-bbox** 追踪、**忽略
  text**，返回貌似合理的 bbox → 静默假成功。
- 根因：[`video_tracker_runner.py:39`](../../apps/api/app/services/video_tracker_runner.py) 用
  `get_project_backend(project_id)` 选后端，而 [`ml_backend.py:248 get_project_backend`](../../apps/api/app/services/ml_backend.py)
  只返回「项目显式绑定的 `ml_backend_id`」或「交互式 fallback」，**完全不看 `model_key`**。本项目绑定 grounded-sam2 →
  所有 tracker（含 sam3_video）都发去它。
- 前端一致：[`useWorkbenchShellModel.tsx:3096`](../../apps/web/src/pages/Workbench/state/useWorkbenchShellModel.tsx)
  `supportedTrackers = mlCapabilities.capability?.supported_trackers`——取的是**单个绑定/交互 backend** 的能力，
  grounded-sam2 不声明 sam3_video → 对话框正确灰置「未绑定后端」。**前端门禁是对的**，只是它如实暴露了「平台没有把
  sam3_video 绑给任何 backend」这个缺口。
- 结论：**sam3_video 的平台链路从未真正跑过 sam3-backend**。这不是回归，是 v0.21.19 里被推迟的 gating 依赖（per-tracker
  backend 选择）从未落地。**据此新增阶段 R，且它是阶段 B 真正 E2E 的前置。**

**C. 分窗窗口默认值已收窄（已落，非本 epic 阻塞）：**
- runner 分窗大小此前所有 tracker 共用 `VIDEO_TRACKER_WINDOW_SIZE_FRAMES=300`——对 sam3_video 意味着单窗可达 300 帧、
  远超 24GB（~35 帧即触顶）。已加 sam3 专用更小分窗 `VIDEO_TRACKER_SAM3_WINDOW_SIZE_FRAMES=16`（runner 按
  `model_key=="sam3_video"` 取较小值，sam2 的 300 不动），backend 侧 `SAM3_VIDEO_MAX_WINDOW_FRAMES` 默认 300→16 兜底。
  **注意**：此项的 E2E 验证仍卡在阶段 R——路由没通，就没法在真 sam3 上验证分窗；直连 8002 已验其显存必要性。

---

## 动机：三条能力线 + 一条路由线，撞同一批平台假设

| 能力线 | backend 现状 | 卡点 |
|---|---|---|
| **A · sam2_video 多目标** | vendor `SAM2VideoPredictor` 原生支持任意 `obj_id`、propagate 返回 `[num_obj,…]`；平台 wrapper 硬编码 `_OBJ_ID=1` 只取 obj[0]（`grounded-sam2-backend/video_predictor.py:62,254`） | 平台单 track 落库 |
| **B · sam3.1 multiplex 多目标** | `sam3.1_multiplex.pt` 是 Object Multiplex 多目标视频模型（128 目标 ~7x），backend **已建且直连可跑**（v0.21.19 PR3） | ① 平台单 track 落库 ② **路由没通（阶段 R）** |
| **C · detect-then-track**（相邻 epic） | 检测式追踪天然多目标 | 平台单 track 落库 |
| **R · tracker backend 路由**（新） | sam3-backend 声明 `supported_trackers:["sam3_video"]`、grounded-sam2 声明 `["sam2_video"]`——能力信息齐 | 平台按「单一绑定 backend」选，不按能力选 |

多目标三条线卡在同一个「单 annotation 单 track」硬假设；路由线卡在「一项目一 backend」的选择模型。二者独立、但**都必须先修，sam3_video 才既能到、又能多目标落库**。

---

## 核心洞察：数据模型已就绪，真瓶颈是 路由 + 落库分组 + 透传（带证据）

**好消息——大半改造面不存在：**

| 层 | 结论 | 证据 |
|---|---|---|
| **DB annotation/几何** | 多 track = 多 annotation 行，`video_track_bbox` 结构复用，**annotation 表不改** | 一个 Annotation = 一个 geometry = 一条 track（`_jsonb_types.py VideoTrackGeometry`，`track_id` 已是一等标识） |
| **协议契约** | `/predict` 返回带 `instance_id` 的 bbox 序列**已声明** | `capability_registry.py:288-296` |
| **前端画布/时间轴** | 已能渲染任意多 track、时间轴按多 track 聚合，**几乎零改** | `VideoKonvaStage.tsx filter(isVideoTrack)`；`videoTrackTimeline.ts buildGlobalTimelineDensity` |
| **前端几何回填** | job 完成靠 `invalidateQueries(["annotations",taskId])` 整体重拉，多 track 天然可见 | `useVideoTrackerJobs.ts:145-147,231-233` |
| **backend 多目标能力** | sam2 vendor 原生多 obj、sam3.1 天生 multiplex；直连已验 | 见「2026-07-10 回归实测 A」 |

**真瓶颈——路由 1 处 + 落库分组 1 处 + 透传 1 处（+ 模式 b 才需的入口/迁移）：**

1. **【新·核心】tracker backend 路由**（阶段 R）：`get_project_backend` → 按 `model_key ∈ supported_trackers` 选后端。
2. **后端 runner 分组落库**（阶段 0）：`apply_tracker_results` 按 `instance_id` 分组、新 instance 建新 annotation；
   跨窗续追从单 `last_bbox` → per-instance 字典。
3. **adapter 透传 instance_id**（阶段 0）：`TrackerFrameResult` 加 `instance_id`（现直接丢弃），分组逻辑放 runner。
4. **前端发起入口 + job 表外键**（仅模式 b）：`annotationId` 移出 URL、payload `seeds[]`、job↔多 track 归属——归阶段 M。

**现成参照（显著降风险）**：平台**已有一条跑通的多目标写路径**——检测式追踪 ingestion：backend 一次返回多条带
`track_id` 的 `video_track_bbox`，worker `for pred in results` 落成**独立 annotation**（`prediction.py to_internal_shape`、
`workers/tasks.py:314,878`），`_new_track_id`（`annotation_propagation.py:45-47`）是检测/交互/3D 共用的 track_id 工厂。
**runner 多目标化 = 把这套「多 track→N annotation」心智搬进 `apply_tracker_results`**，不是从零设计。

---

## 产品模式决策（沿用 2026-07-07）

- **模式 a · 自动发现**：用户框一个 / 给 text，backend 自动发现同类多目标各自成 track。sam3 text-driven /
  detect-then-track 的自然形态，**前端零 UI 改造**（重拉即见多 track）。
- **模式 b · 主动多 seed**：一次框多个目标主动发起。要动 URL 架构 + 多选交互 + job 归属，**归阶段 M（可选）**。

**✅ 已定：模式 a 优先，模式 b 单列后续。** 模式 a 吃到核心红利且前端零改，模式 b 增量、性价比低；模式 a 的多目标底座是
模式 b 的前置。**本 epic 阶段 R/0/A/B/U 全走模式 a，job 表 `annotation_id` 外键不迁移。**

---

## Epic 分期（修订）

### 阶段 R · tracker backend 按能力路由（新 · 前置 · gating 依赖收口）
**目标**：让平台按 `model_key ∈ supported_trackers` 选后端，sam3_video 真正到 sam3-backend；前端据此正确放开灰置。
- **R1 · API 按能力解析**：新增 `get_tracker_backend(project_id, model_key)`——在项目**已启用**的 backend 里挑声明了
  `model_key` 的那个（sam3_video→8002、sam2_video→8001）；[`video_tracker_runner.py:39`](../../apps/api/app/services/video_tracker_runner.py)
  用它替掉 `get_project_backend`。挑不到时**显式失败**（400/job failed 带诊断），而非静默落到绑定 backend。
- **R2 · 持久化 `supported_trackers`**：健康检查抓 `/setup` 时把 `supported_trackers`（及 `text_driven_trackers`）落到
  `ml_backend_registry.health_meta`（现为空），供 R1 解析 + 前端读，避免每次现拉 `/setup`。
- **R3 · 前端能力协商改并集**：sam3_video 可用性改看「项目**所有已启用** backend 的 `supported_trackers` 并集」而非单个
  绑定 backend（`useWorkbenchShellModel.tsx:3096` / `useBackendRouting.ts`）——sam3-backend 一启用就不再灰置、显 text 框。
- **验证**：sam3_video job 的 `/predict` 打到 **8002**（celery 日志核对），跑 sam3.1_multiplex text 检测；顺带在真 sam3 上
  验证阶段 C 的分窗（≤16 帧窗、不 OOM、relay 拼连续轨迹）；sam2_video 零回归（仍到 8001）。
- **风险**：`is_interactive` 与 `supported_trackers` 是两套能力轴，别混；一项目多 tracker-capable backend 时选择需确定性
  （多个都声明同一 tracker → 取绑定优先 / 首个 connected，需定规则）。

### 阶段 0 · 多目标落库底座（公共依赖，模式 a）· ✅ 平台侧已落地（v0.21.26；真 backend 多目标输出待阶段 B）
- **✅ adapter 透传 instance 维度**：`TrackerFrameResult` 加 `instance_id` / `primary` 两个可选字段，
  `_frame_result_from_payload` 从响应读取。单实例老 backend 不发这两键 → 全 `None` → 走原路径。
- **✅ runner 按 instance 分组落库**：新增 `_partition_results_by_instance`（主实例 vs `{instance_id: 非主结果}`）
  + `_new_discovered_track`（继承 source 的 `class_name`/`tool_unit_id`/项目/任务/归属人，`source="ai_tracker"`，
  新 `track_id` 走 `_new_track_id()` 工厂）+ `_persist_tracker_results`（主实例回填源 annotation、每个新 instance
  各建一条 track），接进「取消途中 / 取消窗后 / 正常完成」三处落库点。`source="ai_tracker"` 刻意区别于批量预标的
  `prediction_based`，不被 `batch.py` 的「清理 AI 预标产物」误删。
- **✅ 跨窗主实例续种**：relay 只用主实例（`primary` / `instance_id is None`）的几何续种下一窗，非主实例不污染源
  track 的种子。
- **✅ 单测**：`tests/test_video_tracker_multi_instance.py`——分组纯函数（primary 标记 / 无标记取最小 id / 单实例
  兜底）+ runner E2E（单 seed → 多 instance 跨两窗 → 源 annotation + 2 条 `ai_tracker` 新 track，各自独立 track_id、
  继承 label、跨窗连续）；`test_video_tracker_worker.py` 顺带补齐阶段 R 遗留的路由回归（backend 声明 `supported_trackers`）。
- **契约（2026-07-10 定 · 跨窗身份按模型两条腿，见决策 #7）**：阶段 0 的**落库层与跨窗身份机制解耦**——它只消费
  一个**全局稳定的 `instance_id`** 并按它分组落库，*怎么算出这个 id* 由阶段 B 各条腿负责：**B-mx（multiplex/text）**
  = 平台在窗边界帧 IoU 关联出的全局 id；**B-pvs（PVS 点/框）** = backend 的 **caller 指定 obj_id**（memory 传播，
  身份 by construction 稳定）。两条腿都让阶段 0 的 `_partition_results_by_instance` 拿到全局稳定 id → **阶段 0 无需改**；
  `primary` 仍标源种子对应实例。协议 / 各腿实现归阶段 B（见 [`ml-backend-protocol.md`](../../docs-site/dev/reference/ml-backend-protocol.md) §video_tracker「多目标落库」当前仅描述已实现的单实例 + 字段）。
- **仍待（阶段 B）**：目前无 backend 真吐多 instance（sam3-backend `propagate` 仍 `_pick_target_obj` 收敛到单目标；
  sam2 wrapper 仍 `_OBJ_ID=1`），故 E2E「单 seed → 真·多 annotation」要等阶段 B/A 让 backend 逐帧吐全 instance 才能实测；
  阶段 0 平台侧以合成多实例 payload 单测覆盖。
- **暂缓（前端 U 阶段）**：多 instance 时逐帧 `frame_result` 事件会一帧多条、进度 `total` 按源帧数不含 instance 倍数
  → 实时预览可能闪多框 / 进度早满。因当前无 backend 触发、纯 dormant，留待阶段 U（U5/U6）随多目标实机一起打磨，不在阶段 0 动。

### 阶段 A · sam2_video 接线级扩充（依赖阶段 0）
- **A0. 多目标**：grounded-sam2 wrapper 解除 `_OBJ_ID=1` 硬编码、propagate 输出全 obj、`/setup` 扩声明。
  注意：sam2 从单 seed **不会自动发现**多目标（多目标需多 seed = 模式 b / 阶段 M），故 A0 的多目标红利要等阶段 M
  的多 seed 入口才真正显形；A0 本身是「解除硬编码、为多 obj 输出铺路」，不产出 mode-a 多目标。
- **✅ A1. 真实 confidence（v0.21.26 已落）**：`video_predictor.py` 每帧 confidence 从写死的 `0.0/1.0` 改为
  SAM2 自评的 object score——新增 `_object_score()` 从 `inference_state["output_dict_per_obj"][obj_idx][storage_key]
  [frame_idx]["object_score_logits"]`（vendor 在 yield 前的 `_add_output_per_object` 写入）取 logit → sigmoid 概率，
  空 mask 仍记 0.0、vendor 内部结构取不到时兜底回退 1.0（防 vendor sync 漂移）。配合平台低置信阈值（0.15），
  部分遮挡 / 不确定但仍吐 mask 的帧现在也会被自动标 outside。单测覆盖提取 + sigmoid + 兜底 + 端到端置信度流
  （`test_video_predictor.py`，容器内真实 torch/vendor 13 passed）。镜像已重建。
- **A2. point seed / box+point refine**：wrapper 加 `seed_points`、`/setup.supported_prompts` 加 `point`。
- **A3. mask 输出** → 归 [v0.21.20](2026-07-05-v0.21.20-multi-geometry-track.md)（已交付），不在此。

### 阶段 B · sam3.1 multiplex 多目标视频（依赖阶段 R + 阶段 0 + v0.21.19 PR1/PR2）
- **B1. sam3.1 video backend** = v0.21.19 PR3，**已交付**（`sam3-backend/video_predictor.py` + 独立视频池 + `/warmup
  task=tracker`）。剩：随阶段 R 通路后做真 E2E 回归。
> **两条腿（决策 #7 修订，2026-07-10 spike）**：sam3 有**两个**视频模型，能力互补、映射两种模式，「两条腿全上」。

- **B-mx · multiplex 文本多目标（现有 `sam3_video`，mode-a 自动发现）**：
  - backend `propagate` 不再 `_pick_target_obj` 收敛单目标，一次吐**全部检测对象**（窗内 obj_id）。
  - **跨窗身份 = text 逐窗传播 + 平台 IoU 关联**（**runtime 已验**：spike 证明 multiplex 框种子**不跨帧**、靠 text 逐帧重检测追踪；窗内 obj_id 稳定，跨窗靠平台在边界帧 IoU 关联，窗间重叠 1 帧做共享帧匹配更干净）。落库喂阶段 0 的 `instance_id` = 平台按 IoU 归并出的全局 id。
  - 编排：window 1 text 发现；window 2+ text 重检测 + 与上一窗末帧对象 IoU 关联（未匹配的新对象 → 新 track）。
- **B-pvs · PVS 交互追踪（新 `sam3_video_interactive`，点/框 seed，呼应「sam3 不止文本」+ 官方 demo 能力）**：
  - **新增** backend 接 `build_sam3_video_predictor`（`Sam3VideoPredictorMultiGPU` + `sam3_tracking_predictor`，SAM2 式 memory 传播、多对象、**caller 指定 obj_id**、收 `points`/`box`；权重 `sam3.pt` 已在容器）。
  - **种子穿线 by construction 稳定**（决策 #7 原意，PVS 实现之）：平台首帧按 obj_id 播种点/框 → memory 跨帧追随；分窗时下一窗用上一窗末帧框 + **同一 obj_id** 重播种 → 身份直接是 caller 账本，零 IoU/text 依赖。落库喂阶段 0 的 `instance_id` = caller obj_id。
  - **runtime 已验（2026-07-10 spike ✓）**：`build_sam3_video_predictor(checkpoint_path=sam3.pt)` 加载正常；**框种子无 text 传播 [2,2,2,2]（multiplex 同测归零）→ 框跨帧 memory 传播坐实**。**实现选型**：干净的 caller-obj_id 多对象走 SAM2 式直接 API（`init_state`/`add_new_points_or_box(obj_id=…)`/`propagate_in_video`，见 vendor `sam3_for_sam2_video_task_example.ipynb`），非 `handle_request(add_prompt)`（后者语义 prompt 风格、多对象/点有坑）。
  - **✅ 第 1 件 · backend wrapper 已落 + 真机验证**（`apps/sam3-backend/pvs_video_predictor.py` · `SAM3PVSVideoTracker`）：走 `model.tracker`（嫁接 `detector.backbone`）+ SAM2 式直接 API，逐对象 `add_new_points_or_box(obj_id, box/points)` → `propagate_in_video(propagate_preflight=True)`。真机双 seed（obj1/obj2）跨 7 帧连续追踪、框随目标平滑移动、`instance_id`=caller obj_id、confidence=object_score。**VRAM 惊喜**：载入 3.4GB、峰值 **4.1GB @ 7 帧**——PVS（SAM2 式 memory：memory bank + 当前帧）比 multiplex（整窗特征 18.9GB@16f）**轻一个量级**，窗口可开大得多（`SAM3_PVS_MAX_WINDOW_FRAMES` 暂保守 16，item 2/3 再调）。坑记档：`propagate_preflight=True`（否则「no points」）、tracker 无 `reset_state`（丢引用 + `empty_cache`）、bf16 输出转 numpy 需先 `.float()`。
  - **✅ 第 2 件 · backend `/predict` + `/setup` 接线 + E2E**（`main.py`）：新 tracker `model_key="sam3_video_interactive"`（种子驱动、**非** text_driven）；`/predict` 按 `model_key` 分派到 `_run_pvs_video_tracker`（读 `seeds[]` 多目标 / 回退 `source_geometry` 单目标 → PVS propagate）；PVS 独立懒加载/idle 卸载/`_video_pool_status`（cap=2）/`/unload`/`/warmup task=interactive`。`/setup.supported_trackers=["sam3_video","sam3_video_interactive"]`。**HTTP E2E 已验**（children 视频）：单 seed → 1 track 7 帧、多 `seeds[]` → 2 track（instance_id 1/2）7 帧、`/health.video_pool` 报 `sam3_video_interactive`。镜像已重建。
  - **✅ 第 3 件 · 平台 adapter/runner**：`video_tracker_adapters._REGISTRY` 注册 `sam3_video_interactive`→`MLBackendVideoTrackerAdapter`（seed 驱动、透传 `source_geometry`，与 sam2_video 同）；`_tracker_windows` 给它也用 sam3 小分窗（backend wrapper 上限 16）。路由复用阶段 R（`get_tracker_backend` 按 `supported_trackers` 挑 sam3-backend）、落库复用阶段 0（`instance_id`=obj_id，单目标 instance_id="1"→回填源 annotation）。`model_key` 无白名单校验（自由 str），后端相关切片 40 测试过。**剩余**：④ 前端模型选项（`TRACKER_MODEL_OPTIONS` + 人类可读名，随 U3）→ 全栈 E2E。
- **B-combo（可选，最完整）**：multiplex 发现 → 取其框喂 PVS 逐对象追踪 → 干净身份 + 自动发现兼得。工作量最大，B-mx / B-pvs 各自跑通后再评。
- **阶段 M 收敛**：模式 b「用户主动画多框」= PVS 多 obj_id 播种的前端入口，天然落在 B-pvs 上；阶段 M 只剩纯前端多选/多框交互。
- **显存硬约束（实测重申）**：单卡同容图像 sam3.pt(~5.8GB) + 视频 multiplex，且视频前向峰值随窗口线性增长（0.85–1.3
  GB/帧）。已有对策：sam3 专用小分窗（`VIDEO_TRACKER_SAM3_WINDOW_SIZE_FRAMES=16`，见「回归实测 C」）+ idle 让渡。
  **与图像模型常驻并存时，24GB 卡建议分窗再降到 ~12**；或图像/视频分卡部署。多目标 multiplex 每目标占一个 memory 槽
  （≤16），窗内目标越多显存越高——分窗仍是主要对策，不因多目标改。

### 阶段 U · AI 传播前端 UI/UX 优化（可与阶段 R 并行，前端独立）
> 现状对话框见 [`VideoTrackerPropagateDialog.tsx`](../../apps/web/src/pages/Workbench/stage/VideoTrackerPropagateDialog.tsx)
> （「AI 追踪 Ctrl+B」，浮层、顶部居中）。2026-07-10 实机截图暴露的问题，按优先级：

- **U1 · 方向标签反直觉（高）**：`forward`→「向后」、`backward`→「向前」（`:370`）。时间上「后=更晚的帧」虽自洽，但
  「向后追踪」读起来像倒放。改用无歧义措辞（如「往后续（更晚帧）」/「往前补（更早帧）」或直接配小箭头 →/←）+ 范围预览
  同步高亮，消除方向-范围不一致的历史坑。
- **U2 · 默认模型是 `mock_bbox`（高）**：实机默认选中测试模型（`resolveTrackerDefaultModel` 在无 `projectDefaultModel` /
  记忆时的回退，`:56-75`），用户要手动切到 sam2/sam3，且有误跑假框风险。项目已绑真实后端时应默认真实模型（`preferNonMockModel`
  语义收紧 / 由阶段 R 的能力信息驱动默认），mock 仅测试环境可见。
- **U3 · 模型名是裸 key（中）**：下拉显示 `mock_bbox` / `sam2_video` / `sam3_video`（`TRACKER_MODEL_OPTIONS:34-38`），
  对标注员无意义。改人类可读名 + 一句能力说明（如「SAM2 · 框追踪」「SAM3 · 文本检测追踪」），并把「文本驱动 vs 种子传播」
  的心智前置到模型选择处（现仅藏在 text 框下方小字 `:447-449`）。
- **U4 · 灰置提示无指引（中）**：sam3_video 灰置显「未绑定后端」（`:424,431`），不告诉用户怎么绑。阶段 R 落地后此项多数
  自然放开；仍灰置时给可点的「去启用」链路或明确「本项目未启用 sam3-backend」。
- **U5 · 无多目标感知（中，随阶段 0/B）**：多目标是本 epic 核心，但对话框无「将追踪 N 个同类目标 / 单目标」的表达。
  模式 a 落地后，text/检测式应提示「自动发现多目标」，seed-bbox 提示「跟随所选目标」。
- **U6 · 无窗口/耗时预估（中）**：分窗后一个 30 帧范围 = 多窗，用户无「≈N 窗 / ≈N 秒」感知；大范围尤其需要，避免以为卡死。
  可复用已实测的每帧成本给粗估。
- **U7 · 对话框与选中卡重叠（低）**：顶部居中浮层与右侧选中卡视觉相撞（实机可见）。调位置/宽度或联动收起。
- **U8 · 进度与对话框脱节（低）**：提交后对话框关闭，进度只在别处的 job badge。可在对话框内保留轻量进行态 / 或提交后就地转
  进度条（对齐图片侧交互式 AI 的即时反馈）。
- **原则**：U1/U2/U4 是纯前端、低风险、**现在可做**（不依赖阶段 R/0）；U5/U6 随多目标/分窗落地补；不为「整齐」重构已稳的
  范围刷选（Shift 拖选 `:407-409`）与记忆逻辑。

### 阶段 M（可选，后续）· 模式 b 主动多 seed
- **范围已收窄**（决策 #7）：多 seed 的**后端/协议管线（`seeds[]` 透传 + 逐对象回传 + runner 逐对象种子）已并入阶段 B**
  的续追路径。阶段 M 只剩**模式 b 的前端入口**：画布多选/圈多框一次发起、task 级批量 propagate 端点、job↔多 track 归属
  展示。**仅产品确需「用户主动框多个目标」时启动**（mode-a 自动发现已覆盖大多数场景）。

---

## 风险

- **阶段 R 是 sam3_video 一切的总闸**：R 不通，阶段 B 的 E2E、阶段 C 分窗验证、U4/U5 的放开都悬空。**R 应最先做。**
- **多 tracker-capable backend 的选择歧义**：一项目若启用多个都声明同一 tracker 的 backend，R1 需确定性规则（绑定优先 /
  首个 connected），否则路由不稳定。
- **新 annotation 创建策略牵动审阅链路**：多目标新建 annotation 的 label / `source` / status / 是否进 review，需与现有
  source 体系对齐，是 runner 改造里最易漏的产品面（非纯技术）。
- **sam3.1 显存硬约束**：图像+视频权重单卡同容 + 前向随窗口线性（实测），是 B 的真实部署风险；已有小分窗对策，极端场景
  需分卡。
- **job/进度归属展示**：一个 job 产出多 track 时 badge 挂哪条 / 是否聚合（`useVideoTrackerJobs.ts byAnnotation`），需小设计。
- **UI/UX 改动别踩暗色/token 红线**：阶段 U 全部走语义类 + `--sc-*`（见 CLAUDE.md §6），方向/状态色配对 `dark:`。
- **B-pvs API 选型（决策 #7；runtime 已验框传播）**：spike 已证框跨帧 memory 传播，但**须用 SAM2 式直接 API**
  （`init_state`/`add_new_points_or_box(obj_id=…)`/`propagate_in_video`）拿干净 caller-obj_id 多对象——`handle_request(add_prompt)`
  是语义 prompt 风格（框像检测返多对象、每调用疑似 reset、点路径 spike 时报错），不适合逐对象精确种子。B-pvs backend 按直接 API 接。
- **B-mx 发现 vs 续追编排**：multiplex window 2+ text 重检测出的对象要与上一窗末帧对象**边界帧 IoU 关联**（未匹配 → 新 track），
  窗间可重叠 1 帧做共享帧匹配。关联阈值 / 重叠是 B-mx 最易出错处。
- **三模型显存（B-pvs 引入）**：图像 sam3.pt + 视频 multiplex + **PVS(sam3.pt 权重)** 三者单卡（24GB）需 idle 卸载调度；
  PVS 与图像可能共享 sam3.pt 权重（待确认，若共享则增量小）。B-pvs 落地先确认 VRAM 画像。

---

## 决策记录 + 开放问题

**✅ 已定：**
1. **触发模式**：模式 a 优先，模式 b 归阶段 M。
2. **新 track 归属**：继承 source label + `source=ai_tracker` + 默认不进 review。
3. **detect-then-track 松耦合**：阶段 0 自写分组落库，只共享 `_new_track_id` + 落库心智参照，不重构已上线的检测式路径。
4. **（2026-07-10）阶段 R 为前置**：sam3_video 平台链路的路由收口先于阶段 B 的多目标消费；分窗默认值已收窄（C）。

**✅ 已定（2026-07-10 续）：**
5. **版本号编排**：**阶段 R = v0.21.25**（首个切片先发）；阶段 0 / A / B / U 顺延 v0.21.26+，各自开工时定。
6. **多 tracker-capable backend 选择规则**（R1）：**绑定优先 → 否则首个 connected 且启用的**（`get_tracker_backend`
   先看项目 `ml_backend_id` 是否支持该 tracker，不支持再在已启用 backend 里按 `supported_trackers` 挑首个 connected）。
7. **（2026-07-10，经 spike 两轮修订）跨窗多目标身份 = 按模型分两条腿**。演进：先定「种子穿线」，spike 发现
   **sam3 有两个视频模型**、身份机制按模型不同，遂改为两条腿（决策依据是 runtime + 代码 spike，不是纸面推演）。
   - **B-mx · multiplex（text 自动发现）→ text 逐窗传播 + 平台 IoU 关联**。**runtime 已验**：multiplex 框种子
     **不跨帧传播**（真机测 children 视频：框 only 种子帧 1:1 但传播归零；靠 **text** 逐帧重检测才追踪，窗内 obj_id 稳定）。
     故跨窗身份由**平台在边界帧 IoU 关联**（窗间重叠 1 帧更干净）→ `instance_id` = 平台归并的全局 id。
   - **B-pvs · PVS（点/框 seed）→ caller obj_id 种子穿线、by construction 稳定**。这才是「种子穿线」原意的真正落点：
     PVS predictor（`build_sam3_video_predictor` / `sam3_tracking_predictor`）是 **SAM2 式 memory 传播 + 多对象 + caller
     指定 obj_id**，框/点首帧播种即跨帧追随；分窗时下一窗用上一窗末帧框 + **同一 obj_id** 重播种 → 身份直接是 caller 账本，
     零 IoU/text。**runtime 已验（2026-07-10 spike，children 视频）**：`build_sam3_video_predictor(checkpoint_path=sam3.pt)`
     加载正常；**框种子无 text 传播 [2,2,2,2]（对照 multiplex 框传播归零）→ 框跨帧 memory 传播坐实**，这是 B-pvs 的地基。
     **实现选型**：`handle_request(add_prompt)` 是「语义 prompt」风格（框像检测、多对象、每调用疑似 reset，点路径报错），
     干净的 **caller-obj_id 多对象**要走 vendor notebook 那套 **SAM2 式直接 API**（`init_state` / `add_new_points_or_box(obj_id=…)`
     / `propagate_in_video`），非 handle_request——B-pvs 按此接。
   - **连带**：① 「sam3 不止文本」= 新增 PVS 交互追踪（B-pvs）；② 阶段 M 的「多框主动发起」= PVS 多 obj_id 播种入口，
     并入 B-pvs，M 只剩前端多选交互；③ 阶段 0 落库层两条腿都不改（喂它的 `instance_id`：B-mx=平台 IoU 全局 id、
     B-pvs=caller obj_id）。
   - **教训记档**：初版把「框种子穿线」当既成写进决策，multiplex 真机一测即被推翻（框不传播）；PVS 这条虽代码极像
     SAM2，仍标「runtime 待确认」不当既成——**vendor 行为一律先 runtime 验证再固化**。

**待拍板：**
8. **阶段 U 切法**：U1/U2/U4 现在就随手做（不等 R），还是整段跟 R 一起发？
