# B · 工具坞高度溢出（实施与验收记录）

> Status: Completed
>
> 创建日期：2026-09-07；代码核验基线：cd2521f1。
>
> 所属 Epic：[图片与视频工作台交互改进](1788766038_image-video-workbench-interaction-epic.md)。
>
> 执行授权：2026-09-07 用户已要求按草案逐步实施，实测后提交；实施状态：已完成；浏览器验收：B-1–B-4 已通过。

## 1. 交付范围与依赖

交付结果：工具坞高度溢出。保持现有工具能力过滤和 3D 工具入口；不实施视频单帧/轨迹分段切换，后者在 F2。

硬依赖：无硬依赖；按 Epic 优先 A–D 的顺序排队。

## 2. 设计与实现合同

使用浏览器原生 `ResizeObserver` 观察画布面板内的工具坞容器，按实际按钮、分隔和分组标题尺寸计算容量。仅在发生溢出时预留“更多”按钮空间；高度恢复时恢复原分组顺序。[ResizeObserver 文档](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserver)说明了按元素尺寸观察的能力；[CVAT 固定快照源码](https://github.com/cvat-ai/cvat/blob/cd392352e76fc314a4cb8c271ad18097224afb77/cvat-ui/src/components/annotation-page/standard-workspace/controls-side-bar/control-visibility-observer.tsx)提供了溢出入口预留与移入弹层的参考机制。

**固定规则**

- 常驻优先级为选择工具、当前工具、其余工具的现有分组顺序。当前工具不重复渲染；低高度时选择与当前工具优先，其余进入可滚动菜单。
- 工具是否可用仍由任务能力、tool unit、AI 开关和禁用原因决定；溢出计算只处理已过滤的描述项。
- 主栏与菜单复用同一个工具描述和动作，不复制几何或工具状态。菜单复用本地 `DropdownMenu`，保持图标、当前态、快捷键与禁用原因可访问。
- Esc 关闭菜单并还焦点；在菜单中按字母不触发画布操作。缩放或 resize 导致聚焦项移位时，将焦点归还“更多”入口。
- 图片和视频共用溢出算法；3D 保持原入口和语义。极小容器采用可滚动工具区域，不允许静默丢工具。

**验收**

在高度 768/900/1080px、浏览器 100%/125%/150% 缩放、停靠面板 resize 和时间轴展开/收起后，全部可用工具都能访问，当前工具可见，菜单没有画布点击穿透。覆盖现有窄屏工作台门控；不能通过缩小截图代替浏览器缩放验收。

实现集中于 `shell/ToolDock.tsx`，增加相邻纯容量计算模块，复用现有 UI adapter。扩展 `ToolDock.test.tsx`，增加 `e2e/tests/workbench-tool-dock.spec.ts` 并运行现有布局回归，验证 Stage DOM 身份和草稿未因溢出改变。

## 3. 主要实现位置

- [apps/web/src/pages/Workbench/shell/ToolDock.tsx](../../apps/web/src/pages/Workbench/shell/ToolDock.tsx)

新增内部模块只服务本步职责；不得复制完整 Workbench/Mask/SAM/tracker 状态树。公共边界、失败语义和回滚约束沿用本文件设计及 Epic 跨步骤不变量。

## 4. 浏览器实测验收

每个里程碑必须同时具备真实浏览器交互与持久结果证据，不能用单测、截图存在或 API 成功替代整条用户路径。使用当前 worktree 的 Web/API 和经验证的一次性测试数据库；记录 URL、实际代码目录/提交、浏览器、视口、测试任务与逐项结果。正常路径不拦截业务 API；确定性 ML 夹具或人为注入的失败必须明确标注，不能称为真实模型质量验证。

在可见 Chrome 中通过真实点击、按键和拖动执行下表，检查相关控制台与 API 错误。用户已明确允许使用 Playwright：缩放矩阵使用独立临时 Chromium 配置，通过浏览器原生每标签缩放控制并读回比例、DPR 和 CSS 视口；不使用 CSS zoom 或截图缩放代替。新增自动化场景使用真实 Chromium 与 API，禁止用 DOM dispatchEvent 替代用户输入。涉及保存的步骤刷新后读回；涉及性能的步骤对同一数据做前后比较。

本机环境已核验：3000/8000 服务来自主工作区，3001 是 Grafana，均不能当作当前 worktree 的验收服务。本步已完成 Chrome 人工操作及独立 Chromium 的自动化实测。A 实测使用 3010 Web / 8011 API；本步实施时重新检查端口占用并启动当前 worktree 的隔离服务。完整启动、端口覆盖与清理约定见 Epic 的“浏览器实测环境”段。

| 编号 | 操作与通过条件                                                                                                                       | 当前结果 |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| B-1  | 在图片与视频工作台依次设置高度 768/900/1080px，再用浏览器真实 125%/150% 缩放；全部可用工具能从主栏或更多菜单访问，当前工具始终可见。 | 通过     |
| B-2  | 展开/收起详细时间轴时工具坞高度与分配保持稳定；拖动停靠分隔线改变实际高度后重新分配，分组顺序、Stage DOM、当前草稿和选择不变。       | 通过     |
| B-3  | 用键盘打开更多菜单、选择工具、关闭菜单；焦点正确返回，菜单中的字母和 Esc 不泄漏到画布。                                              | 通过     |
| B-4  | 在工具被项目禁用或后端能力不支持时打开更多菜单，禁用原因与主栏一致；菜单外点击不误画对象。                                           | 通过     |

## 5. 自动化检查与文档

下面列的是仓库标准命令；本机执行 E2E 时按 Epic 环境约定附加临时端口覆盖配置，不直接占用 Grafana 的 3001。只运行本步相关套件；Mask native 场景使用 `PLAYWRIGHT_RASTER_MASK_MATRIX=native`，按既有配置启用原生写入。

```bash
pnpm --filter @anno/web test src/pages/Workbench/shell/ToolDock.test.tsx
pnpm --filter @anno/web test:e2e e2e/tests/workbench-layout.spec.ts --project=chromium
pnpm --filter @anno/web typecheck
pnpm --filter @anno/web lint
git diff --check
```

文档同步：对应用户指南、开发者合同（如改变）和 CHANGELOG Unreleased；具体路径沿用 Epic 对应步骤的文档表。

## 6. 执行、记录与回滚

- 用户于 2026-09-07 更新执行要求：独立草案形成后按序实施，每个里程碑浏览器实测通过后提交。本草案已包含在该执行授权中。
- 按依赖核对当前代码与已交付合同，实施本文件范围，保持原有状态所有者、任务锁与异步代次保护。
- 运行定向回归、浏览器验收和受影响文档检查，修复发现的问题，再复核最终 diff。
- 浏览器所有必测项通过后才将本里程碑标为完成；失败或未测明确保留为未完成，不以其它检查代替。
- 追加 `## Outcome`，记录实际改动、文档位置、测试结果、浏览器逐项结果与证据、清理情况；只有实际存在才记录提交号。更新 Epic 状态索引。
- 每次测试后清理本次中间产物和测试数据；保留明确交付的最小证据。停止本任务启动的进程，恢复浏览器缩放，清理临时配置，不修改共享 .env/依赖或停止主工作区服务。
- 本里程碑验收并提交后，按依赖继续下一份草案。版本与发布日期仍由维护者决定，本轮不作版本发布。

回滚：按本里程碑回退实现；保留已保存的标准标注/反馈数据及后续客户端可选读取字段，不清空用户数据。

## Outcome

完成日期：2026-09-07。实施与验收基线为本工作区 `f5f6c83f` 加本里程碑改动；页面 `http://127.0.0.1:3010` 与 API `http://127.0.0.1:8011` 均来自 `/home/hehao/.codex/worktrees/0dc6/ai-annotation-platform`。

### 交付结果

图片与视频共用按真实工具坞高度计算的容量模块，保留选择及当前工具，溢出项通过本地 Radix 菜单访问。主栏和菜单复用动作、能力过滤、快捷键及禁用原因；极小高度提供滚动兜底。关闭菜单恢复焦点，点击画布后调整高度不会抢回旧焦点，菜单关闭后的普通快捷键恢复。

统一输入隔离保护 Polygon、Mask、SAM、类别选择器和视频追踪面板。工具菜单的 portal 按键不触发布局保存；菜单外点击不新增几何点。时间轴展开恢复按钮焦点时使用 `preventScroll`，避免内部画布滚动把工具坞推离可见区域。Stage 与业务草稿仍由原 owner 持有。

同步了工作台用户指南、外壳架构文档和 CHANGELOG Unreleased。此前设置隔离 helper 的 Markdown 引用已随名称更新；未变更 API、包版本或锁文件。

### 浏览器逐项证据

最初 Chrome 实测使用 1440×768：旧工具按钮发生压缩，最后一个轨迹工具底边超过容器；改动后按钮保持 38px，溢出项进入更多。Chrome 人工操作验证了 End/Enter 选择末尾 Mask 轨迹、菜单字母与 Esc 隔离，以及高度恢复时焦点回到当前工具。后续完整矩阵及保存流程按用户授权使用独立 Playwright Chromium，浏览器原生缩放通过临时扩展的 automatic/per-tab 模式设置，不使用 CSS zoom、设备像素比模拟或用户浏览器设置。

| 项目 | 结果                                                                                                                                                                                                                                                                                                                 |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B-1  | 图片 12 项、视频 15 项工具在 768/900/1080px 内容高度和 100%/125%/150% 原生缩放的 18 组组合中均完整可达，无重复与裁切；当前 Mask/Mask 轨迹在主栏可见，Stage DOM 身份不变。缩放读回与 DPR 均为 1/1.25/1.5，根 CSS zoom 始终为 1；关闭菜单后 V 可回选择。                                                               |
| B-2  | 视频时间轴展开/收起后，工具坞高度、顶部位置和工具分配保持稳定。图片和视频均把讨论面板停靠到底部，实际拖动分隔线约 65px，再改变视口高度；Stage 与 Konva canvas 是原 DOM 对象。最终各保存一个三顶点多边形，刷新并从 API 读回仍为三个顶点。既有布局用例另验证讨论草稿、人工选择、视频非零帧和解码元素在布局变化中保留。 |
| B-3  | 用方向键打开菜单，Home/End 导航，字母与 Esc 不改变背景多边形；菜单关闭后焦点恢复。高度变化收回菜单，更多消失时回到当前工具。单测另覆盖空格按住态释放、菜单关闭后的快捷键与点击画布后 resize 不抢焦点。                                                                                                               |
| B-4  | 菜单保留后端不支持的智能点入口，并提供可访问的禁用原因；点击菜单外画布不追加第四个点。待选类别时打开与关闭更多不会取消草稿，之后选类仍只保存一项。项目能力过滤的既有单测及窄屏布局回归通过。                                                                                                                         |

保存和布局操作使用实际 API，没有拦截业务请求。测试夹具为一次性项目及视频任务，所有手工工具单元使用明确的类别配置；AI 后端能力不可用仅用于验证禁用入口，不涉及模型质量。最终图片、视频及缩放用例的 `pageerror` 记录为空。

### 检查与清理

- 11 个相关单测文件共 179 项通过，覆盖工具坞、容量、输入守卫、图片动作、类别选择器、视频画布/追踪/时间轴和 Dockview 外壳。既有 Dockview 测试仍输出异步 `act` 警告，断言全部通过。
- 新增工具坞 3 项 Chromium/API 用例通过；现有布局 4 项通过。原生缩放用例最终耗时约 30.5 秒，图片与视频保存回归分别约 9.2 / 10.7 秒。
- Web typecheck、完整 lint（含 CSS tokens）、计划时效及 `git diff --check` 通过；未运行远端 CI。
- 每轮失败或完成后的测试数据和报告均清理。最终确认 3010/8011 已停止，独占测试库零连接且归属正确后删除；临时浏览器配置、扩展、截图、报告及端口覆盖文件均移除。Chrome 视口已恢复，测试页已关闭。主工作区服务和共享 `.env` 未修改。

B 已完成，按 Epic 顺序继续 C。
