# v0.11.6 · 笔画 timeline（stretch）

> [epic 索引](2026-05-25-v0.11-annotation-feedback-convergence.md) · B 组（前端，stretch）· 依赖：v0.11.2 comments tab
>
> **stretch**：v0.10.21 已判定"无 UX 增量"延期。无明确客户信号则可整组跳过。

## 目标

I4 原意：canvas 批注的每条 stroke 带时间，评论卡片下显示迷你时间条，hover 评论高亮对应 stroke 区间。

## 前端改动（无后端 schema 变更）

- **`CanvasDrawingEditor.tsx`**：创建 stroke 时给每条挂 `id` + `started_at` + `ended_at`。`canvas_drawing.shapes[]` 已是 JSONB 数组，新字段向后兼容；旧 stroke 缺字段时 UI 降级为"无时间轴"模式。
- **`CanvasDrawingPreview.tsx`**：评论卡片下方新增迷你时间条（默认折叠）；hover 单条评论时通过 `useHoveredCommentStore`（底座已存在）把 stroke filter 从"全部"切到"按 timeline 区间"高亮。

## 验证

- `pnpm --filter web typecheck` + `lint:css-tokens`。
- 走查：新画的 stroke 带时间条；旧数据降级无报错；hover 评论高亮对应 stroke。

## 回退

纯前端增量，JSONB 向后兼容，回退 = 还原两文件。

## 备注

无后端改动、无 schema、无 alembic。若资源紧张，本切片优先级最低。
