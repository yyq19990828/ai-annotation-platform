# 第一批：内部上线可用性与账号生命周期

> 状态：已实施并完成本地隔离环境验收。按用户指定的 `gpt-5.6-luna`、`max` 推理强度子代理并行实施，由主代理审查、补修和整合；生产发布尚未执行，不分配发布版本或日期。
> 调研基线：2026-09-09，Git `25313227`。实际实现、浏览器验收和剩余边界见 Outcome。

## 目标与独立交付边界

交付一个员工可以恢复账号、持续完成标注，管理员可以停用和交接人员的完整工作流程。
本批直接复用现有邀请、SMTP 设置、项目成员、任务派发、审核、离线队列和审计，不依赖后续批次。

- [第二批：易用性、管理效率与超管系统设置](1788952434_internal-usability-admin-settings.md)负责入职便捷操作、批量管理、任务入口和运行时配置扩展。
- [第三批：身份集成与进阶治理](backlog/1788952434_internal-identity-governance.md)按企业身份平台、跨项目职责和自动化使用需求触发。

最小交付选择现有服务上的流程补齐。完整培训系统、自定义权限编辑器、统一身份平台建设及新的配置中心不属于本批。
本批预计涉及超过 8 个文件，应按下列独立工作包合并；每个工作包都需包含相应文档和验证。

## 当前事实与实施锚点

| 主题         | 当前事实                                                                       | 主要实现                                                                                                                                                                                      |
| ------------ | ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 账号恢复     | 忘记密码生成令牌后只记录日志，尚无发送调用；已有验证邮件和测试邮件发送服务     | `apps/api/app/api/v1/auth.py`、`apps/api/app/services/password_reset.py`、`apps/api/app/services/email.py`                                                                                    |
| 密码输入     | 设置页标签要求至少 8 位，但前端长度判断仍为 6；服务端校验至少 8 位并要求强度   | `apps/web/src/pages/Settings/SettingsPage.tsx`、`apps/api/app/schemas/me.py`                                                                                                                  |
| 用户生命周期 | 用户列表只返回启用账号；已有停用和删除，未见恢复接口；删除还会脱敏历史审计字段 | `apps/api/app/api/v1/users.py`、`apps/api/app/db/models/user.py`                                                                                                                              |
| 交接         | 删除前检查 Task 和锁；批次标注员、审核员、项目 owner 另有归属                  | `apps/api/app/db/models/task_batch.py`、`apps/api/app/api/v1/projects.py`、`apps/api/app/services/batch.py`                                                                                   |
| 列表与异常   | 标注任务查询每页 100，列表未消费下一页操作；部分首页将请求失败展示为持续加载   | `apps/web/src/hooks/useTasks.ts`、`apps/web/src/pages/Annotate/AnnotatePage.tsx`、`apps/web/src/pages/Dashboard/AnnotatorDashboard.tsx`、`apps/web/src/pages/Dashboard/ReviewerDashboard.tsx` |
| 保存与提交   | 已有本地离线队列、重试及提交流转；同步连接和标注持久化状态需要清楚区分         | `apps/web/src/pages/Workbench/state/useWorkbenchOfflineQueue.ts`、`apps/web/src/pages/Workbench/state/useWorkbenchTaskFlow.ts`、`apps/web/src/pages/Workbench/shell/StatusBar.tsx`            |

以上列表后页、保存异常属于定向验收对象；不能由局部源码推断整个工作台已经存在数据丢失。

## 工作包 A：账号恢复可完成

1. 在现有邮件服务增加重置邮件发送，复用 SMTP 配置、前端基础地址和重置令牌。发送成功必须依据实际邮件调用结果，不能依据日志文案。
2. 保持忘记密码对外统一响应；SMTP 不可用或未配置时，页面提供清晰的管理员协助说明，不暴露邮箱是否存在。
3. 管理员侧复用现有测试邮件、临时密码入口；发送失败记录可定位的结果，不记录明文令牌或密码。重试复用受限的申请入口，不增加新的常驻邮件服务。
4. 重置成功后检查旧会话失效、当前重新登录及管理员临时密码后的改密引导；复用现有 token generation 和密码重置标记，按真实调用补接。
5. 前端注册、重置、个人改密的提示和输入规则与服务端一致；服务端保持最终校验。

涉及：上述 auth、password_reset、email、me schema，以及登录/忘记密码/重置页面、SettingsPage、现有认证 hooks。
本包不依赖第二批系统设置扩展；邮件地址、SMTP 和邀请有效期已经可配置。

验收：实际收到邮件并完成登录；错误、过期、已使用链接均有说明；SMTP 失败可定位且不误报发送成功；无邮件环境能通过现有管理员流程恢复。

## 工作包 B：停用、恢复和交接分清楚

### 产品行为

- 人员列表提供启用、停用、全部筛选。停用保留历史记录；删除继续保留现有独立语义，不能把删除按钮改名后作为可逆停用。
- 正常离职采用“查看待交接事项 → 按职责选择接收人 → 确认交接并停用”。紧急停用可以立即阻止使用，并保留管理员可查询的待交接清单。
- 待交接清单覆盖项目 owner、批次标注员/审核员、待标注/审核/退回任务及编辑锁；展示个人 API Key 的用途或名称，提示集成负责人另行换发，不转交旧明文密钥。
- 按项目分别选择合格的标注接收人、审核接收人和项目负责人；不强迫单一接收者承担所有角色。接收人必须启用且具备目标项目实际权限。
- 恢复账号只恢复允许恢复的停用状态；不抢回已交接任务、不恢复已撤销密钥、不承诺还原已脱敏历史。
- 保留最后一名超管、自身停用及项目管理员管理范围的现有限制。

### 接口、数据与一致性

- 现有 `GET /api/v1/users` 增加可选账号状态过滤，缺省仍为启用，保持现有返回数组契约。列表分页属于第二批的兼容扩展。
- 新增 `GET /api/v1/users/{id}/offboarding-preview`，按项目返回当前职责、数量、可选接收人和阻断原因；预览不修改数据。
- 新增 `POST /api/v1/users/{id}/offboarding`，接收逐项目的接收者映射、预览版本和原因；执行时重新核对职责、权限与锁，状态已变化则返回 409 并要求刷新预览。
- 新增 `POST /api/v1/users/{id}/reactivate`，只允许明确可恢复的停用账号。
- 在 User 上补充明确的停用类型、时间、操作者、原因；继续以 `is_active` 承担现有认证检查，不挪用表示在线状态的 `status`。所有停用/删除/自助注销写入路径同步维护生命周期信息。
- 历史 `is_active=false` 不能一律视作可恢复。可可靠判定来源的记录按既有操作归类；无法判定的归入历史未知状态，不自动开启恢复，也不反向重建被脱敏的审计信息。
- 用户停用、任务和批次归属、项目负责人、锁释放及审计在同一事务内完成。复用现有批次级联和项目转让规则，并处理所有仍可继续流转的任务状态，包括退回任务。
- 迁移采用可兼容的新增字段，按实施当天 Alembic head 生成迁移，不在本文预分配迁移编号。不得通过批量重建用户完成迁移。

```text
成员管理页 → 交接预览 API → 项目 / 批次 / Task / 锁
           → 确认 API → 重新校验 → 单事务更新归属与账号状态
                                     └→ 审计 → 接收者继续工作
```

验收：分别使用跨两个项目的标注员、审核员和项目 owner 验证交接，标注员场景包括退回任务；交接后各职责的接收者都能继续工作。历史混合职责作为异常数据单列，不要求第一批先支持同账号跨项目不同角色。并发领取、接收者被停用、权限不足时不出现部分转交；紧急停用立即阻止新业务操作，旧会话和 API Key 也不能继续以该账号工作。

## 工作包 C：查询失败与任务后页可处理

1. 标注/审核首页和任务列表分别处理加载、空数据、失败、权限变化；失败提供重试，不把失败转成空任务列表。
2. 标注任务列表接入既有分页能力，提供加载更多或可持续滚动入口，保留筛选条件与当前选择。
3. 在旧数据已加载后的刷新失败中保留旧内容并说明更新失败；请求切换后迟到响应不能覆盖当前项目或筛选。
4. 保留可重试错误的必要上下文，并复用 BUG 反馈采集入口；不在员工页面堆放后端堆栈。

验收：250 条任务的批次可访问第一条和最后一条；断网、403、500、空队列四种场景可区分；筛选切换和返回后没有重复或丢失列表项。

## 工作包 D：让用户确认保存与提交结果

1. 固定显示保存中、已保存、仅保存在本机、同步失败；实时进度连接状态不再代表标注已经保存。
2. 提交前使用既有持久化/离线队列 owner 判定，只有服务端确认后才显示送审成功并自动下一题；不得另建一份保存状态或任务选择状态。
3. 必填校验失败定位具体对象及字段；保留已编辑内容。提交成功、退回、撤回都给出明确结果。
4. 按现有工作台状态约束验证切任务、切项目、退出账号和旧请求晚到；不允许旧账号/旧任务的队列写入新上下文。

实施时遵循 `.agents/skills/aap-workbench-state/SKILL.md`；重点扩展真实行为验收，不重写已存在的离线机制。

验收：断网编辑后能辨认仅本地保存；联网后确认同步；刷新、快速提交、切题和登录过期不会静默丢失或误报成功；有待处理本地记录时退出提供明确处理选择。

## 验证与清理

以下是实施阶段的验证要求。实施前已在本次独立测试库运行 28 项既有账号与权限回归，全部通过；新行为与浏览器验收结果在完成后记录。

- Python 优先使用当前 checkout 的虚拟环境。运行前核对 `TEST_DATABASE_URL` 实际指向一次性测试库；`apps/api/tests/conftest.py` 会自动运行迁移，不能依赖默认库名假定安全。
- API 起点：在 `apps/api` 执行 `uv run pytest -q tests/test_admin_password_reset.py tests/test_auth_refresh.py tests/test_users_delete_transfer.py tests/test_users_role_matrix.py`，并运行新增的恢复邮件、停用/恢复与完整交接回归。
- Web 起点：`pnpm --filter @anno/web test src/pages/Settings/SettingsPage.test.tsx src/hooks/useAuth.test.tsx`，补改动到的首页、任务分页、保存和提交行为测试。
- 运行 `pnpm --filter @anno/web typecheck`；样式改变后运行 `pnpm --filter @anno/web lint:css-tokens`；API 改动后运行 `pnpm openapi:export`、`pnpm codegen`、`pnpm openapi:check`。
- 浏览器：按 `apps/web/e2e/README.md` 的独立数据库、端口和存储约定运行 `auth.spec.ts`、`annotation.spec.ts`、`batch-flow.spec.ts` 的相关路径，并完成三个角色的手工业务链。
- 实际验收链：管理员建项目/导入/派发 → 标注员提交 → 审核员退回 → 标注员修改 → 审核通过 → 导出，再执行一次人员交接和一次断网恢复。
- 结束后清理本次测试账号、项目、数据库、对象前缀、邮件、报告、截图、编译缓存及自建测试进程；只清理本次生成的内容，保留业务数据和已有产物。
- 最后审阅 diff 并执行 `git diff --check`，分别报告自动化结果、真实浏览器结果和未覆盖项。

## 文档、上线与回退

- 同步 `docs-site/user-guide/superadmin/user-management.md`、`docs-site/user-guide/getting-started.md`、`docs-site/user-guide/reference/settings.md`、相关工作台/审核指南，以及 `CHANGELOG.md` 的 Unreleased。
- API 变更同步 `docs-site/api/`、`README.md` 和受影响的 OpenAPI/生成类型。特别纠正文档中与代码不符的解封、逐个添加成员、管理员代管 API Key 描述。
- 涉及 worker 的实现按 `.agents/skills/aap-runtime/SKILL.md` 刷新预定环境的相关 worker；先查挂载和服务，不能把源码已改视为运行进程已更新。
- 发布采用新增接口与兼容字段；界面可回退到旧入口。账号恢复和交接已经产生的业务变化不能靠代码回滚自动撤销；按审计重新检查并执行显式反向交接，不能回滚整库覆盖新工作。
- 任一工作包单独落地后仍须保持原有注册、标注、审核与派发路径可用，不等待第二或第三批补完才能工作。
- 实施完成后追加 `## Outcome`，记录实际提交、正式文档、验收和剩余范围；不把本次计划保存误记为功能完成。

## 工作量与主要风险

账号恢复为小范围改动；错误/分页、保存反馈为中等范围；生命周期和跨职责交接为较大范围。
最大的风险是职责与权限在预览后变化，以及将可逆停用与已有删除脱敏混为一谈。以单事务校验、明确恢复边界和实际接收者可继续工作作为验收核心。

## Outcome

第一批四个工作包已完成；第二批和第三批保留各自规划，未实施其中的配置扩展或身份集成。

### 实现与提交

- 账号恢复：`6dbd613c`。重置邮件通过现有 SMTP 发送，统一申请响应，链接单次使用，重置后旧会话失效；密码规则和临时密码引导同步。
- 账号生命周期：`503f7328`、`674838b3`、`15ffcda3`。增加状态筛选、逐项目职责交接、紧急停用、恢复及凭据撤销；历史删除与来源未知账号不会被错误恢复。
- 查询与分页：`6a765f4a`、`c68b698f`。分别处理权限、服务、网络、暂停和空数据状态；列表支持加载后页及去重。
- 工作台保存：`fe69d8c9`、`7207b9f8`。显示保存和离线同步结果，校验必填字段，阻止未完成保存时送审，退出时明确处理本机记录。
- 审查补修：`b802f92d`、`ac5f00d2`、`ff21e035`。补齐持久创建幂等、交接后任务归属检查、批次接收资格、停用并发复核、历史迁移分类、审核中返工可见性、多标签页队列互斥、账号与撤销历史隔离，以及重做请求标识。

正式说明已同步到账号管理、入门、个人设置、工作台、批次和审核指南，认证及任务 API 指南、任务锁开发说明、README、CHANGELOG 和 OpenAPI 快照；本计划只保留实施与验收记录。

### 本地验证

| 层次       | 实际结果                                                                                                                                                         |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| API        | 19 个文件的主回归 162 项通过；另将跨两个项目的交接续作扩展为 3 项，分别验证新负责人修改项目、新审核员领取并审核通过、新标注员创建标注。按最终用例去重共 163 项。 |
| 迁移       | 3 项通过；在独立一次性迁移库实际执行历史状态迁移的降级、分类种子与升级验证。新增幂等类型随隔离运行库升级并通过真实创建接口验证。                                 |
| Web        | 主回归 25 个文件、213 项通过；另通过 51 项撤销历史、3D 历史适配和相关管线回归，共 264 项互不重复的测试。                                                         |
| 静态与契约 | Ruff、Prettier、完整 Web ESLint/CSS token 检查、TypeScript、OpenAPI 导出/生成/一致性检查通过；最终实现提交的正常 pre-commit 全部通过。                           |
| 文档       | VitePress 构建通过；输出到本次临时目录，生成路由索引已同步。                                                                                                     |

### 隔离浏览器验收

- 通过实际 SMTP 收到邮件，打开邮件中的重置链接并重新登录；旧登录令牌失效。
- 管理员创建图片项目、关联数据集、加入成员并分派五个批次；标注员绘制并填写必填属性、送审，审核员退回后原标注员重新编辑，再审核通过。
- 下载并核对导出 ZIP：COCO 和 AAP JSON 均包含两条通过审核的标注及重做后的属性值。
- 同一批次加载到 250 条任务，打开首条和末条，返回后无重复；浏览器注入的 403、500、网络错误及空响应分别显示对应状态，移除注入后恢复真实列表。
- 实际完成跨两个项目的标注员交接、停用及恢复；各职责接收者继续工作的写入权限由上述真实 API 回归验证。
- 离线新建、修改、删除均显示本机保存并阻止提交，联网后读回结果；刷新后修改仍在。
- 模拟服务端创建已提交但响应丢失，随后保持同步失败：队列经刷新、明确保留后退出和切换到审核账号仍保留原归属；原账号重新登录并逐条重试后，服务端仅有一条对象。
- 实际验证另一标签页退出与登录后的身份同步，以及离线撤销、联网重做后只恢复一个新对象。

### 边界

本次验收使用独立 API、Web、PostgreSQL、Redis、对象存储、SMTP 和 Celery；未部署生产、未推送远端，也未运行远端 CI。密码恢复复用进程内后台发送，进程在发送前终止时需通过现有入口重试或联系管理员。真实浏览器主流程采用图片；不将本次验收视为视频或点云渲染性能认证。

本次独立服务、六个测试容器及其中的账号、项目、数据库、对象和邮件已清理；临时报告、截图、导出包、文档构建输出和编译中间产物已移除。九个修改子代理工作树均在确认干净且提交补丁等价整合后删除；保留其他工作树与日常环境。
