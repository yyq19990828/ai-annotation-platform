# v0.21.1 · 检测式视频跟踪（detect-then-track）作为 pipeline 源阶段

状态：**定案（accepted，2026-07-02）**。承接 v0.21.0 全局编排选择器，是它的**第一个「非整图源」
消费方**。把「检测模型 + ByteTrack/BoT-SORT」的全自动多目标跟踪落成一个 pipeline 源阶段，源类型
从 `image` 扩到 `video`。此前四个悬空点（转换链 / worker 并发 / 方向 A 归位 / tracker 选择归属）
已读实定死，见各 WS。

**执行顺序（依赖链）**：`WS0`（前端去硬编码，gating，先落）→ `WS1`（协议契约）→ `WS2`
（yolo-backend tracker task）→ `WS4`（平台落库，真实工作量重心）；`WS3`（onnxtools）跟进、不绑首发。

## 背景

- **现状只有交互式 tracking**：`VideoTrackerJob` 走 SAM2/SAM3 种子传播——`annotation_id`
  强制必填（人先画一帧种子框），走 `predict_interactive`（有状态、单 task），平台侧
  `video_tracker_runner.py` 做跨窗续追。它**绕过标准 `/predict`**，由 api 层 `video_tracker_adapters`
  直调。这条是「人在环、单对象、workbench 交互算子」，本 plan **原样不动**。
- **检测式 tracking 一行实现都没有**：`capability_registry.py:272` 的 `tracker` task 把
  ByteTrack/OC-SORT 列为 `typical_models` 文字示例，但 adapter 注册表只有 `mock_bbox` /
  `sam2_video` / `sam3_video`，全是交互式。
- **它和交互式是不同物种**：无种子、多对象、全自动、离线批量；**时间关联全在 backend 内**
  （ultralytics tracker），平台只投视频、收聚合 track，自己一点时间编排都不做。因此它天生
  是 pipeline 源阶段的形状 = 「`video` 源的 detection，输出多一个 `track_id`」，走**标准批量
  `/predict` + Celery pipeline**，不进交互式那条链。
- **ultralytics 原生免费**：yolo-backend 已依赖 ultralytics 8.4.x。`model.track(source,
  persist=True, tracker="bytetrack.yaml", stream=True)` 内建 ByteTrack/BoT-SORT，直接吃整段
  视频、内部解帧、逐帧出 `boxes.id`。首发零新依赖。
- **硬边界（ultralytics 文档明证）**：单帧图像无跨帧状态、产生不了有意义的 track_id。故检测式
  track 的 `supported_inputs` **只能是 `video`，不含 `full_image`/`crop`**。

## 设计定位

- 输出直接落已存在的 `VideoTrackGeometry`（`_jsonb_types.py:460`，`track_id` + `keyframes[]`），
  schema 零改动。
- backend 给的原生 int track_id → 平台映射成 `trk_<uuid>`，原 int 存 `semantic_label`
  （如 `car_3`）。`source="prediction"`。
- **坐标系**：平台内部**统一 `0-1` 归一化**（`prediction.py` 落库时 `_percent_scale` 自动检测：
  任一值 >1 视为百分比除 100，否则视为已归一）。backend **直接返回 `0-1`**（与交互式 tracker +
  现有 video 协议一致），**不要依赖百分比自动检测**——小目标 bbox 各分量可能全 <1，跨该边界有
  误判风险（参见 [[project_interactive_candidate_coords]] 的坐标系教训）。`VideoTrackKeyframe.bbox`
  存 `{x,y,w,h}` 0-1。

## 工作项（高层，细节待立项展开）

### WS1 · 协议契约扩展（`apps/_shared/protocol_v2`）

- **`vocab.py` 新建 `INPUT_VALUES` 受控词表**：`("full_image", "crop", "bbox_prompt",
  "point_prompt", "video")`。把现在散落的 `supported_inputs` 自由字符串收编（onnxtools 的
  `full_image`/`crop` 对齐进词表）。**这一步的词表本身在 v0.21.0 前置里先落**（见下），WS1 只补
  `video` 值 + 校验接线。
- **`schemas.py` 支持 track item 形状**。检测式 track 的 `/predict` 响应里，`result[]` 每项是
  一个**已聚合好的轨迹**（后端 stream 整段视频、按 `boxes.id` 聚合，平台不再聚合）：
  ```jsonc
  {
    "type": "video_track_bbox",
    "track_id": 3,                 // backend 原生 int（ultralytics boxes.id）
    "class_name": "car",
    "score": 0.87,                 // 轨迹级（均值或代表帧）
    "keyframes": [
      // bbox 用 {x,y,w,h}、0-1 归一化——对齐 VideoTrackKeyframe + 现有交互式 tracker wire
      { "frame_index": 0, "bbox": { "x": 0.102, "y": 0.201, "w": 0.080, "h": 0.065 }, "score": 0.90 },
      { "frame_index": 1, "bbox": { "x": 0.105, "y": 0.203, "w": 0.081, "h": 0.065 }, "score": 0.88 }
      // id=None 的帧（低置信）直接丢弃
    ]
  }
  ```
- **`/setup` model 条目声明**：`task="tracker"`、`supported_inputs=["video"]`、新增
  `supported_trackers=["bytetrack", "botsort"]`、`params` schema（`conf`/`iou`/`tracker`/`classes`）。
- **tracker 算法选择 = `params.tracker`，不是 variant 轴，不进全局池（已读实定死）**。
  variant 轴在本系统里严格是「选哪个物理权重」（yolo `series×size → .pt`，`model_registry.py:31`
  MODEL_MATRIX；gsam2 sam/dino 权重档）。`bytetrack` vs `botsort` **不换权重**——同一 det 权重、
  只换推理时关联算法，语义是 param 不是 variant，塞进 `supported_variants` 会与
  `variants_shared_across_tasks`（同权重跨 task 合并）等假设冲突。而 params 是 v0.21.0 **故意不进
  全局池、apply-time 再选**的（与 `conf`/`iou`/阈值一致）。故 tracker 算法选择也落 apply-time。
- **`supported_trackers` 与 `params.tracker` 不冗余——角色分工写明**（这是当前含糊点）：

  | 字段 | 角色 | 谁消费 | 表单控件？ |
  |---|---|---|---|
  | `supported_trackers=[…]` | **能力广播**：「我是 tracker，可选算法这些」 | 可达性判定 / 源类型徽标 / ModelMarket | ❌ 分类信号，非控件 |
  | `params.tracker`（enum 约束到 `supported_trackers`，默认 `[0]`） | **apply-time 实选** | 采纳/应用 pipeline 到项目时 | ✅ 在 apply 层，不在池 |

  同 `supported_prompts`（广播能力）vs 实际 predict 发的 prompt 的既有模式。全局池源阶段只从
  `task=="tracker"` + `supported_trackers` 非空 判出「video 源」，**不在池里选哪个 tracker**。
- 文档：`docs-site/dev/reference/ml-backend-protocol.md` 补「检测式 tracker（批量 `/predict`）」
  一节，与现有「§2.2 交互式 `video_tracker`」并列，讲清两者是两条链。

### WS2 · yolo-backend 实现（首发，ultralytics 原生）

**进度（2026-07-02）· `/setup` 声明部分 ✅ 已完成**：
- `model_registry.py` 加 `TASK_TRACKER="tracker"`；**MODEL_MATRIX 别名到 detection**
  （`MODEL_MATRIX[TASK_TRACKER] = MODEL_MATRIX[TASK_DETECTION]`，共享同一 series→sizes 对象、零漂移）、
  `_TASK_SUFFIX[TASK_TRACKER]=""`（解出 detection 权重文件名）。**读实修正**：定案原写"MODEL_MATRIX[tracker]
  复用"，落地选**别名而非新键**——测试套件整体假设「task 是 MODEL_MATRIX 键」（`test_setup_variant_combinations_all_legal`
  等直接 `MODEL_MATRIX[task][series]`），别名让这些校验对 tracker 一致工作；`iter_supported_combinations`
  从 80→113（+33 tracker），`download_weights` 因 `_download_one` 的 `weight_path.exists()` 命中而**零重复下载**。
- `main.py` `_build_tracker_model_entry()`：`task=tracker`、`supported_inputs=["video"]`、
  `supported_trackers=["bytetrack","botsort"]`、复用 detection 的 series×size variant、`params` 带
  `tracker` enum（默认 bytetrack）；注册进 `/setup` models（8→9 条）。类别表复用 COCO detection。
- 测试：`test_setup_shape` 加 `test_setup_tracker_model_shape` + 更新 4 处全局断言（9 model / supported_inputs
  video 分支 / task 集含 tracker）；`test_model_registry` 加 tracker 权重文件名 + combo 数 80→113 + 矩阵键断言。
  yolo-backend 全量 153 passed（1 pre-existing 失败 `test_world_mask_falls_back_to_box` 与本变更无关：
  predictor 已改抛 422 但旧测试仍期望静默降级）。ruff 干净。

**剩余（未做，需 GPU/video 栈，与协议文档 §2.2 并列的检测式 tracker 章节同批落）**：
`/predict` 的 `context.type="tracker"` 分支（`predictor.py` 调 `model.track(stream=True, persist=True,
tracker=<algo>.yaml, conf, iou, classes)` 聚合 `video_track_bbox` item，坐标直返 0-1）+ `ml-backend-protocol.md`
补「检测式 tracker（批量 `/predict`）」一节。**doc 与 predict 分支同 commit**（避免文档超前于代码，CLAUDE.md §9）。

---

**编排形态 = 独立 `task="tracker"` 节点，不给 det 加 track 参数（已读实定死）**。平台整套源类型/
可达性/徽标机制都挂在 `task` 上（capability_registry、`deriveSourceShape`、modality、
`supported_inputs`），独立 `task="tracker"`（registry 已有，`default_modalities=("video",)`）一次
接进全部；给 det 加 param 则全绕过（params 不在 `/instances`，池子看不见，源类型无法从 task 派生，
直接违反 v0.21.0 前置 #2「源类型数据驱动」）。且 det→扁平 bbox、tracker→`video_track_bbox`+keyframes
输出分叉，独立 task 让「本阶段产轨迹」静态可知。**权重复用正交、零冲突**：`_TASK_SUFFIX`
（`model_registry.py:92`）det 后缀为 `""`，tracker 也给 `""` → `resolve_weight_filename("tracker",
"yolo11","s")` 直接解出 `yolo11s.pt` = det 权重。独立 task ≠ 独立权重下载。

- `model_registry.py`：加 `TASK_TRACKER="tracker"`，`MODEL_MATRIX[tracker]` **复用 det 那份
  series→sizes**，`_TASK_SUFFIX[tracker]=""`（权重零新增）。
- `/setup` 加 tracker model 条目：`task="tracker"`、`supported_inputs=["video"]`、
  `supported_trackers=["bytetrack","botsort"]`、`supported_variants`=复用 det 的 series/size 轴、
  `params`（`conf`/`iou`/`tracker`/`classes`）。
- `/predict` 新增 `context.type="tracker"` 分支：
  - `task.file_path` = 视频 signed URL；backend 流式 fetch。
  - `model.track(source, stream=True, persist=True, tracker=<bytetrack|botsort>.yaml,
    conf, iou, classes)` 逐帧收 `boxes.id`。
  - 按 track_id 聚合成 `video_track_bbox` item；坐标**直接返回 0-1**（勿转百分比、勿依赖平台端
    自动探测，见「设计定位·坐标系」+ WS4）。
- **依赖零新增**：ByteTrack/BoT-SORT + lap 由 ultralytics 自带。
- 显存：`stream=True` 逐帧，轨迹状态轻量，支持长视频（对比 SAM2 需 300 帧窗口防爆显存）。

### WS3 · onnxtools-backend 实现（跟进，不与 WS2 绑首发）

- onnxtools 无 ultralytics → 需**自带 ByteTrack 关联**（纯 numpy/scipy：IoU + 卡尔曼 + 匈牙利），
  或移植 ultralytics `trackers/byte_tracker.py`。
- `/setup` 加 `vehicle-track` model 条目（`atom`，`task="tracker"`，`supported_inputs=["video"]`）；
  复用现有 `vehicle-detect` 逐帧 RTDETR，外接 ByteTrack 状态。

### WS4 · 平台侧：pipeline 源阶段接入 `video` 源

**投递（改动极小）**：现有源阶段 `_run_batch` 已是 `batch_size=1` 逐 task 投 signed URL，且对
`file_type` image/video 一视同仁（`tasks.py:289`）。video task 天然一视频一 task，`batch_size=1`
正合适——投视频 URL、backend 内解帧，**平台投递侧几乎不改**。仅需 `_build_predict_context`
（`tasks.py:558`）透传 `type="tracker"` + tracker 选择 + params。

**超时（关键约束，必须改）**：worker 调 backend 的 `ml_predict_timeout` 默认 **100s**
（`config.py:144`，`ml_client.py:184` 用它建 httpx client），长视频整段 track 会被掐断，且无
分片重试。首版方案：
  - 加 **tracker 专属长超时**（新增 `ml_predict_timeout_tracker`，或按 `context.type` 选超时值），
    默认 300–600s；
  - backend `model.track(stream=True)` 单次整段跑（内存友好），**首版限制视频时长/帧数上限**，
    超限 `log()` 明确截断（不静默丢）；
  - 分窗续追 / 异步作业模式（像交互式 `VideoTrackerJob` 那样触发+轮询）留作规模化后续——注意
    分窗会把「跨窗 track_id 续接」推回平台侧、破坏「时间关联全在 backend 内」，故首版**不做**。
  - **worker 并发约束（运维，必须在验收/文档写明）**：长超时期间 httpx 单请求挂 300–600s，
    **该 Celery worker slot 被独占**。并发投几段长视频会占满 worker 池、饿死其它批量预标注。首版
    至少在文档/验收里写明「**tracker 源建议独立 queue 或限并发**」——这是一句运维约束、非代码量，
    现在写便宜；不写则第一次有人投 10 段视频就拖垮整个预标注 worker。

**落库（真实工作量所在）**：现在 `prediction.result` 只输出 bbox/polygon 平铺列表，**无 track_id/
keyframes 结构**。打通检测式 track → `VideoTrackGeometry` annotation 的链路。**已读实转换链，结论如下
（把此前"需验证 `to_internal_shape` 认这个 type"的未知数定死）**：

> **进度（2026-07-02）· 下述 #1 `to_internal_shape` 分支 ✅ 已完成**：`prediction.py` 加
> `_track_to_internal_shape()` + `to_internal_shape` 的 `video_track_bbox` 早返分支——遍历
> keyframes 逐帧组 `{frame_index, bbox, source:"prediction"}` → `VideoTrackGeometry` dict，
> **信 0-1 不走 `_percent_scale`**，class_name 取顶层，semantic_label 有则带、无则省，
> tool_unit_id=bbox。`test_prediction_schema_adapter` 加 5 例（含"小坐标不被百分比误缩"
> + "产出 geometry 过 `VideoTrackGeometry` Pydantic 校验"锁死 accept 落库不 422）。堵住了
> 下面说的"静默落空几何"失败面。**剩 #3 int→uuid ingestion 映射**（worker 侧，需在 worker
> 存 `prediction.result` 前把原生 int track_id 映射成 `trk_<uuid>` + semantic_label；`to_internal_shape`
> 已假设收到的是映射后的字符串 track_id）**+ accept 路径验证**，与 predict 分支同批（需栈）。

- **`to_internal_shape` 现状：不认，且失败是静默的**。它只处理**扁平单几何** LS shape——
  `prediction.py:147` `if "geometry" in s: return s`（已内部形状才透传），否则读 `val = s.get("value")`
  只分支 `rectanglelabels / polygonlabels / polylinelabels / keypointlabels`，**其它一律
  `else → geometry = {}`**（`:260`）。plan 提议的 track item `{type, track_id, class_name, keyframes:[…]}`
  既无 `geometry` 键也无 `value` 键 → 掉进 `else` → **keyframes 静默丢弃**；accept 路径
  `annotation.py:376` `geometry=shape.get("geometry", {})` 写库**无 `VideoTrackGeometry` union 写时校验**
  → **静默落一批空几何 annotation，不报错**。这是最坏形态，必须在 WS4 堵住。

- **WS4 落库 = 三件事，不是"补一支"**：
  1. **新 `video_track_bbox` 分支（结构）**：现有分支全是"读 `value.{x,y,w,h}` 出单几何"，track item 是
     **嵌套聚合**结构，须新写一支：遍历 `keyframes[]` → 逐帧组 `{frame_index, bbox, source:"prediction"}`
     → 拼成 `VideoTrackGeometry` dict。无现有分支可复用。
  2. **坐标归一化：新代码，且不走 percent 自动探测**。`_percent_scale` 今天**根本到不了 keyframes**
     （代码不递归进去），所谓"扩展 `_percent_scale`"是误述——这是全新代码。按坐标系红线
     （[[project_interactive_candidate_coords]]），该支**直接信 backend 的 0-1，不调 `_percent_scale`**
     （小目标 bbox 各分量可能全 <1，自动探测会误判为百分比）。
  3. **`int track_id → trk_<uuid>` 映射放在 worker ingestion，不进 `to_internal_shape`**。
     `to_internal_shape` 在**每次 read（predictions 列表）都会跑**，若在其中 `uuid4()` → 每次读生成不同
     trk_id、**轨迹身份漂移**。故映射必须在 ingestion 一次性做（worker 收 backend 结果、写
     `prediction.result` **之前**），存库 result 里已是 `trk_<uuid>`；`to_internal_shape` 只负责把已带 uuid
     的 track 重塑成 geometry，保持纯函数确定性。`semantic_label="{class}_{int}"` 同处生成。
  4. **补 unknown-track-type 的显式失败/warning**，堵掉上面的"静默落空几何"数据丢失面。

- **可放心信（已就位，plan 无需动）**：tool_unit 派生对 `video_track_bbox` 已 OK
  （`prediction.py:74` 显式 + `:45` 默认 fall-through 均归 `bbox`）；`VideoTrackGeometry` /
  `VideoTrackKeyframe` schema 齐全，`source="prediction"` 枚举值已存在，**schema 零改动**属实；
  前端渲染/审核复用属实。
- `PredictionMeta.extra.pipeline` 记录 stage 溯源（复用现有机制）。

**前端（复用）**：视频工作台已能渲染 `VideoTrackGeometry`（交互式 tracker 在用）；检测式 track 落的
是同一几何类型，渲染 + 预标注审核（`predictions` 表待审接受）**直接复用**，本版不新增画布代码。

## v0.21.0 前置调整（必须在 v0.21.0 落，否则 v0.21.1 回改选择器）

v0.21.0 WS2 选择器要「按 `supported_inputs` 判父子可达性」。若这版把 `supported_inputs` 的枚举
写死成现有 4 值、或把源阶段硬设为「吃整图」，v0.21.1 加 `video` 源时选择器 + 校验要回改。故：

1. **`supported_inputs` 受控词表 + 数据驱动过滤**：v0.21.0 在 `vocab.py` 建 `INPUT_VALUES`
   受控词表（此刻可**含 `video` 占位**，即使还没 backend 声明它），选择器 `pipelineGraph.ts` /
   `stageWarning` 的渲染与可达性判定**从词表读**，不 hardcode。
2. **源阶段「源类型」不硬设整图**：可达性逻辑里，源阶段（`parent_stage=null`）不排斥
   `supported_inputs` 含 `video` 的 backend——它应被判为合法源，只是源类型标注为 `video`。

**最小原则**：v0.21.0 只做「词表 + 过滤/可达性数据驱动化」，**不预建 video 源的专属 UI**
（那是 v0.21.1 WS4）。避免投机性抽象。

**状态（2026-07-02）**：v0.21.0 **已发布**，上面 #1（`INPUT_VALUES` 词表 + `video` 占位 +
`tracker` task/modality）随选择器对齐一并落地；但 #2 的**渲染层去硬编码（下述方向 A）并未随 0.21.0 落**
——它仍是 documented-only。故方向 A **重新归位为 v0.21.1 的 WS0**（tracker 的第一步：先去硬编码、
再上 video 源），不再声称「v0.21.0 收尾随手做」。

### WS0 · 编排节点 UI 去硬编码（v0.21.1 第一步，先于 tracker 落）· ✅ 已完成（2026-07-02）

上面前置 #2 的**具体落地**，因未随 0.21.0 发布而下沉为 v0.21.1 首个工作项：编排节点 UI 现在把
「源 = 检测 = 整图」写死在 6 处，全都没读已在手的 `taskType` 与词表。tracker（video 源）落地前
**必须先做这一步**，否则 6 处硬编码会把 video 源渲染成「检测/整图」。

**落地记录**：`pipelineGraph.ts` 新增 `deriveSourceShape(model) → {role, sourceType, sourceTypeLabel,
productLabel, countLabel}`（对称 `deriveDownstreamShape`，词表 `TASKS`/`MODALITIES` 驱动）；6 处硬编码
（`ProjectDetailPanel` / `GlobalPipelineLibraryPage` 源节点、`GlobalStageInspector` 源标题、
`PipelineGraphCanvas` tooltip+name+footer 计数）全部改派生；节点头加**源类型徽标**（图像 / 视频）。
`pipelineGraph.test.ts` 补 5 例（detection→image / tracker→video / 缺 supported_inputs 回落 task 默认
模态 / null 兜底）。tsc + 全量 test（AIPreAnnotate 160 例）+ lint 全绿。
**两处对定案的微调**：(a) 源类型徽标暂放节点头首行行内，**未预建 ROADMAP §A scene「执行单位」的
第二行空槽**（避免投机性空结构，§2；scene 落地时再加一行不影响现结构）；(b) `SourceShape` 去掉了
`producesGeometry`（算了没消费方——源的 `canAddChild` 由 `sidProducesGeometry` 对 ROOT_SID 恒 true 决定，
tracker 作父门控是 WS4 的事）。

**硬编码债清单（SSOT）**：

| 位置 | 硬编码 |
|---|---|
| `utils/pipelineGraph.ts:44` | 源 `role = {label:"检测", icon:"box"}` |
| `components/ProjectDetailPanel.tsx:390,395` | 源 role "检测" + `producesGeometry:true` + `detail:"检测框"` |
| `GlobalPipelineLibraryPage.tsx:168,171` | 同上 |
| `components/GlobalStageInspector.tsx:269` | `"源阶段 · 检测参数"` |
| `components/PipelineGraphCanvas.tsx:110,127` | `"检测（源）"` / `isSource ? "检测"` |

**关键**：源节点**已经拿到 `taskType`**（项目侧 `cfg.primaryModel?.task`、全局侧 `srcOption?.model.task`），
前端词表 `capabilityVocab.gen.ts` **已有 `video` input + `tracker` task（label「视频追踪」）+ modality**。
债只在渲染层没读词表，去硬编码近乎免费。

**做法（对称已落地的 `deriveDownstreamShape`，同一套模式）**：

1. `pipelineGraph.ts` 加 `deriveSourceShape(model) → { role, icon, sourceType, producesGeometry, detail }`：
   - `role`/`label` 走 `TASKS[task].label`（detection→"目标检测"、tracker→"视频追踪"）。
   - `sourceType` 走 `supported_inputs` / `defaultModalities`（image / video / …），词表驱动，不 hardcode。
   - `producesGeometry` 由 task 判（tracker 也产几何，但语义是轨迹 track，`detail` 相应改「轨迹」）。
2. **节点头加「源类型」徽标**（图像 / 视频 / …）——这是当前完全缺失的维度：tracker 源和 detection
   源在画布上现在长得一模一样，用户分不出投的是图还是视频。徽标槽位**预留第二行**给后续「执行单位」
   （见 ROADMAP §A scene 源条目），避免 scene 落地时重排节点头。
3. 替换上表 6 处硬编码为 `deriveSourceShape` 派生；`GlobalStageInspector` 源阶段标题按 shape 动态。
4. `pipelineGraph.test.ts` 补 `deriveSourceShape` 判据锁死（detection→image 源 / tracker→video 源）。

**仍守最小原则**：只做去硬编码 + 源类型徽标（数据已在手），**不建 video 源专属编辑 UI / 不改投递校验**
（那是 v0.21.1 WS4）。

## 风险

- **推理超时**（`ml_predict_timeout=100s` 掐断长视频，无分片重试）：见 WS4 超时方案；本质是首版
  视频时长上限的来源。tracker 专属长超时是首版必须项，不是可选优化。
- **落库 schema 扩展是主要工作量**：`prediction.result` → `video_track_bbox` → `VideoTrackGeometry`
  的转换链（WS4「落库」）是本版真正的实现重心，backend 侧反而轻（ultralytics 免费）。
- **输出 payload 体量**（长视频 × 多 track × 多帧 keyframes）：首版限制视频时长/帧数上限，
  超限 `log()` 明确截断（不静默丢），落库分批。
- **track_id 稳定性**：ultralytics int id 单次 `track()` 会话内唯一；首版**单次整段 track**，
  不分窗，避免跨窗续接的 id 重编。
- **onnxtools 自带 ByteTrack 的移植成本**：故 WS3 跟进、与 WS2 解耦。
- **时序**：v0.21.0 前置 #1（词表）已落；#2 的渲染层去硬编码下沉为 **WS0，是 v0.21.1 的第一步**，
  须先于 tracker（WS1/WS2/WS4）落，否则 6 处硬编码把 video 源渲染成「检测/整图」。
- **worker 并发**：tracker 长超时独占 worker slot，并发长视频会饿死批量预标注池；首版须以独立
  queue / 限并发约束（见 WS4 超时节），非代码量、是运维/验收项。

## 验收

- yolo-backend tracker model：投一段视频 → 返回聚合 `video_track_bbox[]`，每 track 有稳定
  track_id + 逐帧 bbox。
- pipeline 选 tracker backend 作源阶段 → 跑完落一批 `VideoTrackGeometry` 预标注，视频工作台
  能渲染轨迹、人工可审核接受。
- v0.21.0 选择器能列出 `supported_inputs=["video"]` 的 backend 且判为合法源阶段（前置生效）。

## 不在范围

- **`image_sequence`（scene 抽帧跨 task 聚合）源**：打破 pipeline 「per-task 独立执行」单位，
  逼执行单位从 task 升到 scene——最贵的一块，单独立项（见 Q4 探讨结论）。
- **检测式 track 下游逐帧 crop 分类**（帧 × track 二维展开，`roi.py` 要从「图内多框」扩成
  「track 内多帧多框」）：后续。
- **交互式 SAM2/SAM3 tracking**：原样不动。
- **ReID / BoT-SORT 外观特征调优**：首版 ByteTrack 基线。
