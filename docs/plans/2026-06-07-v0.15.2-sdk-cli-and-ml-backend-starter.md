# v0.15.2 · Python SDK / CLI + ML Backend Starter

> 规划日期:2026-06-07 · 范围:把平台已有 OpenAPI、API key、ML Backend 协议产品化为外部集成入口。此计划承接开源平台对标第 3 点,顺延到 0.15,不挤占 0.14 的产品面 patch 线。
>
> 定位:先覆盖稳定工作流,不要一次性包全 API。SDK 是客户集成物料,不是内部前端 codegen 的替代品。

## 1. 背景

本平台已有:

- FastAPI OpenAPI。
- API key service。
- 异步 jobs。
- 数据集上传 / 导入。
- 预测导入 / 导出。
- ML Backend `/health` / `/setup` / `/predict`。

缺的是:

- 外部用户可直接安装的 Python SDK。
- CLI cookbook。
- ML Backend starter 模板。
- 版本兼容策略。

Label Studio 的 `label-studio-sdk` 和 `label-studio-ml-backend` 是强产品资产。本平台应做轻量版,但保留自身协议边界。

## 2. SDK 首版范围

包名暂定:

```text
ai-annotation-sdk
```

核心对象:

```python
from ai_annotation import Client

client = Client(base_url="...", api_key="...")
```

覆盖 8 个稳定工作流:

1. `projects.list/create/get`
2. `datasets.create/upload_files/upload_zip/link_project`
3. `tasks.list/get/next`
4. `annotations.list/create/update/delete`
5. `predictions.import_file`
6. `jobs.list/get/wait`
7. `exports.create/wait/download`
8. `api_keys.list/create/revoke`(可选,admin only)

不覆盖:

- 所有 admin 页面 API。
- 所有 dashboard/analytics API。
- 内部 workbench patch 细节。

## 3. CLI 首版范围

命令:

```bash
aap login --url ... --api-key ...
aap projects list
aap projects create --name ... --type image|video|lidar
aap datasets create --name ...
aap datasets upload <dataset-id> ./data --zip
aap datasets link <dataset-id> <project-id>
aap predictions import <project-id> result.json --format aap_json
aap jobs wait <job-id>
aap export project <project-id> --target aap_json --out out.zip
```

配置:

- `~/.config/ai-annotation/config.toml`
- 支持 env: `AAP_BASE_URL`, `AAP_API_KEY`

## 4. ML Backend Starter

新增模板仓库或目录:

```text
examples/ml-backend-starter/
  pyproject.toml
  app.py
  README.md
  models/
    echo_detector.py
    ocr_stub.py
```

必须封装:

- `/health`
- `/setup`
- `/predict`
- 读取 presigned URL。
- 返回 LabelStudio-compatible result。
- capability snapshot 示例。
- metrics/logging 示例。

不做:

- 训练框架。
- 模型下载器。
- GPU 调度。

## 5. 文档

docs-site 新增:

- `dev/sdk/quickstart.md`
- `dev/sdk/python-client.md`
- `dev/sdk/cli.md`
- `dev/ml-backend/starter.md`
- cookbook:
  - 上传数据并创建项目
  - 批量导入预测
  - 等待导出完成
  - 写一个 OCR backend

## 6. 生成策略

建议:

- 使用 OpenAPI 生成 typed client 作为底层。
- 手写薄 wrapper 提供稳定工作流。
- 生成层不作为公开 API;公开 API 是 wrapper。

版本策略:

- SDK minor 跟平台 minor。
- breaking change 写 migration guide。
- 首版标记 beta。

## 7. 测试

- SDK against mocked HTTP。
- CLI snapshot tests。
- starter backend contract tests:
  - `/health` shape。
  - `/setup` capability。
  - `/predict` example response。
- docs snippets 可执行 smoke test。

## 8. 风险

- **过早锁死 API**:只包稳定 workflow,不要暴露内部接口。
- **OpenAPI 生成层泄漏**:用户不应直接依赖 generated client 的所有方法。
- **认证安全**:CLI config 文件权限提示。
- **Starter 变成框架**:只提供最小模板,不要维护复杂模型 runtime。

## 9. 与其他计划关系

- v0.14.9 的模型能力目录会给 starter 的 `/setup` 示例提供真实字段。
- v0.14.8 的 Data Manager 暂不进 SDK 首版;filter DSL 稳定后再加。
- v0.15.0/0.15.1 是 3D 时序线,与 SDK 并行;SDK 首版不覆盖 ego pose 相关实验 API。
