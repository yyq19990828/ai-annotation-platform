# v0.23.13 · WebCodecs 精确帧正确性与 Konva 接线

> Status: Planned · Epic Phase 1 · 2026-07-24
>
> 父 Epic：[WebCodecs 精确帧与 Konva 播放链路](2026-07-24-v0.23.13-webcodecs-konva-precise-frame-epic.md)
>
> 前置：v0.23.12
>
> 后续：[v0.23.14 GOP 会话与资源预算](2026-07-24-v0.23.14-webcodecs-gop-session-resource-budget.md)

## 1. 本版目标

本版把已经存在但生产不可达的 WebCodecs decoder 恢复为可用的 Konva 暂停帧来源，并先建立可证明的
正确性与降级合同。

成功后：

- 设置中开启“WebCodecs 精确解码”并刷新，会真实请求当前帧所属 chunk；
- 前端用后端 sample manifest 构造 `EncodedVideoChunk[]`；
- decoder 按目标 timestamp 选择 `VideoFrame`；
- precise bitmap 在暂停态优先进入 `VideoKonvaMediaLayer`；
- 当前帧 JPEG 与画布读取同一个 bitmap；
- pending、缺 samples、unsupported codec、CORS 和 decode failure 都回退；
- 播放态完全保留 `<video>`；
- 快速 seek 不显示旧帧；
- 当前明显死代码和误导注释得到收口。

本版本不实现持久 GOP decoder session；同一帧请求 single-flight，但随机目标仍可从关键帧重新建立一次
无状态 decode。性能复用在 v0.23.14 单独完成。

## 2. 基线事实

### 2.1 已有后端合同

| 能力                                   | 位置                                               | 状态 |
| -------------------------------------- | -------------------------------------------------- | ---- |
| chunk 行与 signed URL                  | `apps/api/app/services/video_frame_service.py`     | 已有 |
| chunk smart-copy / transcode           | `apps/api/app/workers/media.py`、`media_chunks.py` | 已有 |
| packet sample manifest                 | `apps/api/app/workers/media_probe.py`              | 已有 |
| `avcC/hvcC` description                | `apps/api/app/workers/media_codec.py`              | 已有 |
| `/videos/{id}/chunks/{cid}/samples`    | `apps/api/app/api/v1/videos.py`                    | 已有 |
| task manifest v2 + `chunk_size_frames` | `apps/api/app/api/v1/tasks/video.py`               | 已有 |

不新增后端业务功能，只补已有端点缺失的测试。

### 2.2 已有前端骨架

| 能力                        | 位置                                  | 缺口                                      |
| --------------------------- | ------------------------------------- | ----------------------------------------- |
| flag + decoder + bitmap LRU | `stage/useVideoChunkDecoder.ts`       | 无生产调用；目标帧选择错误                |
| chunk/sample API wrapper    | `api/videos.ts`                       | 缺单 chunk 方法；现有方法无消费者         |
| chunk/sample 类型           | `types/index.ts`                      | 无 manifest v2 前端类型                   |
| native bitmap fallback      | `stage/useVideoBitmapCache.ts`        | 正常                                      |
| 播放/seek 控制              | `stage/useVideoPlaybackController.ts` | 明确 TODO 未迁 WebCodecs                  |
| Konva 媒体层                | `stage/VideoKonvaMediaLayer.tsx`      | 已支持“播放 video / 暂停 bitmap”          |
| Konva stage                 | `stage/VideoKonvaStage.tsx`           | 读取 `activeBitmap`，忽略 `displayBitmap` |
| 实验设置                    | `state/workbenchSettingsFields.ts`    | 可写 flag，但开启后当前无效果             |

### 2.3 已确认的错误

`decodeChunkToBitmap(config, chunks, targetFrameIndex)` 当前使用：

```ts
const wanted = frames[targetFrameIndex] ?? frames[frames.length - 1];
```

这里的 `targetFrameIndex` 是视频全局帧号，而 `frames` 只包含局部 GOP decoder output。即使把它改成
局部 index，B 帧也会让 decode order 与 presentation order 不同。唯一可靠选择键是输入 chunk 的
timestamp 与 output `VideoFrame.timestamp`。

## 3. 范围与非目标

### 3.1 本版范围

- 前端 manifest v2、单 chunk 与 samples 读取；
- chunk bytes 获取与 abort；
- 纯函数 demux build；
- decoder config support、timestamp target 与资源清理；
- 无状态 single-flight；
- paused/stable frame 编排；
- Konva `displayBitmap` 接线；
- 当前帧 JPEG 同源；
- fallback reason 与最小诊断；
- 定向 API、Vitest 与控制器测试；
- 设置说明和 Unreleased CHANGELOG。

### 3.2 本版非目标

- 不做 decoder session 复用、向前增量 decode 或跨帧预取；
- 不把 bitmap cache 改成字节预算；
- 不引入 Worker；
- 不新增 Range 请求；
- 不修改默认 flag；
- 不改后端 schema / DB / env；
- 不做完整真实浏览器资格矩阵；v0.23.15 负责；
- 不删除后端 `/chunks` 列表端点。

## 4. 冻结实现设计

### 4.1 新增前端 manifest v2 类型

在 `apps/web/src/types/index.ts` 增加与后端 `VideoManifestV2Response` 对齐的类型。至少完整承载：

```ts
export interface VideoManifestSegmentOut {
  id: string;
  segment_index: number;
  start_frame: number;
  end_frame: number;
  status: "open" | "assigned" | "locked" | "completed";
  assignee_id: string | null;
  locked_by: string | null;
  locked_at: string | null;
  lock_expires_at: string | null;
}

export interface VideoManifestV2Response {
  task_id: string | null;
  dataset_item_id: string;
  video_url: string;
  poster_url: string | null;
  fps: number | null;
  frame_count: number | null;
  duration_ms: number | null;
  chunks_manifest_url: string;
  frame_timetable_url: string;
  frame_service_base: string;
  chunk_size_frames: number;
  segments: VideoManifestSegmentOut[];
  frame_cache_formats: Array<"webp" | "jpeg">;
  expires_in: number;
}
```

要求：

- 使用 API JSON 的 snake_case，不创建第二套 camelCase mirror；
- `chunk_size_frames` 必须是正整数，否则 precise pipeline fallback；
- 现有 v1 `TaskVideoManifestResponse` 保持不变；
- 不让正常视频工作台为了实验功能切换主 manifest；v2 仅在 decoder active 时增量查询。

### 4.2 API client

在 `apps/web/src/api/tasks.ts`：

```ts
getVideoManifestV2(taskId: string): Promise<VideoManifestV2Response>
```

在 `apps/web/src/api/videos.ts`：

```ts
getChunk(datasetItemId: string, chunkId: number): Promise<VideoChunkOut>
getChunkSamples(datasetItemId: string, chunkId: number): Promise<VideoChunkSamplesResponse>
```

决策：

- task manifest 方法与现有 `tasksApi.getVideoManifest()` 同属 `tasksApi`；
- 使用单 chunk `GET /videos/{id}/chunks/{chunk_id}`；
- 不使用无范围 `getChunks()` 获取 chunk size；
- `getChunks()` 若 `rg` 确认仍无其它消费者，随 `VideoChunksResponse` 前端类型一起删除；
- 后端列表端点和 schema 不删除。

### 4.3 新增 `videoChunkDemux.ts`

文件：

```text
apps/web/src/pages/Workbench/stage/videoChunkDemux.ts
apps/web/src/pages/Workbench/stage/videoChunkDemux.test.ts
```

导出：

```ts
export interface EncodedVideoDecodePlan {
  config: VideoDecoderConfig;
  chunks: EncodedVideoChunk[];
  targetFrameIndex: number;
  targetTimestampUs: number;
  chunkId: number;
  gopStartDecodeIndex: number;
  targetDecodeIndex: number;
}

export function buildEncodedVideoDecodePlan(
  chunkBytes: ArrayBuffer,
  samples: VideoChunkSamplesResponse,
  targetFrameIndex: number,
): EncodedVideoDecodePlan | null;
```

算法：

1. 验证 `codec_string`、width、height 和 `samples`。
2. 在保持后端 packet decode order 的 `samples` 数组中查找
   `sample.frame_index === targetFrameIndex`。
3. 从 target decode index 向前找最近 `is_keyframe=true`。
4. 若未找到 key sample，返回 null；不能把数组第 0 项强行标记为 key。
5. 验证 `[gopStartDecodeIndex, targetDecodeIndex]` 每个 sample：
   - `offset_in_chunk`、`size_bytes` 为安全整数；
   - offset ≥ 0、size > 0；
   - `offset + size <= chunkBytes.byteLength`；
   - `pts_ms`、`duration_ms` 为有限数。
6. 对每个 sample 用 `Uint8Array(chunkBytes, offset, size)` 构造
   `EncodedVideoChunk`，避免先 `slice()` 产生一次不必要副本；由原生 constructor 决定是否复制。
7. `type` 只读 sample key flag。
8. `timestamp = Math.round(pts_ms * 1000)`。
9. `duration_ms > 0` 时设置 `duration = Math.round(duration_ms * 1000)`。
10. base64 description 解成 `Uint8Array`，填入 `VideoDecoderConfig.description`。
11. config 使用 `codec/codedWidth/codedHeight`。
12. 返回目标 sample 的 timestamp，不把 target frameIndex 传作 output 下标。

注意：

- `atob` 失败或 description 空时返回 null，并记录 `description_unavailable`；
- 当前 chunk 是 MP4 AVCC/HVCC sample，缺 description 不尝试 Annex-B 猜测；
- 本版不在前端转换 NALU 格式；
- B 帧测试必须使用类似 decode order `[frame 10, frame 12, frame 11]` 的 fixture。

### 4.4 修正 decoder 核心

重构 `decodeChunkToBitmap`：

```ts
export async function decodePlanToBitmap(
  plan: EncodedVideoDecodePlan,
): Promise<DecodedVideoFrameBitmap | null>;
```

能力探测：

```ts
VideoDecoder;
EncodedVideoChunk;
createImageBitmap;
```

config 支持：

```ts
const support = await VideoDecoder.isConfigSupported(plan.config);
if (!support.supported) return null;
decoder.configure(support.config);
```

兼容边界：

- 如果实现提供 `VideoDecoder` 但缺少 `isConfigSupported`，本版允许走 configure try/catch；
- 该降级要进入诊断，不能假设 supported；
- `isSecureContext` 只作为诊断字段，不单独阻断，因为某些本地环境可能有实现差异。

output 策略：

```ts
let wanted: VideoFrame | null = null;

output(frame) {
  if (frame.timestamp === plan.targetTimestampUs && wanted === null) {
    wanted = frame;
  } else {
    frame.close();
  }
}
```

flush 后：

- 没有 wanted → null；
- `createImageBitmap(wanted)`；
- 返回 entry 的 `frameIndex = plan.targetFrameIndex`；
- finally 关闭 wanted 与 decoder；
- bitmap 成功交付后不在 finally 关闭，由缓存接管。

error callback：

- 保存首个 error；
- flush 后即使 promise 异常行为因浏览器不同，也按 error 状态返回 null；
- 不向 console 打完整 chunk、description 或二进制。

### 4.5 hook 缓存与 single-flight

`useVideoChunkDecoder` 保留现有外部职责，但调整方法：

```ts
decodePlan(plan): Promise<DecodedVideoFrameBitmap | null>
showFrame(frameIndex): DecodedVideoFrameBitmap | null
clear(): void
```

修改：

- `inFlightRef` 从 `Set<string>` 改为
  `Map<string, Promise<DecodedVideoFrameBitmap | null>>`；
- 相同 `taskId:frameIndex` 的并发请求 await 同一 promise；
- decode 成功只入缓存，不自动激活；
- 只有显式 `showFrame(frameIndex)` 修改 active pointer；
- `remember()` 不把 diagnostics.activeFrameIndex 错写为刚完成但未展示的 frame；
- task 变化后结果关闭 bitmap，不入新任务缓存；
- 保留 LRU 和卸载 close；
- `maxItems` 接入 `performanceConfig.videoDecoderCache`；
- diagnostics 增加：
  - `configUnsupported`
  - `staleResults`
  - `lastFallbackReason`
  - `lastDecodeMs`

v0.23.14 会把 item count 改为 byte budget，本版不提前混入。

### 4.6 新增 `useVideoPreciseFrame`

文件：

```text
apps/web/src/pages/Workbench/stage/useVideoPreciseFrame.ts
apps/web/src/pages/Workbench/stage/useVideoPreciseFrame.test.ts
```

输入：

```ts
interface UseVideoPreciseFrameArgs {
  taskId: string | null | undefined;
  frameIndex: number;
  enabled: boolean; // paused && !frameClock.isSeeking
  maxItems: number;
}
```

输出：

```ts
interface UseVideoPreciseFrameResult {
  active: boolean;
  bitmap: CachedVideoBitmap | null;
  sourceState:
    | "disabled"
    | "idle"
    | "chunk-pending"
    | "loading"
    | "decoding"
    | "ready"
    | "fallback";
  fallbackReason: PreciseFrameFallbackReason | null;
  diagnostics: VideoPreciseFrameDiagnostics;
}
```

query 流：

1. `useVideoChunkDecoder({ taskId, maxItems })` 先解析 flag 和浏览器能力。
2. decoder inactive 时所有后续 query `enabled=false`。
3. active 时请求 task manifest v2。
4. `targetChunkId = floor(frameIndex / chunk_size_frames)`。
5. `getChunk(datasetItemId, targetChunkId)`：
   - pending：按响应 `retry_after`，clamp 到 1–10 秒轮询；
   - ready：停止轮询；
   - failed：停止并 fallback；
   - query 不使用 `staleTime=Infinity` 持有 signed URL；
   - URL 过期导致 bytes 403 时 invalidate 后只重取一次 metadata。
6. ready 后请求 samples：
   - query key 为 dataset + chunk；
   - `staleTime=Infinity`；
   - 404 不重试，fallback `samples_unavailable`。
7. ready URL + samples 后请求 chunk bytes：
   - raw `fetch(url, { signal })`；
   - 检查 `response.ok`；
   - query key 使用 dataset + chunk，不使用带签名 URL；
   - `gcTime` 设短值，例如 60 秒；
   - v0.23.14 再升级为显式 byte LRU。
8. bytes + samples 就绪时 build plan 并 decode。
9. decode 完成后，仅当最新 ref 仍匹配 taskId + frameIndex 且 pipeline enabled，调用
   `decoder.showFrame(frameIndex)`。
10. 返回 bitmap 时再次验证 `bitmap.frameIndex === frameIndex`。

播放/seek 门：

- `enabled=false` 时不发新的 metadata/chunk/sample/bytes/decode；
- 已有 bitmap 可以留在 cache，但媒体层播放时不会使用；
- `frameClock.isSeeking=true` 时等待原生 seek 稳定，避免 scrub 每个中间值都启动 pipeline；
- 稳定后 effect 会因 `isSeeking` 变 false 自动启动当前目标。

### 4.7 接入播放控制器

在 `useVideoPlaybackController`：

1. 保留 `useVideoBitmapCache` 作为即时显示与 fallback。
2. 在 `frameClock` 和 `performanceConfig` 可用后调用：

   ```ts
   const precise = useVideoPreciseFrame({
     taskId: manifest?.task_id,
     frameIndex,
     enabled: !isPlaybackActive && !frameClock.isSeeking,
     maxItems: performanceConfig.videoDecoderCache,
   });
   ```

3. 选择：

   ```ts
   const preciseBitmap = precise.bitmap?.frameIndex === frameIndex ? precise.bitmap : null;
   const displayBitmap = preciseBitmap ?? activeBitmap;
   ```

4. `activeBitmapRef` 改为 `displayBitmapRef`。
5. `captureCurrentFrameJpeg` 先读 `displayBitmapRef.current`，再回退 `<video>`。
6. 返回 `frameSource`：

   ```ts
   preciseBitmap ? "webcodecs" : displayBitmap ? "video-bitmap" : "video-element";
   ```

7. 删除未使用的 `showCachedBitmap` result 字段。
8. 删除顶部未迁移 TODO。

native bitmap 仍在 seek 后捕获，不因 precise active 而关闭。这样精确帧冷启动期间仍能立即显示 fallback。

### 4.8 接入 Konva stage

`VideoKonvaStage`：

- 从 controller 解构 `displayBitmap`，不再把 `activeBitmap` 直接传给媒体层；
- `VideoKonvaMediaLayer.bitmap={displayBitmap?.bitmap ?? null}`；
- 可添加只读调试属性：

  ```tsx
  data-video-frame-source={controller.frameSource}
  ```

  仅用于 E2E 和诊断，不作为视觉 UI。

`VideoKonvaMediaLayer` 的 source 选择函数不改变：

- playing → `videoEl`；
- paused → bitmap → `videoEl`。

补测试证明 controller 给的是 precise bitmap，而 media layer 仍遵守播放优先级。

## 5. 死代码与文档注释收口

实施完成前运行：

```bash
rg -n "useVideoChunkDecoder|videoApi\\.getChunks|displayBitmap|showCachedBitmap|videoDecoderCache"
```

处理规则：

- `useVideoChunkDecoder` 必须有生产调用；
- `displayBitmap` 必须有 Konva consumer；
- `videoDecoderCache` 必须被传入 decoder；
- `showCachedBitmap` 无消费者则删除接口和计算；
- `videoApi.getChunks` / `VideoChunksResponse` 无消费者则删除前端 wrapper / 手写类型；
- 不删除后端列表 API；
- 删除 `useVideoChunkDecoder` 文件头“前端 manifest 尚未暴露”等过时 TODO；
- 设置 description 改为“实验性；暂停、逐帧和 seek 使用精确解码，刷新后生效”；
- 由 `pnpm docs:settings` 重生设置文档，不手改 generated 文件。

## 6. 改动文件清单

### 新增

```text
apps/web/src/pages/Workbench/stage/videoChunkDemux.ts
apps/web/src/pages/Workbench/stage/videoChunkDemux.test.ts
apps/web/src/pages/Workbench/stage/useVideoPreciseFrame.ts
apps/web/src/pages/Workbench/stage/useVideoPreciseFrame.test.ts
```

### 修改

```text
apps/web/src/types/index.ts
apps/web/src/api/tasks.ts
apps/web/src/api/videos.ts
apps/web/src/pages/Workbench/stage/useVideoChunkDecoder.ts
apps/web/src/pages/Workbench/stage/useVideoChunkDecoder.test.ts
apps/web/src/pages/Workbench/stage/useVideoPlaybackController.ts
apps/web/src/pages/Workbench/stage/useVideoPlaybackController.test.ts
apps/web/src/pages/Workbench/stage/VideoKonvaStage.tsx
apps/web/src/pages/Workbench/stage/VideoKonvaStage.konva.test.tsx
apps/web/src/pages/Workbench/stage/VideoKonvaMediaLayer.konva.test.tsx
apps/web/src/pages/Workbench/state/workbenchSettingsFields.ts
apps/web/src/pages/Workbench/state/workbenchSettingsFields.test.ts
apps/api/tests/test_video_frame_service.py
CHANGELOG.md
docs-site/user-guide/workbench/settings.md
docs-site/user-guide/workbench/settings.generated.md   # generated
```

API schema 不变，因此不应产生 OpenAPI diff；若 pre-commit 产生无关 snapshot diff，应查明原因，不直接接受。

## 7. 测试矩阵

### 7.1 `videoChunkDemux.test.ts`

| 场景                           | 断言                                                      |
| ------------------------------ | --------------------------------------------------------- |
| 普通 I/P GOP                   | 从最近 key 到 target；timestamp/duration 为微秒           |
| target 即 key                  | 只构造一个 key chunk                                      |
| B 帧 decode order `[10,12,11]` | target 11/12 得到各自 timestamp，不按数组展示顺序重排输入 |
| 两个 GOP                       | target 只回溯到同 GOP 最近 key                            |
| target 不存在                  | null + `invalid_sample_range`                             |
| GOP 前无 key                   | null，不伪造 key                                          |
| offset 负数/小数/越界          | null                                                      |
| size 0/越界                    | null                                                      |
| description base64             | config.description 字节一致                               |
| description 非法/缺失          | null                                                      |
| duration 0                     | chunk init 不带 duration                                  |
| coded size 非正数              | null                                                      |

### 7.2 decoder 单测

- output 顺序与输入 timestamp 顺序不同，仍命中 target timestamp；
- target 为第二个 output，非目标 frame 立即 close；
- 没有 target timestamp → null；
- `isConfigSupported=false` 不 configure/decode；
- configure/decode/flush/error callback/createImageBitmap 各自失败；
- wanted frame、其它 frame、decoder 各 close 一次；
- 成功 bitmap 不被 decoder finally close；
- 相同 frame 并发只调用一次底层 decode；
- task 切换后旧 bitmap close 且不入新缓存。

### 7.3 precise hook

MSW / mock fetch 覆盖：

- flag off：manifest/chunk/samples/bytes 全部 0 请求；
- manifest v2 缺 dataset / chunk size 非法；
- chunk pending 两次后 ready；
- pending polling 使用 retry_after；
- chunk failed；
- samples 404 不重试；
- bytes CORS/network/500；
- signed URL 403 → metadata refresh 一次 → 成功；
- unsupported config fallback；
- frame 5 慢、frame 6 快：只 frame 6 激活；
- frame 5 → 6 → 5，同 key single-flight 最终可命中 frame 5；
- `enabled=false`（playing/seeking）停止新请求；
- 卸载 abort fetch；
- maxItems 透传。

### 7.4 controller / Konva

- paused + precise frame match：`displayBitmap=precise`、source=webcodecs；
- precise frame 不匹配：使用 native bitmap；
- precise null：使用 native；
- 两者 null：media layer 使用 video element；
- playing：即使 bitmap 存在，`pickMediaImageSource` 选 video；
- `captureCurrentFrameJpeg` 使用 precise bitmap；
- Stage 传 `displayBitmap` 而不是 native `activeBitmap`；
- `data-video-frame-source` 跟随 source。

### 7.5 后端 API

补当前缺口：

- ready chunk samples 200，字段含 description；
- diagnostics 无 samples → 404；
- 不可见 dataset item → 404；
- response samples 保持存储顺序。

## 8. 实施顺序

### P0 · 纯 demux 与 decoder 正确性

1. 新增 decode plan 类型和纯构造函数。
2. 覆盖普通 GOP、B 帧和无效字节。
3. 改 decoder 按 timestamp 选择。
4. 加 config support、single-flight 和资源测试。

退出门：相关 Vitest 全绿；不得先接 UI 再用浏览器手工猜正确性。

### P1 · API 与 precise hook

1. 增 manifest v2 / getChunk client。
2. 实现 query 状态、poll、abort 和 fallback reason。
3. 补快速 seek / pending / URL 过期测试。

退出门：flag off 零请求；pending 能变 ready；旧请求不能激活。

### P2 · Controller 与 Konva

1. 接 controller。
2. 统一 `displayBitmap` / JPEG。
3. Stage 改读 `displayBitmap`。
4. 清死返回字段和 TODO。

退出门：组件测试证明暂停 precise 优先、播放 video 优先。

### P3 · 合同测试与文档

1. 补 samples API 测试。
2. 更新设置源、生成设置页。
3. 写 CHANGELOG Unreleased。
4. 定向检查死代码。

退出门：无 API schema 意外漂移；无未消费的新增类型/方法。

## 9. 验证命令

优先使用现有虚拟环境：

```bash
pnpm --filter @anno/web test -- \
  src/pages/Workbench/stage/videoChunkDemux.test.ts \
  src/pages/Workbench/stage/useVideoChunkDecoder.test.ts \
  src/pages/Workbench/stage/useVideoPreciseFrame.test.ts \
  src/pages/Workbench/stage/useVideoPlaybackController.test.ts \
  src/pages/Workbench/stage/VideoKonvaMediaLayer.konva.test.tsx \
  src/pages/Workbench/stage/VideoKonvaStage.konva.test.tsx

pnpm --filter @anno/web typecheck
pnpm --filter @anno/web lint

cd apps/api
PYTHONDONTWRITEBYTECODE=1 uv run pytest \
  -p no:cacheprovider \
  tests/test_media_chunk_samples.py \
  tests/test_video_frame_service.py
```

随后运行：

```bash
pnpm test
pnpm typecheck
pnpm lint
pnpm docs:settings
pnpm docs:build
```

测试产生的 coverage、trace、临时视频、pytest cache 和临时数据库行必须清理。生成后受版本控制的
`settings.generated.md` 保留。

## 10. 退出门

- [x] flag off 零新增请求。
- [x] flag on + paused 能请求 manifest/chunk/samples/bytes。
- [x] B 帧目标按 timestamp 命中。
- [x] pending 自动轮询至 ready。
- [x] unsupported / missing / malformed / fetch failure 全部 fallback。
- [x] 快速 seek 无 stale bitmap 激活。
- [x] precise bitmap 进入 Konva。
- [x] 当前帧 JPEG 与画布同源。
- [x] 连续播放仍使用 `<video>`。
- [x] decoder cache 消费 performance tier。
- [x] `showCachedBitmap` 等死字段清理。
- [x] 设置文案不再承诺一个无效功能。
- [x] 定向测试、全量 Web 测试、typecheck、lint、docs build 全绿。
- [x] 测试中间产物已清理。

## 11. 风险与回滚

- 最小回滚是关闭 localStorage flag；native 路径保持原状。
- 本版不改变 annotation 或持久数据，回滚无数据迁移。
- 若 manifest v2 query 出现权限或兼容问题，precise hook fallback；不能切换主工作台 manifest。
- 若某 codec `isConfigSupported=true` 但 decode 失败，记录 `decode_failed`，不重复无限重试。
- 若 ImageBitmap 创建造成资源压力，本版由现有 item LRU 止损，v0.23.14 必须完成 byte budget。
- 若本版发现 sample timestamp / frame_index 后端合同错误，暂停 UI 接线，先用最小后端测试复现；
  不在前端猜测纠正。

## 12. 文档影响

本版只更新用户设置说明和 CHANGELOG，开发架构、性能与运维文档集中在 v0.23.15 基于最终 pipeline 一次
写准，避免 v0.23.13 文档刚描述无状态 decoder、v0.23.14 又立即过时。

用户设置正文不显示版本号，写当前行为：

- 本机实验开关；
- 刷新生效；
- 只影响暂停、逐帧和 seek；
- 连续播放仍由浏览器播放器负责；
- 不支持时自动回退。

## 13. Outcome 回填要求

实施完成后追加：

```markdown
## Outcome

- 落地版本：v0.23.13（commit `...`）
- demux / timestamp 合同：...
- Konva source 合同：...
- API / Vitest / typecheck / lint 证据：...
- 用户设置文档：...
- CHANGELOG：Unreleased 已添加
- 已删除死代码：...
- 未尽事项：移交 v0.23.14
```

## Outcome

- 落地版本：v0.23.13（工作树变更就绪，未提交；用户未要求 commit）
- demux / timestamp 合同：`buildEncodedVideoDecodePlan` 返回 `{ ok, plan | reason }`，按后端 packet decode order 定位目标帧、回溯最近关键帧、字节边界校验；目标帧用 `targetTimestampUs = round(pts_ms*1000)` 匹配 `VideoFrame.timestamp`，不把全局 frameIndex 当 output 下标。B 帧 decode order `[10,12,11]` 由 `videoChunkDemux.test.ts` 覆盖。
- Konva source 合同：`useVideoPlaybackController` 只接受与当前 `frameIndex` 匹配的 bitmap，暂停态按 precise → native → `<video>` 选择；播放态强制使用 `<video>`。`VideoKonvaStage` 将同一 `displayBitmap` 交给 `VideoKonvaMediaLayer` 与 Minimap，`captureCurrentFrameJpeg` 读取同一 source ref；`data-video-frame-source` 暴露当前来源供 E2E/诊断。
- 查询与生命周期合同：manifest/chunk/samples 均静默请求并消费 AbortSignal；`retry_after` 按秒换算为毫秒；chunk bytes 以稳定资源键缓存。signed URL 403 会等待 metadata 新 URL 后只重取一次，第二次失败即 fallback；task 切换和卸载会阻止旧 decode 写回，并关闭迟到的 ImageBitmap。
- decoder 合同：通过静态 `VideoDecoder.isConfigSupported()` 探测 config，保留 `codec_unsupported` / `decode_failed` / `stale_request` 的真实原因；同帧并发 decode 共享 promise，成功结果由 LRU 接管，所有未交付的 VideoFrame / ImageBitmap 均显式释放。
- API / Vitest / typecheck / lint 证据：修复后定向 Vitest 74 passed（demux 15 / decoder 25 / precise 16 / controller 9 / media-layer 6 / stage 3）；全量 `pnpm --filter @anno/web test` 为 324 files / 2681 passed；`pnpm --filter @anno/web typecheck` 0 error；根级 `pnpm lint` 通过（Python 0 error，Web 0 error，12 个既有 warning）；`test_video_frame_service.py` 22 passed；`format:check`、`docs:settings`、`docs:build` 通过。
- 用户设置文档：`settings.md` 正文 + `settings.generated.md`（由源重生）均描述当前行为（暂停/逐帧/seek 精确解码、连续播放仍走 `<video>`、不支持自动回退），无版本号。
- CHANGELOG：`## [Unreleased]` 新增 `### Added` 条目。
- 已删除死代码：`useVideoPlaybackController.showCachedBitmap`（字段+计算）、`videoApi.getChunks`、手写 `VideoChunksResponse` 类型、控制器顶部 WebCodecs TODO、`useVideoChunkDecoder` 文件头过时 TODO；`useVideoChunkDecoder` 恢复生产调用（`useVideoPreciseFrame`），`videoDecoderCache` 接入 decoder。
- 未尽事项：移交 v0.23.14（持久 GOP decoder session、向前增量 decode、bitmap / chunk bytes 字节预算、显式 chunk/sample 预取）；v0.23.15（真实 B 帧 Chromium E2E、故障矩阵、性能门、Worker 决策、开发/运维文档）。
