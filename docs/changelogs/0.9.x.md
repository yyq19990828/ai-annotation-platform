# 0.9.x — Grounded-SAM-2 接入（首版 AI 基座）

按 ROADMAP `[0.9.x.md](../../ROADMAP/0.9.x.md)` 五阶段切片：M0 backend 容器化 → M1 embedding 缓存 → M2 工作台 `S` 工具 + 文本入口 → M3 mask→polygon 调参 → M4 `/ai-pre` 文本批量 UI → M5 运维收口。

---

## [0.9.3 phase 3] - 2026-05-07 · 前端接通 ML Backend 注册能力（Happy Meadow）

主项详情见 [`CHANGELOG.md` → 0.9.3 phase 3](../../CHANGELOG.md)。要点速览：

- **新「ML 模型」选项卡**：`ProjectSettingsPage` 加 `MlBackendsSection`（icon `bot`，列在「批次管理」之后），承载本项目作用域 backend 的 list / create / edit / delete / health；写操作按 `role` 门控。
- **共用 Modal**：`MlBackendFormModal` 同时驱动注册 + 编辑两条路径，字段 `name / url / is_interactive / auth_method / auth_token / extra_params`；编辑模式 `auth_token` 留空保持原值；JSON 解析错误阻断提交。
- **`/model-market` RegisteredBackendsTab 由只读 → 可写**：每个 ProjectGroup header 加「+ 注册」、行尾加「健康检查 / 编辑 / 删除」三按钮；与项目设置 tab 共用同一组 hooks + Modal。
- **`useMLBackends.ts` 补齐 useUpdate / useDelete + 双 invalidate**：四个写 mutation 的 `onSuccess` 同时刷新 `["ml-backends", projectId]` + `["admin", "ml-integrations", "overview"]`，跨入口写后视图同步。
- **过时文案变成有效引导**：`GeneralSection.tsx:301` + `CreateProjectWizard.tsx:586` 自此真实存在 ML 模型 tab 可点。
- **测试**：tsc 0 errors / vitest 346 pass。后端无改动。

详细计划：[`docs/plans/2026-05-07-v0.9.3-phase3-ml-backend-registration.md`](../../docs/plans/2026-05-07-v0.9.3-phase3-ml-backend-registration.md)。

---

## [0.9.3 phase 2] - 2026-05-07 · 三页合二，激活模型市场（Merged Market）

主项详情见 [`CHANGELOG.md` → 0.9.3 phase 2](../../CHANGELOG.md)。要点速览：

- **新 `/model-market` 页**（替占位）：Tabs 容器 + URL `?tab=backends|failed` 同步；默认 backends。
- **Tab 1 已接入 Backend**：搬自旧 `MLIntegrationsPage`，去掉与 `/storage` 重复的 Bucket / 对象 StatCard，仅保留「ML Backend / 使用项目」两张 + 项目级 Backend 分组卡片（60s refetch）。
- **Tab 2 失败预测**：搬自旧 `FailedPredictionsPage`，完整保留分页 / dismissed 切换 / retry-dismiss-restore 三种 mutation。
- **Sidebar 动态徽章**：「模型市场」项失败预测 > 0 时显红色「N 失败」徽章（`ml-backend.manage` 权限门控查询，普通用户零请求）；同时摘掉 `ai-pre` 的 mock「3 运行中」徽章。
- **删除**：路由 `/admin/ml-integrations` + `/admin/failed-predictions`；`MLIntegrationsPage.tsx` + `FailedPredictionsPage.tsx` + 对应 test；PageKey `admin-ml-integrations` / `admin-failed-predictions`。
- **入口跳转重定向**：AdminDashboard 失败预测卡片 → `/model-market?tab=failed`；集成总览按钮 → `/model-market`。
- **`/storage` 完全未动**：权限面广，不适合承载管理视角内容。
- **测试**：tsc 0 errors / vitest 346 pass。后端 API 完全兼容（`adminMlIntegrationsApi.overview` / `failed-predictions` 系列不动）。

详细计划：[`docs/plans/2026-05-07-v0.9.3-phase2-merged-market.md`](../../docs/plans/2026-05-07-v0.9.3-phase2-merged-market.md)。

---

## [0.9.3] - 2026-05-07 · 前端杂项收口（Refactored Lighthouse）

主项详情见 [`CHANGELOG.md` → 0.9.3 phase 1](../../CHANGELOG.md)。要点速览：

- **API 密钥**：完整后端（`api_keys` 表 + `/me/api-keys` CRUD + `ak_` token 走 `get_current_user`）+ 前端 `ApiKeysModal`（list / create 一次性显明文 / revoke）。
- **超管 ML 集成总览**：新页 `/admin/ml-integrations` 只读聚合 storage health + 跨项目 ml_backends（仅 super_admin）。**注**：phase 2 已将该路由合入 `/model-market`，本条仅为 phase 1 当时落地状态记录。
- **登录 progressive CAPTCHA**：同 IP 失败 ≥ 5 次后强制 Turnstile，401 加 `X-Login-Failed-Count` header；正常用户零打扰；dev 短路。
- **IoU rbush 索引**：同类分桶 RBush + 候选裁剪 + iouShape 精确判定 + some() 早退保留；千框场景预热。
- **DropdownMenu 收编**：通用组件加 `content` 自定义槽，把 ExportSection / NotificationsPopover 收到统一浮层骨架。
- **测试**：后端新增 9 case（api_keys / admin_ml_integrations / progressive_captcha），前端新增 6 case（iou-index / DropdownMenu content）。

详细计划：[`docs/plans/2026-05-07-v0.9.3-phase1-refactored-lighthouse.md`](../../docs/plans/2026-05-07-v0.9.3-phase1-refactored-lighthouse.md)。

---

## [0.9.2] - 2026-05-07 · M2 工作台 `S` 工具 + 文本入口（Luminous Canvas）

主项详情见 [`CHANGELOG.md` → 0.9.2](../../CHANGELOG.md)。要点速览：

- 工作台新增 `S` 工具（点 / Alt+点 / 拖框）+ AI 助手 SAM 文本提示输入框；候选 polygon 紫虚线叠加 Konva，`Enter` / `Esc` / `Tab` 接受 / 取消 / 切换。
- `useInteractiveAI` 集中路由 + 80ms 防抖 + inflight 计数防过期请求覆盖。
- ProjectSettings 加 DINO `box_threshold` / `text_threshold` 项目级 override（默认 0.35 / 0.25），`/predict` 协议透传。
- 一致的紫色虚线视觉语言（拖框预览 + 候选 polygon + 视觉同源），与 BboxTool 工具流完全隔离（独立 `samProbe` DragInit）。
- 测试：vitest 349 / pytest 277 全过；新增 `useInteractiveAI` 10 case + interactive 阈值注入 3 case。E2E 推迟到下版本（需要 `/_test_seed` ML backend 工厂）。

详细计划：[`docs/plans/2026-05-07-v0.9.2-luminous-canvas.md`](../../docs/plans/2026-05-07-v0.9.2-luminous-canvas.md)。

---

## [0.9.1] - 2026-05-07 · M1 SAM 2 image embedding LRU 缓存

详情见 [`CHANGELOG.md` → 0.9.1](../../CHANGELOG.md)。要点：

- LRU 缓存 + Prometheus 4 metric + `/cache/stats`；同图二次操作从 ~1.6s 降到 < 50ms（4060 / tiny）。
- 三种 prompt 路径全部接缓存；point/bbox 命中跳过 `_fetch_image()`，text 仍需原图给 DINO。
- 协议契约 / 平台 API 零改动；缓存对 `apps/api` 透明。

---

## [0.9.0] - 2026-05-07 · M0 grounded-sam2-backend 容器化

详情见 [`CHANGELOG.md` → 0.9.0](../../CHANGELOG.md)。要点：

- 新服务 `apps/grounded-sam2-backend/`，FastAPI + PyTorch 2.3 + CUDA 12.1，4 协议端点 + 三种 prompt 同端点分流。
- vendor IDEA-Research/Grounded-SAM-2 commit `b7a9c29`，`scripts/sync_vendor.sh` 一键同步；docker-compose `profiles: ["gpu"]`。
- ProjectSettings 已能测试连接（v0.8.6 F3）；checkpoints 启动时 hf_hub_download 拉到 volume `gsam2_checkpoints`。
