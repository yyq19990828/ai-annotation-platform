---
title: 高清营销资产目录
description: 营销录屏的主题、分镜、时长规格与 Agent 后期制作约定
audience: [developer]
type: reference
status: stable
last_reviewed: 2026-08-16
---

# 高清营销资产目录

高清营销资产是可追溯的后期母版，不是一条已完成的广告。每个资产 ID
只表达一个明确目标，使用独立 Playwright 测试和独立原始视频归档。不得把
多个无关功能为了凑时长录到同一母版。

规格的代码真值源是
`apps/web/e2e/screenshots/_helpers/marketing-assets.ts`。归档器会把对应的主题、
目标、分镜和剪辑提示复制到运行 `manifest.json`。

## 时长与归档门禁

每条规格同时声明三个时长：

- **最短**：归档硬门禁。低于该时长表明操作过快或镜头不完整，录制直接失败。
- **目标**：原始母版的建议节奏，也是后期 Agent 设计主版时的参考。
- **最长**：归档硬门禁。超过上限通常表明推理、加载或自动化已卡住。

母版还必须是 **4K UHD（3840×2160）/ 60fps**。工作台按 1440×810 逻辑 viewport
和 1.8 设备像素倍率渲染为 2592×1458，使控件、文字和画布主体保持文档录制时的视觉比例；
X11 主动采样保留真实 60Hz 动作，并由 NVENC 硬件编码，再以 Lanczos 生成 3840×2160 交付母版。录制开始前会用校准色块测出
Chromium 内容区的真实 X11 边界；只有边界恰好为 2592×1458、没有裁掉顶部或引入黑边时才启动正式流程。
分辨率、帧率、有效帧节奏或时长不符合规格时，
不写入资产库。

## 资产明细

| 资产 ID                                | 主题与详细画面                                                                                         | 时长（最短 / 目标 / 最长） | 后期用途                           |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------------------------- | ---------------------------------- |
| `ai-prediction-import`                 | 加载道路图像 → 显示与车辆对齐的导入候选 → 查看候选详情 → 采纳 → 展示人工标注已落库                     | 8 / 10 / 20 秒             | AI 预标注导入结果；不含人工画框    |
| `ai-preannotate`                       | 选择范围和模型 → 发起作业 → 等待完成 → 查看 Job 详情 → 返回工作台核对候选                              | 8 / 12 / 25 秒             | AI 批处理、项目管理                |
| `sam-tools/smart-point`                | 启用智能点 → 在真实轿车上放置正点 → 展示轮廓 → 选择 `car` → 展示已保存结果                             | 7 / 10 / 25 秒             | AI 交互工具单项介绍                |
| `sam-tools/smart-box`                  | 启用智能框 → 沿真实轿车拖出提示框 → 展示轮廓 → 选择 `car` → 展示已保存结果                             | 7 / 10 / 25 秒             | AI 交互工具单项介绍                |
| `sam-tools/exemplar`                   | 框选真实轿车作为正示例 → 展示相似目标候选 → 选择 `car` → 展示已保存结果                                | 8 / 12 / 30 秒             | 相似目标检索与批量标注             |
| `smart-scribble`                       | 展示原 Mask → 添加有意义的正负笔迹 → 展示精修候选 → 选择 `car` 并保存更新后边界                        | 8 / 12 / 30 秒             | Mask 精修、前后对比                |
| `ai-assisted-annotation`               | 沿真实车辆拖出 Magic Box → 展示推理和候选 → 选择正确类别 → 确认为正式标注                              | 8 / 12 / 30 秒             | AI 辅助标注主视觉                  |
| `review-reject`                        | 批次卡片进度 → 进入待审任务 → 最终 / diff 核对 → 结构化退回 → 返回确认待审数减少                       | 20 / 28 / 40 秒            | 质量闭环、审核角色、批次进度       |
| `batch-bulk-actions`                   | 进入批次列表 → 勾选多任务 → 执行批量动作 → 展示结果                                                    | 7 / 10 / 20 秒             | 批次管理、效率展示                 |
| `ai-pre-variant-selector`              | 打开模型选择 → 切换模型 → 观察变体/档位联动                                                            | 7 / 10 / 20 秒             | 多模型、推理档位配置               |
| `ocr-real-scene`                       | 展示原图 → 发起 RapidOCR → 运行状态 → 文字框与识别结果                                                 | 10 / 15 / 90 秒            | OCR 能力介绍、真实推理             |
| `current-task-image-inference`         | 已加载 OCR 图片 → 运行项目编排 → 查看文字候选、识别结果与置信度 → 采纳一个候选 → 展示正式标注          | 15 / 19 / 90 秒            | 当前题 AI 完整人工接管链路         |
| `current-frame-video-inference`        | 定位车辆清晰帧 → 运行专用车辆检测 → 查看帧级候选与置信度 → 采纳中间卡车 → 切换相邻帧并返回核对         | 16 / 22 / 90 秒            | 视频当前帧 AI 与单帧作用域         |
| `secondary-inference-attribute`        | 选中准确文字区域 → 运行裁剪 OCR → 展示文本/方向/语言的 AI 来源 → 人工修正文案并保留混合溯源            | 14 / 20 / 90 秒            | 二次推理属性写回与人工接管         |
| `rotated-bbox`                         | 沿真实轿车绘制旋转框 → 选择 `car` 并落库 → 展示手柄 → 调整角度并保存                                   | 8 / 11 / 22 秒             | 方向目标标注                       |
| `bbox-draw`                            | 无人工框的道路图 → 隐藏 AI 层 → 启用矩形工具 → 沿真实轿车拖框 → 选择 `car` → 展示已保存人工框          | 4 / 4.5 / 8 秒             | 人工车辆标注；不含 AI 导入或提交   |
| `polyline-draw`                        | 沿真实车道线逐点放置 → 完成几何 → 选择正确类别 → 展示已保存折线                                        | 7 / 10 / 20 秒             | 道路、边缘、线性结构               |
| `polygon-draw`                         | 沿真实轿车轮廓逐点勾勒 → 闭合图形 → 选择 `car` → 展示已保存填充                                        | 7 / 10 / 20 秒             | 不规则目标轮廓                     |
| `mask-draw`                            | 在真实轿车区域多次涂抹 → 完成 Mask → 选择 `car` → 展示已保存半透明区域                                 | 7 / 10 / 22 秒             | 像素级手工标注                     |
| `candidate-keyboard-review`            | 展示多个 AI 候选 → 键盘采纳/驳回 → 焦点自动前进                                                        | 10 / 14 / 30 秒            | 高速 AI 候选审阅                   |
| `candidate-review-lifecycle`           | 三个车辆候选 → 跳过一项 → 采纳一项 → 自动前进并驳回一项 → 最终计数                                     | 16 / 22 / 28 秒            | AI 候选完整审阅生命周期            |
| `video-track`                          | 视频画布与时间轴 → 播放/逐帧移动 → 展示轨迹跨帧延续                                                    | 10 / 15 / 30 秒            | 视频标注工作台概览                 |
| `video-track-batch-propagate`          | 多选两条真实公交车轨迹 → 一次批量 AI 延展 → 跨帧复核双目标候选 → 整批回填原轨迹                        | 18 / 28 / 45 秒            | 多目标批量追踪与候选审阅           |
| `video-propagate-track-vs-copy`        | 同一中间卡车轨迹纯几何复制到 F30 → 核对固定框漂移 → 撤销 → 真实 SAM3 延展 → 跨帧审阅并回填             | 18 / 28 / 36 秒            | 几何复制与 AI 追踪适用边界         |
| `video-timeline-zoom`                  | 展示初始范围 → 以指针为锨点缩放 → 复位全局                                                             | 8 / 12 / 25 秒             | 长视频时间轴导航                   |
| `video-timeline-prediction-navigation` | 已生成多帧车辆候选 → 展开 violet AI 预测密度轨 → 连续前后跳转预测帧 → 核对画布候选与帧号               | 12 / 17 / 24 秒            | AI 预测密度与逐帧审阅导航          |
| `video-chapter`                        | 拖出章节 → 悬停显示手柄 → 调整起止边界                                                                 | 10 / 15 / 30 秒            | 章节创建与范围修改；可派生两条短片 |
| `video-tracker-range`                  | 为真实公交车设置 `bus` 和点种子 → Shift 刷选范围 → 发起追踪 → 审阅并采纳候选                           | 12 / 18 / 90 秒            | AI 追踪范围设置                    |
| `video-tracker-cross-frame-points`     | 为左右公交车分别设置 F0/F4 正点 → 同时追踪两个目标 → 拖动时间轴核对跨帧候选框 → 整批采纳               | 12 / 18 / 90 秒            | 双目标跨帧多正点追踪               |
| `video-tracker-positive-negative`      | 为左右公交车分别设置跨帧正点和紧邻车身的红色负点 → 同时追踪 → 拖动时间轴核对 → 整批采纳                | 13 / 19 / 90 秒            | 双目标正负点修正                   |
| `video-tracker-box-seed`               | 沿左右公交车完整车身分别绘制 F0 框 → 同时追踪 → 拖动时间轴核对跨帧候选框 → 整批采纳                    | 10 / 15 / 90 秒            | 双目标整车框种子追踪               |
| `video-tracker-text-discovery`         | 输入 `bus` → 筛选左右两辆完整公交车 → 跨帧核对 → 采纳两条轨迹并拒绝剩余噪声候选                        | 18 / 24 / 30 秒            | 文本驱动多目标发现追踪             |
| `video-tracker-combo-discovery`        | 输入 `bus` → 文本发现铸造内部种子 → 逐对象记忆追踪 → 跨窗口核对 → 采纳两条稳定轨迹                     | 30 / 42 / 50 秒            | 文本发现与 PVS 组合追踪            |
| `video-mask-correction-propagate`      | 查看 F5 漂移边界 → 笔刷补入、橡皮扣除 → 原生 Mask seed 向后重传播 → 跨帧核对并更新原轨迹               | 20 / 28 / 36 秒            | 视频 Mask 错帧纠正与重传播         |
| `pipeline-template-create`             | 命名公共模板 → YOLO 车辆检测 → 添加车辆属性分类 → 选择车型/颜色写回键 → 保存并进入编排库               | 18 / 24 / 30 秒            | 创建公共 AI 编排模板               |
| `pipeline-apply-project`               | 选择公共两阶段模板 → 套用为项目默认 → 工作台按项目编排运行 → 查看车型与颜色候选                        | 18 / 28 / 90 秒            | 套用并执行公共 AI 编排             |
| `jobs-retry-recovery`                  | 打开 RapidOCR 失败作业 → 核对错误摘要 → 真实重试 → 确认新作业完成 → 进入工作台审阅候选                 | 16 / 24 / 45 秒            | AI 作业失败恢复与结果审阅          |
| `model-market-runtime-pool`            | 核对运行时摘要与数据来源 → 展开车辆检测服务池 → 比较双实例路由状态/并发/GPU → 切换详情 Sheet           | 15 / 22 / 30 秒            | 模型服务池运行时观测               |
| `model-market-video-pool`              | 定位视频追踪服务池 → 展开 SAM 3 实例 → 核对独立视频池容量/会话/GPU → 展开视频权重与预热入口            | 12 / 18 / 28 秒            | 视频 tracker 独立显存池观测        |
| `model-market-runtime-partial-failure` | 4/5 来源新鲜 → 展开部分失败告警 → 悬停 fresh 拓扑 → 悬停 stale 路由账本核对超时、更新时间与退避        | 9 / 12 / 20 秒             | 运行时数据可信度与部分失败         |
| `model-market-gpu-resource-overview`   | 切换 GPU 资源 → 核对 runtime ready / 全局 enforce → 对比静态预算与 committed → 展开 blocker 卡反查实例 | 10 / 14 / 22 秒            | GPU 资源就绪性、预算与阻断影响     |
| `platform-overview`                    | 平台 KPI / 分布 → 30 天注册来源 → ML 后端与成本 → 近期审计 → 图片 / 视频 / 点云项目                    | 16 / 22 / 30 秒            | 超级管理员全局运营概览             |
| `project-actions-menu`                 | 项目行 → 更多操作 → 依次定位导出标注、复制配置、导入预测三个高频入口                                   | 9 / 12 / 20 秒             | 项目数据流与配置复用入口           |
| `jobs-bell-active`                     | 顶栏角标 → 展开预标 / 导入 / 导出混排 → 核对进度与取消 → 定位 ZIP 摘要和下载                           | 10 / 14 / 22 秒            | 全局异步任务进度与后续动作         |
| `video-tracker-job-states`             | 四状态同屏 → 选择视频项目 → 筛选待审阅 → 核对帧范围与方向 → 返回对应视频工作台                         | 12 / 17 / 26 秒            | 视频追踪执行态与候选审阅态         |
| `project-ml-routing`                   | 启用 YOLO → 设为批量主后端 → 工作台当前题 AI 命中 YOLO → Smart Point 自动命中 SAM3                     | 18 / 25 / 32 秒            | 项目多后端启用与能力自动分流       |
| `background-export-download`           | 选择 COCO + AAP JSON → 导出入队 → 任务铃展示运行状态 → 核对 ZIP 产物并下载                             | 16 / 24 / 32 秒            | 异步多格式导出与真实产物校验       |
| `project-create-existing-resources`    | 新建图片项目 → 添加 `car` 类别 → 关联已有道路数据集 → 选择现有标注员和审核员 → 核对创建摘要            | 14 / 19 / 32 秒            | 项目创建、数据与团队资源复用       |
| `video-track-carryover`                | 展示当前框 → 切帧后出现虚影 → 续写新框 → Tab 前进                                                      | 9 / 13 / 28 秒             | 视频标注效率技巧                   |
| `video-mask-track-edit`                | 首帧创建 Mask 轨迹 → 切换后续帧 → 修正同一轨迹                                                         | 10 / 16 / 35 秒            | 视频分割与轨迹编辑                 |
| `ai-tracker-panel`                     | 打开 AI 追踪面板 → 拖动 → 缩放 → 展示与画布的互斥                                                      | 8 / 12 / 25 秒             | AI 追踪操作面板                    |
| `pointcloud-controls`                  | 展示初始点云 → 切换上色 → 调整点大小 → 调整深度范围                                                    | 10 / 15 / 40 秒            | 3D 显示控件                        |
| `pointcloud-view`                      | 展示初始视角 → 平滑拖动旋转 → 在新视角稳定停留                                                         | 10 / 15 / 40 秒            | 3D 视图导航                        |
| `pointcloud-camera-seed-3d-box`        | 放大同步相机图 → 圈定前景物体 → 视锥选点拟合 3D 框 → 主点云与三视图核对                                | 16 / 24 / 36 秒            | 相机图辅助 3D 种框                 |
| `pointcloud-crossframe-track`          | 已复核 3D 框 → 延续到第 1 帧 → 修正位置 → 延续到第 2 帧 → 逐帧回看同轨迹邻帧参考框                     | 22 / 30 / 40 秒            | 3D 跨帧延续、轨迹身份与人工修正    |
| `pointcloud-billboard-label`           | 已复核 3D 框 → 打开标签内容的轨迹段 → 启用属性 → 核对完整标签 → 环绕多个视角确认文字持续正对相机       | 14 / 20 / 26 秒            | 3D 框标签配置与多角度可读性        |
| `video-draw`                           | 起始帧创建框 → 后续帧调整 → 来回逐帧检查插值                                                           | 11 / 17 / 35 秒            | 关键帧轨迹与插值                   |
| `large-image-progressive`              | 展示大图全景 → 缩放到局部 → 渐进加载 → 高清切片到位                                                    | 10 / 15 / 40 秒            | 超大图处理与性能                   |
| `large-image-pyramid-recovery`         | 进入局部高清 → 单切片请求失败 → 保留 overview 并自动重试 → 恢复后继续缩放平移                          | 16 / 22 / 28 秒            | 超大图故障恢复                     |
| `large-image-mask-limit`               | Cosmic Cliffs 局部放大 → 落库“目标”矩形框 → 悬停置灰 Mask 入口核对实际尺寸与两类上限                   | 10 / 14 / 24 秒            | 超大图标注能力边界                 |
| `hotkey-cheatsheet`                    | 按 `?` 打开面板 → 展示分组 → 搜索命令 → 关闭面板                                                       | 8 / 12 / 25 秒             | 标注效率、快捷键教程               |

## Google Drive 同步约定

尚未进入代码规格的后续母版维护在
`docs-site/maintainers/marketing-recording-backlog.md`。只有开始实现独立 flow 时，才把对应项目迁入本页和
`marketing-assets.ts`，避免目录把“计划”误报为“已可录制”。

本地 `.artifacts/marketing/<run_id>/` 是一次录制的不可变批次；Google Drive 的日常工作目录固定为
`AAP资产:营销素材/高清母版/current/`。更新素材时，把最新完整批次镜像到 `current/`，不要为每次运行创建新的
时间戳目录，否则相同主题会在云端反复占用空间。

```bash
rclone sync ".artifacts/marketing/<run_id>" \
  "AAP资产:营销素材/高清母版/current" \
  --checkers 8 --transfers 4

rclone check ".artifacts/marketing/<run_id>" \
  "AAP资产:营销素材/高清母版/current" \
  --one-way --checkers 8
```

`sync` 会删除目标中源端已不存在的文件，因此只允许把目标精确限定到上述 `current/`；不得对
`AAP资产:`、`营销素材/` 或 `高清母版/` 上层目录执行。需要保留里程碑时，才在人工确认后额外复制到
`AAP资产:营销素材/高清母版/archive/<run_id>/`。Rclone 凭证只保存在操作者环境中，不写入仓库或
`manifest.json`。

## 后期 Agent 交接规则

1. 先读取运行 `manifest.json` 中的 `content`，不要只根据文件名猜测素材内容。
2. 每个 `asset_id` 是独立母版。如需组合广告，先按脚本选择多个资产，不要要求单个录制脚本混入无关功能。
3. `capture_source` 是按真实操作窗口裁切的不可变 4K60 MKV/H.264 采集源，`universal_mp4` 是同一窗口的 4K60 H.264 通用母版。两者都会去掉登录、首屏空白、骨架屏和网络等待，并在 `source_clip_seconds` 记录相对未裁切缓存的来源区间；未裁切缓存只用于生成母版，成功或失败后都会删除。平台裁剪版存入 `deliverables/`，并保留来源 `asset_id` 和 SHA-256。
4. 可删除骨架屏、网络等待和无意义停顿，但不得删除 `shots` 中构成因果关系的必需镜头。
5. 生成社交平台版前，复核敏感信息、模型结果、画面对齐和文字可读性。
6. 时长规格必须来自自然操作和必需镜头；不得通过慢放、故意放慢指针、重复动作或无信息静帧满足最短时长。如自然流程短于当前最短值，应降低该资产的时长规格，或增加与主题直接相关的必需镜头。

## 内容与流畅度合同

- 图像和视频锚点以 `normalized_media` 坐标保存，并映射到画布中实际渲染媒体的边界；不得把包含 letterbox 的整个 stage 当作媒体区域。
- 每项人工标注必须等待类别选择器出现，按锚点声明选择精确类别，并等待创建或更新 API 成功后才能结束录制。
- AI 项目必须至少包含“有意义的真实目标提示 → 候选结果 → 人工审阅/采纳 → 已保存结果”；只录点击或等待中的前半段不合格。
- 指针输入逐帧使用受信任的 Playwright 鼠标事件按 60Hz 节拍发送。采集器还会在正式流程前录制 GPU 合成层校准动画，要求至少 58 个采样帧、有效独立画面不低于 55fps 且独立帧占比不低于 90%；这些实测值写入 manifest，只有容器标称 `60fps` 不能通过门禁。

## 组合教程（不归档为营销母版）

`e2e-quickstart` 同时包含登录、人工画框、提交质检和 AI 导入标注，
只用于用户文档中的组合教程 GIF。`marketing-master` project 会显式跳过它，
后期 Agent 应从 `bbox-draw`、`ai-prediction-import` 等原子母版中按脚本组合成片。

下表仅解释用户文档 GIF 的原有结构，不是营销资产规格：

| 顺序 | 画面                            | 必须表达的信息                 | 建议稳定停留 |
| ---- | ------------------------------- | ------------------------------ | ------------ |
| 1    | 登录页与已填演示账号            | 流程从真实用户入口开始         | 1–2 秒       |
| 2    | 无标注的道路图像和工具栏        | 标注对象与工作台上下文         | 1.5–2 秒     |
| 3    | 鼠标沿货车边界拖框              | 人工创建目标检测标注           | 拖拽约 1 秒  |
| 4    | 类别选择层与已确认的 `truck` 框 | 几何和语义类别都已保存         | 各 1–2 秒    |
| 5    | 提交成功提示                    | 当前任务进入质检               | 1–2 秒       |
| 6    | 下一题与对齐车辆的 AI 预标注    | 平台同时支持人工标注和 AI 候选 | 3 秒         |
