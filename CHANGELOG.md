# Changelog

本文件记录 AI 标注平台的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

历史版本详情见 [`docs/changelogs/`](docs/changelogs/)：

| 版本组 | 文件                                                   |
| ------ | ------------------------------------------------------ |
| 0.24.x | [docs/changelogs/0.24.x.md](docs/changelogs/0.24.x.md) |
| 0.23.x | [docs/changelogs/0.23.x.md](docs/changelogs/0.23.x.md) |
| 0.22.x | [docs/changelogs/0.22.x.md](docs/changelogs/0.22.x.md) |
| 0.21.x | [docs/changelogs/0.21.x.md](docs/changelogs/0.21.x.md) |
| 0.20.x | [docs/changelogs/0.20.x.md](docs/changelogs/0.20.x.md) |
| 0.19.x | [docs/changelogs/0.19.x.md](docs/changelogs/0.19.x.md) |
| 0.18.x | [docs/changelogs/0.18.x.md](docs/changelogs/0.18.x.md) |
| 0.17.x | [docs/changelogs/0.17.x.md](docs/changelogs/0.17.x.md) |
| 0.16.x | [docs/changelogs/0.16.x.md](docs/changelogs/0.16.x.md) |
| 0.15.x | [docs/changelogs/0.15.x.md](docs/changelogs/0.15.x.md) |
| 0.14.x | [docs/changelogs/0.14.x.md](docs/changelogs/0.14.x.md) |
| 0.13.x | [docs/changelogs/0.13.x.md](docs/changelogs/0.13.x.md) |
| 0.12.x | [docs/changelogs/0.12.x.md](docs/changelogs/0.12.x.md) |
| 0.11.x | [docs/changelogs/0.11.x.md](docs/changelogs/0.11.x.md) |
| 0.10.x | [docs/changelogs/0.10.x.md](docs/changelogs/0.10.x.md) |
| 0.9.x  | [docs/changelogs/0.9.x.md](docs/changelogs/0.9.x.md)   |
| 0.8.x  | [docs/changelogs/0.8.x.md](docs/changelogs/0.8.x.md)   |
| 0.7.x  | [docs/changelogs/0.7.x.md](docs/changelogs/0.7.x.md)   |
| 0.6.x  | [docs/changelogs/0.6.x.md](docs/changelogs/0.6.x.md)   |
| 0.5.x  | [docs/changelogs/0.5.x.md](docs/changelogs/0.5.x.md)   |
| 0.4.x  | [docs/changelogs/0.4.x.md](docs/changelogs/0.4.x.md)   |
| 0.3.x  | [docs/changelogs/0.3.x.md](docs/changelogs/0.3.x.md)   |
| 0.2.x  | [docs/changelogs/0.2.x.md](docs/changelogs/0.2.x.md)   |
| 0.1.x  | [docs/changelogs/0.1.x.md](docs/changelogs/0.1.x.md)   |

---

## [Unreleased]

### Added

- 平台身份与项目职责分离：账号新增平台角色 `employee`，项目职责改由**项目成员角色**（标注员 / 质检员 / 观察者）决定，同一个账号可以在 A 项目标注、B 项目质检、C 项目只读观察而无需切换账号或重新登录。项目列表 / 详情新增 `my_project_role`；成员输出新增扁平的 `platform_role`，与项目角色 `role` 分开；邀请继续使用 `project_member_role` 表达目标项目角色。新增 `GET /projects/{id}/access`（项目角色 + 能力集）与成员角色预检 / 变更端点（先只读预检阻塞项、受影响资源快照与一次性 token，再按成员版本 + token 在一个事务内原子交接）。Python SDK 同步更新模型与成员 / 项目访问方法，CLI 新增 `aap members preview-role` / `change-role`（`--role` 增加 `viewer`）。

- 支持管理员对未分派的草稿批次先跑批量 AI 预标注，再分派人工（Issue #124）：AI 预标页的批次列表现在同时列出 `active` 批次与「尚未分派标注员与质检员」的 `draft` 批次（后者带「草稿·未分派」徽标），数据管理里显式选择这类批次中的待标注任务同样可以发起预标，无需为了跑 AI 先分派人员或提前激活批次。预标完成后草稿批次保持 `draft`：候选预测挂在任务上保留，批次不自动激活、不隐式分派人员；随后在批次页签正常分派标注员与质检员并激活，标注员即可在工作台接管这批候选。AI 预标首页的项目卡片「可预标」计数与详情页准入同源，同样包含未分派人员的草稿批次；其他管理员在当前面板打开期间改派标注员 / 质检员时，列表与计数会实时刷新，不再保留已失效的可选项。已分派人员的草稿批次、已进入标注 / 审核流程的批次仍按原规则拒绝（提示语已更新为真实限制），批次管理员锁、他人编辑锁与终态任务保护不变；已激活批次的预标流程、失败记录、重试与跳过已预标行为不变。

- 登录后版本更新提醒：平台每次发版后，用户登录时弹一次「本次更新」窗口，展示当前版本更新日志的要点；刷新恢复会话不重复弹出，未确认版本留到下次登录再提醒。点击「知道了」按账号跨设备记录已读版本，同一版本不再重复提醒；直接关闭窗口则下次登录时再次提醒。顶栏版本号可点击，随时查看当前版本的更新内容。

### Changed

- 首页项目入口按项目职责分流：标注面板只展示可标注项目，审核历史进入审核工作台，观察者进入只读数据管理页，避免混合职责账号打开项目后被错误入口拦回首页。

- 员工授权不再来自账号级全局 `annotator` / `reviewer`：迁移把这两个全局角色转换为 `employee`（保留 ID、停用状态与全部历史），项目访问与写入一律由项目成员关系解析；平台 `viewer` 加入项目时只能是只读项目角色，不会被邀请静默升级。合法的跨项目混合职责（如 A 标注 / B 质检）不再被判为账号异常，也不会阻塞离职、停用等生命周期操作；删除 / 变更成员角色遇到未完成工作时要求先显式交接，不再静默清空指派。

- 全项目 / 批次 / 选定任务导出改为要求项目导出能力：仅项目质检员与项目负责人可用，项目标注员与观察者被拒绝。这是有意的收紧，导出创建、后台执行、缓存命中与结果获取都按同一能力校验；项目绩效明细与 CSV 仍只对负责人 / 超级管理员开放。

- 批次与审核操作的弹窗统一为应用内决策弹窗：项目批次页的「驳回批次」「撤销归档 / 重开审核 / 直接复审」「重置到草稿」「锁定批次」、审核页的「整批退回」，以及审核页、质检工作台首页与审核工作台画布内的「批量退回 / 退回」改用同一套决策弹窗（统一的标题、图标与危险态样式，必填与字数上限就地校验，Esc / 点遮罩取消并归位键盘焦点），各操作文案与提交行为不变。批量 / 单条退回由「单窗口选原因类型 + 可选备注」改为两步：先选退回原因类型，再填写可选补充说明，任一步取消都不会提交；被标注员跳过的任务在第一步保留跳过提示，第二步补充说明预填「标注员跳过：原因」（可直接改写），确认后随退回一并提交，不再丢失跳过说明。重置到草稿的警示要点并入弹窗正文，原有「至少 10 字」原因下限改为弹窗内即时校验。
- 工作台与系统设置的操作确认弹窗从浏览器原生对话框改为应用内统一决策对话框：Mask 离开与擦空 Mask 由两次连续确认改为一次三选一（「保存后离开 / 丢弃并离开 / 继续编辑」与「删除对象 / 撤销擦空 / 继续编辑」），删除视频轨迹、覆盖未保存 Mask 稿件、追踪审阅覆盖人工关键帧、清空离线队列、删除 2D 成员、按插值展开全部帧、补全属性字段等确认同步迁移，危险操作带红色警示并默认聚焦「取消」；系统设置有未保存修改时，离开确认（含浏览器前进 / 后退与页内分区切换）等待对话框决定后再继续原导航，刷新 / 关闭页面的浏览器原生确认保持不变。弹窗打开期间工作台快捷键（如审核流的 A / R）不再穿透到画布误触后台操作；确认等待期间切换题目或轨迹已被并行删除时会安全放弃、不对旧题标注提交删除；等待期间登出或换账号会取消排队中的弹窗，不会用新凭据执行旧会话的变更；清空离线队列只丢弃确认时看到的那批操作，等待期间新入队的未同步编辑不受影响。
- 管理端操作确认从浏览器原生 confirm 改为应用内统一决策对话框：数据集切换 LiDAR 坐标系约定、轮换 / 吊销 API 密钥、删除存储连接器、删除问题反馈、删除项目模板、删除类别与属性，以及标注页、项目批次列表与「我的批次」的「提交质检」确认。新弹窗标题说明动作与对象，正文说明后果，删除模板、类别与属性时显示使用次数等受影响详情，危险操作带红色警示并默认聚焦「取消」；各操作本身的提交行为与结果反馈不变。
- 导入数据集向导的文件校验提示不再使用浏览器原生 alert：「请选择 .zip 文件」「ZIP 包不能超过 200MB」改为文件选择区正下方的行内错误提示，选中合规文件后自动清除。
- 项目设置「类别与属性」页签右上角操作收敛为两项：保留「从 ML Backend 预填」（类别 + 属性的唯一批量入口），「导出属性 JSON」「导入属性」合并进「属性 JSON」下拉菜单，菜单项更名为「导出 / 导入属性 schema (JSON)」并附说明文字：仅含当前工具单位的属性 schema，类别不能经 JSON 导入 / 导出。导入 / 导出行为与文件格式不变。
- 项目设置所有页签的面板标题与右上角操作按钮统一风格：标题统一为 13px 半粗体并带分隔线头部（工作台规范、视频采样、标注指引三个页签此前为 15px 大标题、无分隔线），头部操作按钮统一为同一高度的小号幽灵按钮（成员、数据集、批次、ML 模型页签此前为大号描边 / 主色按钮，批次页签为更矮的自定义按钮），「导入属性」改用与其他头部按钮一致的组件实现。各页签交互与功能不变。
- 筛选栏移除冗余装饰标签：审计「事件范围」、成员「账号」、离线分析「时间范围」、模型注册「健康」、邀请「邀请状态」、标注「任务状态」、审核「指派范围」、后台任务弹层「状态」，以及各总览 / 数据集 / 模板列表的状态、类型、范围切换组，不再显示漏斗图标加文字前缀，选项内容本身已说明含义；无障碍名称保留，读屏与键盘操作不受影响。成员管理、质检面板等并排筛选组之间起区分作用的标签保留。

### Fixed

- 开发种子脚本现在创建合法的平台身份与显式项目职责：`apps/api/scripts/seed.py` 的 `qa` / `anno` / `anno2` / `anno3` 改为平台 `employee`（`viewer` 保持只读），并在播种后为夹具项目（P-COCO8 / P-VIDEO-DEV / P-PC-DEV / P-PC-MULTI / P-OCR）幂等补齐项目成员关系（anno / anno2 / anno3 = 标注员，qa = 质检员，viewer = 观察者；`pm` / `admin` 通过项目负责人或超管身份管理，不写成员行）。此前这些账号被写成迁移 0174 已废弃的平台 `annotator` / `reviewer`，且没有任何成员关系，新环境按 README / DEV.md 入口播种出的账号无法访问任何项目。`scripts/seed_scale.py` 改为经 `project_members` 解析标注员，并为其压测项目写入明确职责；截图种子的 persona 键保持不变，平台角色同步改为 `employee`。

- 管理端「人员管理」卡片的角色徽章改为按平台身份呈现（超级管理员 / 项目管理员 / 员工 / 观察者，含中文标签），不再把平台角色与项目职责字符串 `annotator` 比较——该比较在员工角色迁移后恒为假，所有卡片都会渲染同一个强调色徽章。

- 修复数据管理页筛选字段加载失败后一直停在「加载中」并反复请求的问题：筛选字段配置请求失败时，页面现在稳定显示错误提示与「重试」按钮，不再因错误分支被重新挂载而自动重复请求；点击「重试」才会重新获取配置并继续查询任务。

- 项目级员工角色闭环的评审修复：批量邀请选择目标项目后把账号角色切到「平台观察者」时，项目职责现在自动同步为「观察者」，不再提交 `viewer + 标注员` 的冲突组合导致整批预览被拒；项目管理员打开成员编辑弹窗不再展示必然 403 的平台角色编辑入口（改为只读展示并引导到项目成员管理），用户列表对观察者行也不再提供编辑 / 删除等后端不接受的生命周期操作；员工首页对只持有观察者成员关系的账号新增「浏览项目」只读列表（此前两个工作页签全空且无法打开项目）；点云质量入口改按项目能力（`review.write`）门控，项目质检员不再因全局角色移除而丢失质量查询与工具栏入口；离线队列在项目访问检查遭遇网络 / 服务器故障时自动退避重试，不再误报「项目权限已变更」，重放 404（任务 / 标注已在别处删除）不再被当成永久权限拒绝——已不存在的目标删除操作直接出队计为已同步；实时通知在业务事务提交后才发布，慢事务下不再丢失实时推送。

- 工作台上下文工具条（如追踪审阅栏、Mask 工具条）在确认应用内决策弹窗后不再意外收起：慢环境下弹窗关闭与外部交互检测存在竞态，确认「覆盖」等破坏性决定时叠加的工具条面板会被误判为点击外部而折叠，导致追踪审阅中途丢失进度面板。现在落在模态决策弹窗内的指针与焦点交互一律不折叠下层工具条，真正的画布外部点击仍正常收起。

- 「添加项目成员」可在同一弹窗中分别选择标注员和审核员并一次确认：切换角色不再清空已选人员，同一成员不会重复获得两种项目职责；部分失败时只保留失败成员重试，提交期间不会重复发送。

- 员工项目角色迁移审计不再把启用的超级管理员、合法项目负责人的代标 / 代审误报为缺少项目成员关系；普通员工的缺失 / 错误成员职责、跨项目管理员和停用账号仍被检查，不会为了消除报告而改写业务成员关系或补造审核证据。

- 修复换账号登录后被带回「无权访问此页面」的问题（Issue #123）：此前在错误页或受限页主动退出后，登录页会沿用退出前的地址作为返回目标，换账号登录后直接回到「无权访问此页面」，提示与新账号的实际权限无关。现在主动退出不再沿用旧会话的返回地址，新账号登录后进入其角色对应的默认首页（超级管理员进「平台概览」，其余角色进各自 Dashboard）；`/unauthorized` 等错误页与登录 / 注册 / 找回密码等认证页也不再成为登录后的返回目标，仅保留邀请链接「登录后确认加入」所需、带 `token` 的注册页返回目标。会话过期或未登录时打开正常业务链接，登录后仍返回原页面；主动退出在退出请求返回 401、凭证已被清空，以及在其它标签页收到同一账号退出时同样生效；管理员重置密码后的强制改密跳转保持不变。
- 工作台「当前题 AI」不再被批量预标注的任务状态校验拒绝（Issue #121）：在此之前，对正在标注（`in_progress`）的当前题运行 AI 会返回 `explicit preannotation tasks must be pending`，尚未激活的草稿（`draft`）批次同样被挡。现在图片工作台的当前题 AI 按当前用户对本题的编辑语义执行，允许 `in_progress` 任务与 `draft` 批次，仍保留批次管理员锁、他人编辑锁以及已提交审核 / 已完成任务的保护；被拒绝时面板给出明确中文原因（如「当前任务正由其他成员编辑」），不再是英文错误。数据管理批量预标原有的 pending + 活动批次限制不变。
- 项目管理员的人员可见与可操作范围一致：新建项目分配成员、已有项目「添加项目成员」与「用户与权限」使用同一套范围。项目管理员现在在「用户与权限」中也能搜到启用但尚未加入任何项目的标注员 / 质检员（此前会误以为账号不存在），并且可直接对这些启用账号执行编辑成员、重置密码、分配数据组与角色切换（标注员 ↔ 质检员），不再提示「仅可查看」；离职处理、停用、删除等涉及工作交接的操作对未分配账号同样放行，但目标的关联项目（成员、批次、任务与锁，含已移出成员关系但仍在他人项目持工的账号）一旦出现在其他人的项目里仍须超级管理员处理，转交接收人必须是每个待转交任务所在项目的成员，已停用且未分配的账号不再放行给项目管理员交接。跨项目账号的行内操作保留编辑与重置密码，隐藏离职 / 删除入口（`is_lifecycle_managed=false`），避免点击后必然 403。启用的超级管理员对其保持只读以便联系核对。搜索、角色筛选、分页、统计卡与导出名单采用相同范围；已停用账号、其他项目管理员和观察者的可见范围不变。
- 「属性 JSON」下拉菜单键盘可用性：打开菜单后焦点现在真实移入面板首项（↑↓ / Home / End / Enter / Space 可直接操作，Tab / Shift+Tab 关闭菜单并把焦点归还触发按钮，Esc 关闭后焦点同样回到触发按钮）；此前面板仅做视觉高亮、不移动真实焦点，portal 面板对键盘用户不可达。
- 批次管理页签头部在窄屏（约 480px 视口）不再溢出：标题与右上角操作（视图切换 + 两个按钮）改为可换行布局，超宽时操作组折行到标题下方。
- 修掉视频时间轴点击的 stale seek 竞态（应用层，Issue #114）：高负载下点击时间轴触发的异步精确取帧可能迟到，与随后的键盘逐帧步进竞争后把当前帧号拽回点击时刻的旧帧（实测 F120 被拽回 F105，画面与 `data-video-painted-frame-index` 一并跳回）。播放控制器现在在帧号写入汇聚点加单调栅栏：暂停态只接受「最新一次导航的目标帧或其回声」，迟到的旧帧回报直接丢弃（仍入位图缓存，切回该帧即时可见）；播放态逐帧推进不受影响；精确绘制回执同步改为按最新帧校验，过期绘制不再发布。时间轴点击不再先发起一次网格 snap seek 再被目标 seek 取消。导航目标随每次提交收敛到当前帧,「seek 后播放推进/宿主外部跳转再暂停」也不会让旧目标的迟到回报过栅栏。此前 E2E 通过 `expectPaintSettled` 串行化规避（见下一条），现转为冗余防护。
- CI 前端 E2E 不再间歇性误红：布局压力套件里图片 / 视频用例原先也跑在点云项目的 SwiftShader 软渲染浏览器上，高负载 runner 在刷新恢复布局时会把渲染主线程冻结数十秒，导致视频画布 30 秒等待超时（真实后端响应一直正常）。现把图片 / 视频布局矩阵移入普通 chromium 项目、画布等待改为先等宿主挂载并对压力场景放宽到 90 秒；压力套件允许一次重试以区分确定性损坏与 runner 资源饥饿（重试通过仍标记 flaky，视觉基线维持零重试）。同时修复视频 Issue 用例中时间轴点击的异步取帧迟到、把键盘步进后的帧号拽回旧值导致的偶发失败，改为等绘制帧号与选中帧号一致后再继续。
- 修复审计页与成员管理页筛选栏错位：范围 / 账号下拉原先占满整行被挤到第二行，使同行「筛选」按钮与结果计数垂直错开；现在与相邻控件同排对齐。
- 整批送审不再只改批次状态：标注页、项目批次列表与「我的批次」的「提交质检」现在会把批次内所有未送审任务（未开始 / 标注中）一次性提交质检并锁定，完成后批次才进入审核中；已退回任务需先「接受退回」重做，不会混入本次送审。标注页按钮改为按「是否还有未送审任务」显示，批次已进入审核中但仍有遗留任务时也能继续整批补交。
- 审核页「整批退回」对部分送审的批次可用：批次只有部分任务送审、状态仍是未开始 / 标注中时，审核员与超级管理员也能整批打回重做；退回原因改为应用内弹窗填写（不再依赖浏览器 `prompt`，避免在不支持或拦截弹窗的浏览器里点了没反应），并修复退回成功后左侧批次树与批次进度不刷新的问题。
- 工作台顶部栏在中等窗口宽度下不再重叠：项目 / 任务标识与「上一个任务 / 提交 / 跳过 / 下一个任务」此前会在约 700–1200px 窗口区间叠在一起（命名空间化的长任务 ID 如 `T-E2E-…` 会触发）。中段现在不会压缩到内容以下，空间不足时先收缩左侧项目名；约 1000px 起把操作按钮收起为保留悬停提示的图标，并把位置徽章、版本更新、Bug 反馈、主题与设置等次要入口收进「更多工具」。任务位置仍显示在左侧任务队列的「当前 / 总数」中。

### Security

- 自审拒绝改为基于任务上持久化的贡献者证据：标注阶段的实际操作者（含批量、撤销 / 恢复、转换、导入、AI 接受、视频 / 场景 / 多相机写入）、当前审核轮次冻结的贡献者与提交者。提交者不能领取、编辑、通过或退回自己参与过的内容，包含管理者动作与批处理路径；历史遗留且证据不完整的任务在单独批准的修复完成前阻断审核（`409 review_contributors_unknown`，平台不附带自动回填工具），不按旧全局角色回退。

- 明确已签发直接存储 URL 的撤销边界：撤销成员 / 角色后不再签发新链接，也不再交付新结果，但已签发的链接在到期前仍可使用。当前有效期为下载 1 小时（到期向下一个 10 分钟缓存窗口对齐）、评论附件 5 分钟（无对齐）、导出 7 天（调用方另受同样的对齐约束）；立即失效旧链接需要单独的存储 / 代理改造。

## [0.25.4] - 2026-09-15

### Added

- 通知覆盖全工作台：通知 WebSocket 连接移到应用生命周期，主界面与全屏标注 / 审核工作台顶栏显示同一通知铃铛与未读角标；未读数不再只是一个圆点，1–99 显示精确数字、100 以上显示 `99+`，悬停与读屏标签公布精确数值，刷新失败时保留上次数值或提示「未读数暂不可用」。从通知进入目标前会先执行视频 / Mask 离开检查，取消离开保留当前任务与草稿。
- 重要消息瞬时提醒：可见标签页中对任务退回、批次驳回 / 重开 / 管理员锁定、评论提及、问题回复、后台任务失败、导出失败等类型弹出短暂提醒，1 秒内多条合并为一条，按通知去重、不抢焦点、不自动跳转；隐藏标签页只同步角标。断线期间的事件由 30 秒轮询与回前台 / 重连时的立即刷新补齐，不回放历史提醒。
- 通知偏好支持按类型分别设置「接收通知」与「弹出提示」：设置随账号同步，个人设置页与新增的工作台设置「通知」分类共用同一面板；默认仅重要类型弹出，普通完成事件与未知类型默认不弹，关闭接收只禁用弹出、不重置已存选择。修改自动保存，失败时该行恢复上次保存值并可重试。`GET /notification-preferences` 返回每类有效 `toast`；`PUT /notification-preferences` 接受 `type` 加可选 `in_app` / `toast`（至少一项，拒绝 null），只合并提供的键，旧客户端只发 `in_app` 不会清除已存弹出选择；偏好提交后发布 `notifications.sync reason=preferences` 同步其它会话。
- Data Manager 数据区域改为页面滚动：任务与对象列表随页面自然延伸，标题、摘要和查询工具栏滚动让位，表头保持吸顶；任务分页、对象/轨迹游标加载与虚拟化、视图侧栏、筛选和分享链接行为不变。

- Data Manager 新增标注图片只读预览：任务“匹配对象”抽屉与对象详情顶部显示整图叠加已保存标注（含旋转框、带孔多边形、关键点骨骼与像素掩码），可一键对比原图；预览按任务分页加载标注并高亮选中对象，切换或关闭时取消过期请求，不产生任何标注或工作流写入。

- 成员绩效新增“已标注图片”与“保留标注”保存内容指标：统计区间内创建且仍保留的标注（图片按任务实际媒体类型去重），无需提交或审核即计入；项目汇总独立去重，列表、详情与 CSV 同口径，并提供对应排序。

- Data Manager 任务列表新增“未解决问题”和“评论”两列与数值筛选：问题列对齐工作台问题口径（有效、开放的问题主题），评论列对齐工作台评论总数（任务留言 + 标注评论）；旧“反馈”列、排序与筛选保留为兼容别名并修正为问题计数，恢复旧保存视图时自动映射到新列。

- 工作台评论与任务留言的 `@` 候选加入项目负责人（「项目负责人」）和平台超级管理员（「超级管理员」），即使不在项目成员表中也可被提及并收到站内通知；新增项目维度的候选接口，普通成员无需管理权限即可获取。

- 工作台快捷键面板重构：与工作台设置同一几何，按用途分类导航（常用、绘制与工具、选择与编辑、画布与视角、AI 与审核、播放与轨迹、任务与系统、鼠标操作），支持当前工作台 / 全部类型过滤、跨分类搜索动作 / 按键 / 生效条件，并在行内展示实际生效条件；原「按使用频率排」与使用计数展示移除。

- 账号级快捷键改绑：图片 / 视频工具选择、切题、选中态锁定隐藏、视频帧与关键帧导航、书签等命令支持改绑一主一备组合、停用与恢复默认，随账号跨项目生效并参与统一冲突检查；工具坞角标与提示按生效组合展示。项目类别直选改为数字 1-9 加 0（按配置顺序取前十类），字母不再切换类别；类别面板支持方向键导航。视频「实际尺寸」与 3D 三视图缩放重置改用 Shift+0，图片属性数字键仅在选中卡「属性快捷键」区域聚焦后生效。

- 用户可设置个人头像：上传自己的图片（PNG / JPEG / WebP，不超过 2 MB，服务端自动居中裁成方形并缩放到 256×256），或从 32 个内置像素头像中选择，也可一键恢复为姓名首字母。头像随账号跨设备同步，顶栏、任务与批次责任人头像组、项目成员与用户管理列表都会显示；头像存放在独立对象存储桶，长期保留且不计入清理策略。

- 模型市场注册管理支持完整的 URL 状态：子视图（`registry_view`）、各子视图搜索与筛选、以及对象定位键（`pool_id` / `instance_id` / `gpu_id`）都写入地址栏，刷新、前进后退和分享深链可恢复原来的子页与条件；从服务池「查看实例」和问题中心「定位」的跳转改为 URL 导航，支持往返。

- 模型市场各注册子视图拥有各自的工具栏与明确主操作：实例视图突出「注册实例」并支持按池定位的可移除条件，服务池视图突出「新建服务池」，问题中心新增严重度快捷筛选与按对象名称显示的附着筛选，项目绑定提供搜索与池限定。

- 能力目录条件全部写入 URL（搜索、分组、显示方式与任务/模态/模型族/框架多选），工具栏固定为「搜索/分组/视图/刷新 → 快捷筛选 → 结果计数与条件标签」三行，结果计数显示「匹配 N / 总计 M」并区分协议能力类别数；模型卡首层摘要化（徽标 + 输入→输出摘要 + 运行状态 + 详情展开），协议列表「查看全部 N 个模型」可实际展开剩余模型，未接入能力的推荐后端收进展开区。

- Data Manager 新增项目概览、任务缩略图与画廊、可收起的分组视图，以及项目列表直达入口；原有任务、对象、轨迹筛选和分享链接继续可用。

- 项目负责人可在成员绩效看板查看成员提交、审核、首审质量、已记录时长与待处理工作，包含零活动和历史贡献者，支持同范围详情与 CSV；缺失历史或计时证据会明确显示覆盖状态。

- Data Manager 支持最多 200 个明确选定任务的分派预览、局部导出和预标注，操作复核任务权限、状态与锁，并保留异步作业进度和失败原因。

- 工作树支持一条命令启动筛选手动验收环境，提供专用账号、场景入口与预期结果，停止和重启保留验收数据。

- 图片与视频 AI 任务支持独立的筛选链接，文本搜索等待完整输入后同步重置分页；模型目录在不同分组下使用一致条件，历史队列和通知明确已选及已加载范围。

- 成员、邀请、绩效、审计与 BUG 列表支持筛选链接恢复；搜索统一减少连续请求，成员与邀请条件互不覆盖，审计空字符串条件在列表和导出中保持一致。

- 项目、数据集和模板列表支持可分享的筛选链接及浏览器前进后退恢复，搜索减少连续请求；项目状态标签与高级筛选保持一致，日期范围错误会在应用前提示。

- Data Manager 任务、对象和轨迹视图共用类型化筛选编辑器，支持保留并编辑嵌套 AND/OR、多选枚举和完整区间；刷新与浏览器前进后退恢复条件，未完成输入保留已应用结果。

- 文档高清母版录制支持 Mac 原生窗口采集，可为不依赖 ML 的操作选择相同的 4K60、独立帧率和归档验收，复用现有流程与媒体派生命令。

- 多工作树开发支持独立数据库、Redis、对象存储桶及可选本机 worker，提供环境诊断、停止和需精确确认的重建/清理；测试与端到端验收可使用独立模式，避免不同分支的迁移和任务互相干扰。

- 图片、视频和点云任务留言支持 `@` 项目成员，保留提及并发送可静音的站内通知，点击可定位原任务留言。

- 视频单帧和轨迹标注显示评论数量气泡，跟随当前帧可见位置；沿用默认开启的账号级「标注评论提示」设置。

- 图片问题可关联落点时选中的标注，图片和视频均可在提交前取消对象关联并保留画布位置；问题列表与详情显示关联对象，定位时选中仍可用的对象。

- 图片任务在未选标注时也可使用弹窗批注或直接在题图上绘制，并发送纯绘图留言；任务与标注绘图草稿分别保留。

- 图片画布的正式标注显示评论数量气泡，点击可打开对应讨论；同类别实例独立计数，超过 9 条显示 `9+`。新增默认开启、按账号保存的「标注评论提示」开关。

- 工作台支持未选标注时发送任务文字留言；阅读范围与发送目标分开选择，任务和标注草稿在同一次登录会话中随切题、面板调整及站内返回保留。

- 工作台问题支持完整对话、文字回复及上一条／下一条未解决导航；打开详情与画布定位分开，解决问题后可继续阅读，图钉可直接打开筛选外或较早的问题。

- 问题回复、状态变化和标注评论中的 @ 提及会发送站内通知，可按类型静音并直达原对话或评论；较早内容会继续分页查找，不受同一对话后台刷新的干扰，跳转时保留已有草稿确认。

- 标注指引和项目模板指引支持可视化排版、Markdown 源码与预览；BUG 描述和评论共用紧凑编辑器，支持表格、列表与代码。空白指引可插入起步模板，保存和上传失败时保留正文并提供重试。

- 工作台可把当前面板排列另存为命名布局预设，随账号保存、最多 5 组，支持应用、重命名和删除。预设记录保存时的工作类型，只在同类型任务中可应用；应用时保留当前画布、任务、工具与未提交草稿，并可撤销。

- 成员与邀请管理提供分页、组合筛选及同范围统计与导出；批量邀请和分组显示逐项预览与结果，失败项可单独重试。单批次与项目派发前展示负责人变化和任务负载，数据变化后需重新确认；角色编辑可核对跨项目影响，个人设置支持退出其他设备。

- 邀请可指定目标项目，新账号注册时一并加入用户组和项目，已有账号登录后确认加入；接受时重新检查邀请人及项目权限，并展示可开始工作或等待分派的状态。

- 超级管理员可分组管理邀请上限、离线判定、连接器导入预算、建任务阈值与视频预热，查看有效值来源并恢复部署默认；保存保留其他组草稿，并发冲突需核对最新值，SMTP 支持保留、更换和明确清除密码。

- 通知可直达具体任务与最新退回状态，首页退回入口保留待重做筛选；后台任务支持查看历史及该次结果，失去目标权限、导出过期时提供处理入口，批量预标可只重试失败项。

- 人员管理支持启用、停用和全部账号筛选，按项目分别交接负责人、标注员和审核员职责；紧急停用保留待交接事项，明确区分可恢复停用与删除、历史未知状态。

- 支持在现有环境旁部署独立的局域网生产应用：前端 HTTPS 3030、API HTTPS 8080，复用数据库实例、对象存储和模型服务，生产账号、业务数据、存储桶及后台任务独立；提供客户端证书信任和恢复说明。

### Changed

- 移除筛选栏中纯装饰的漏斗图标 + 文字标签（通知、问题、Bug、离线队列、模型市场、AI 预标注等），保留用于区分多组筛选的条件名称标签，筛选栏更紧凑；`aria-label` 无障碍分组名保持不变。

- 点云渲染资格 benchmark 适配当前工作台 UI：进入后自动展开默认折叠的 Scene 时间轴，三视图精修改经工作台设置对话框「面板与高级布局」开关打开，恢复 v0.25.x 布局改造后失效的三轮资格测试；Apple Metal 三轮资格中两轮完整过门（详见 macOS WebGPU 实测报告与 ROADMAP）。

- 工作台居中悬浮面板（设置、快捷键、标注指引）与全局模态统一对背景做同一程度的模糊压暗；3D / 点云工作台只压暗、不模糊，避免软件渲染下 backdrop-filter 拖慢合成清理并导致后续上下文初始化超时。快捷键面板中的图片、视频、点云阶段标签分别使用蓝、紫、琥珀色，便于在「全部类型」视图快速区分适用范围。

- 工作台设置与快捷键面板的搜索结果高亮命中的关键词，便于在长列表中快速定位匹配项。

- 模型市场注册管理的四张主表改为卡片式圆角表框、浅色表头标签带与更宽松的行内边距，表头与数据层级更清晰；行分隔线由正文颜色改为语义边框色，不再在深色主题呈现亮白网格、浅色主题呈现深黑网格。

- 模型市场页面骨架收敛为一个标题 + 下划线式主 TAB，移除父页面重复的全局统计卡；注册管理改为紧凑摘要带 + 带计数的子 TAB（问题徽标按真实最高严重度着色），主表列收敛后身份与操作在 1440px 宽度同屏可见，ID 默认显示短 ID（tooltip / 复制可取完整值）。

- 运行时观测页头的四个大摘要块改为一条紧凑摘要带；服务池卡精简为「身份 + 健康/路由/新鲜度 + 带标签字段带」两行布局，无流量指标的池只显示一条「暂无路由指标」，驻留/CPU 回退未知时不冒充数值；成员首层补充并发与驻留独立字段。刷新与自动刷新不再引起卡片跳动或丢失展开状态。

- 平台概览的最近项目仅保留设置和打开入口，复制项目统一从项目管理进入。

- 项目管理的列表与卡片支持服务端分页和每页条数选择，页码随链接恢复；搜索、筛选或修改条数回到首页，删除导致页码越界时自动回退。日期筛选支持完整结束日，并正确报告无效日期。

- 平台概览最多展示最近创建的 5 个项目，并提供「查看全部项目」链接进入项目管理，避免项目增多时概览列表过长。

- 批次列表操作按钮默认以图标显示，悬停时展开名称，减少操作列的视觉拥挤；键盘聚焦与触屏操作也能查看名称。顶部的分派、创建按钮与视图切换控件统一字号和高度。

- 更新用户手册静态截图：登录、项目与筛选、Data Manager、AI 预标注、审核、设置、离线分析、系统健康与平台管理页面按当前界面重拍，补充悬浮 AI 面板与 AI 追踪面板配图，并同步刷新首页 Hero 配图。

- 重录矩形、旋转框、折线、多边形、Mask 涂抹、批次批量操作、视频章节、大图渐进、单切片恢复、Mask 上限与快捷键面板演示视频，按当前界面与干净工作树录制并重新审片。

- 全站筛选使用统一漏斗图标与紧凑控件；项目高级筛选改为按钮旁展开，成员、邀请、审计和模型附加条件提供可移除摘要，窄屏使用底部面板，工作台筛选保持原位与原有操作范围。

- 项目高级筛选面板改为首次展开时按需加载，减少首屏 JavaScript；入口和已选摘要保持同步显示，关闭与再次打开保留原有交互。

- 文档站按任务重组导航与栏目入口，优化正文宽度、长页目录、代码和表格阅读，统一中文导航标签与深浅色、窄屏布局；操作指南优先展示完成任务的主流程，并保留原页面链接与章节锚点。

- 项目标注指引在停止输入后自动保存，并显示等待、保存中和失败重试状态，减少编辑后忘记保存的情况。

- 工作台标注指引移到顶栏，通过按钮打开与设置同尺寸的阅读窗口，支持点击外部关闭；返回时保留原标注状态，阅读确认和重试入口保持可见。

- 布局预设示意图采用 16:9 画幅，按实际侧栏宽度和上下分区比例展示，方便比较各预设的画布空间。

- 工作台各布局预设的左右侧栏统一为工作区宽度的 15%，审核协作保留右侧详情和底部讨论，AI 审阅与视频追踪不再默认占用较宽的右栏；窄栏下标题、表单和操作按钮自动换行。

- AI 示例框与二次推理的胶囊悬停区支持直接调节已有置信度；二次推理显示当前能力和目标文本，完整面板与 AI 交互工具统一紧凑布局，使用 X 收起。

- AI 交互胶囊以正负提示、目标文本、输出形态与候选序号替代工具名称；完整设置改为紧凑纵向布局，将候选决策与模型参数分开，空闲时隐藏无效的候选按钮。

- Mask、交互式 AI、二次推理和视频候选审阅统一为按图标与关键信息长度收紧的左侧小胶囊，悬停后在同一表面向右、向下同步展开高频操作，完整设置通过“更多”打开。收起不清空输入、配置或候选；后台追踪候选不会抢占正在进行的 Mask 或 AI 编辑。

- 选择“标准标注”布局时，左右两列的上下内容均按 50% / 50% 分配高度。

- 工作台布局设置以高亮边框标记实际匹配的预设，非预设排列自动归为自定义布局；高级面板操作改为带显隐状态的紧凑列表。

- 工作台布局菜单收拢为常用入口，现可直接保存当前排列并切换同工作类型的命名预设；完整布局与跨类型预设管理位于设置，并提供可视化预览。移除重复的画布移动操作。滑动设置时透明显示工作台并实时更新，边栏宽度接入停靠布局。

### Fixed

- 通知详情和目标错误弹窗打开时，快捷键不再操作背后的标注；从后台任务详情跳转或切换批次前检查视频与 Mask 草稿，取消后保留当前工作。点击离开确认中的「继续绘制」会保留底层任务详情，避免被误判为点击遮罩。跨批次任务通知会同步切换目标批次与任务队列。
- 连续修改多种通知偏好时，各行独立完成保存、失败回滚和重试，旧账号的迟到响应不再影响新账号；工作台设置搜索「通知」或「通知偏好」会展示对应设置项。
- 通知已读 / 删除 / 清空的跨会话同步改为业务事务提交成功后才发布 `notifications.sync`（读操作 `reason=read`，删除类 `reason=deleted`），另一会话收到事件时读到的必然是已提交状态；无行变化或事务失败不再广播，发布失败也不影响已提交的请求。
- Data Manager 的任务、对象和轨迹宽表不再撑出结果区域；横向滚动限制在表格内部，保留页面纵向滚动、表头吸顶和游标加载。

- Data Manager 的“未解决反馈”计数此前混入问题回复、普通评论、BUG 与驳回记录：现在按工作台问题口径只统计有效、开放的问题主题；对象视图与摘要中的问题计数同步修正，问题被解决、搁置或删除后不再残留虚高计数（含已删除主题的存活回复）。

- 工作台讨论的 `@` 候选浮层在靠近窗口底部时改为向上展开，内容不再被视口裁掉；按 Enter 确认候选项时只插入提及，不再同时把评论发送出去，需再次按 Enter 才发送。

- 工作台讨论输入框修复中文输入法在 `@` 提及后被打断的问题：组合期间不再触发草稿同步与候选浮层更新，组合结束统一同步；提及后光标落在可编辑文本节点内，`@` 候选浮层也放行组合中的按键。此前选完 `@` 继续输入拼音时，首字母会被直接提交（如「测试」变成「测试c」）并把光标带偏。

- 主开发环境支持独立的每日定时重启，通过 systemd 管理 Web/API，并限定重启开发 worker 与 beat，避免打断同机生产环境依赖的共享服务。

- 局域网生产环境增加仅针对现有生产容器的定时健康恢复，隔离开发 3000/8000 与共享基础设施；恢复前确认连续故障，并提供冷却、维护暂停和 HTTPS 入口验证。

- 快捷键改绑的边界收紧：写入按域校验命令并只接受当前 schema 版本，未知条目的重置会真正清除；录制拒绝无法持久化的命名键，工具被改绑到 F1 / F5 等键时吞掉浏览器默认行为，面板正确展示 `Ctrl+Backspace` 这类共用修饰键的备选组合。

- 工作台设置面板的分组标题此前与字段名同为 13px，层级不清；现改为更大的加粗字号并加分隔线，与字段名、说明形成清晰层级。

- 工作台快捷键面板统一每一行的行高与分隔线间距：说明与行内操作固定占用同一行，不再因有无备注、备用组合或固定动作而高低不一。

- 工作台自定义快捷键完整应用新组合，并隔离图片、视频与点云工作台；冲突检查覆盖审核和画布固定动作，也用于启用及恢复默认。连续编辑不再覆盖待保存值，退出工作台或切换账号后不会串写偏好。快捷键面板修复录制确认、搜索、属性控件方向键与停用提示。

- 恢复全局默认边框色：Tailwind v4 在未显式设置边框色时回退 `currentColor`，导致对话框、抽屉、气泡、下拉与选择器在深色主题出现近白描边、浅色主题近黑描边；应用主题基础层统一为语义 `--sc-border`，显式状态色与透明边框不受影响。

- 注册管理「实例」子页的工具栏与主表恢复与其他子页一致的区块间距（此前该视图未套用统一的 `gap-3` 容器，工具栏紧贴表头）。

- 注册管理的实例页签在实例列表加载失败时显示「?」而不是 0，不再把失败的列表冒充成空注册表；服务池「驻留」摘要只在驻留状态可信且可解析时给出数量，显式未知、畸形载荷或未核实来源如实显示「未知」；能力目录的无效分组/显示方式条件标签新增移除按钮，点击后回落默认值并把非法参数从地址栏清掉。

- GPU 资源视图在可分配预算未知时不再绘制空进度条冒充 0%，静态声明超售与运行时占用保持两条独立读数；项目绑定不再把按可路由数量推断的「主服务池」当作已配置事实展示。

- 模型卡在 instances 单端点路径（无运行时池信息）时如实显示「运行状态未知」，不再把未知驻留显示成「未加载」。

- 模型市场搜索保留输入中的空格，问题中心筛选的「完成」按钮保留已选条件；对象定位清除目标页冲突条件，实例详情随浏览器前进后退恢复或关闭。畸形驻留状态显示未知，按后端分组的模型卡保留任务徽标。

- 视觉回归使用构建后的页面与媒体就绪检查，截图数据同步补齐缩略图和视频海报，并固定展示编号、时间戳及预标模型选择，减少环境差异造成的误报。

- 项目总览在防抖搜索生效前打开新建项目向导、切换视图或翻页时不再丢失输入中的搜索词；草稿会在防抖结束后正常写入筛选链接。浏览器前进/后退仍按链接恢复搜索。

- 页面顶栏显示当前前端的完整版本号，并随发布版本自动更新。

- 审核首页没有待审批次时不再显示无效的「返回全部批次」按钮；进入项目、批次或标注员筛选范围后仍可返回。

- Data Manager 的任务单独指派在批次改派后保持有效，恢复批次默认后重新跟随；调度优先处理有优先级的批次任务。失败的局部导出和预标注可沿用原任务与参数重新运行。

- 成员绩效保留归档后的首审贡献者，趋势与汇总按任务一致去重，缺失归属与旧视频历史明确提示覆盖不完整；审核覆盖独立计算，空指标分页保持稳定。大项目的工作流统计改为数据库聚合，避免加载全部审计明细。

- 成员筛选变化时清空旧详情，加载更多依据时保留已显示记录；人员管理的固定四周趋势使用正确标题。长时间操作的早期计时区间在状态转换后仍可校验入库，并发同 ID 的冲突事件会被明确识别。

- 数据管理入口使用独立表格图标，避免与筛选混淆；任务、对象和轨迹视图的统计、刷新、保存视图按钮统一为紧凑样式，降低对数据区域的视觉干扰。

- 修复普通项目成员阅读标注指引时示例图被权限检查拦截的问题；标注员、审核员和只读成员可查看图片，上传和删除仍限项目负责人或超级管理员。

- 绩效、成员、邀请和审计筛选在浏览器导航恢复后立即输入时不再停止更新；快速切换其他条件或按 Enter 提交也不会被旧防抖值覆盖。

- Data Manager 的空 OR 条件组与任务筛选保持恒真语义，匹配对象抽屉不再错误返回零项；与对象条件组合时仍只展示实际命中的对象。

- Data Manager 新建视图后保持当前选择，切换任务、对象或轨迹视图时同步恢复各自条件；迟到的保存结果不再覆盖当前导航。

- 审核队列切换范围或账号后清除旧选择和退回表单；Mask 修复预览绑定原任务与条件，点云及视频质检清理旧任务状态，并明确已加载问题数量。

- 管理列表切换账号后，旧详情、评论和导出结果不再覆盖新会话；项目与 Data Manager 查询缓存同时按账号和登录凭据隔离，筛选切换与分页保持一致，避免继续使用已过期的选择范围。

- Data Manager 的匹配对象按实际成立的 AND/OR 分支展示，低置信与待审候选使用相同集合，跨对象、预测和追踪来源的分页保持准确总数。

- 工作台分别展示当前帧待审数量和已加载候选的批量采纳数量，视频跨帧操作范围明确可见；切题恢复帧与来源筛选，账号切换后旧候选回调不再发起写入或覆盖新会话。

- Data Manager 在执行前校验筛选树、日期、UUID 和数字，错误条件返回可处理的校验结果；保留可空字段的未分配筛选，并使字段列表与项目实际支持的操作一致。

- 修复视频非矩形候选的当前帧筛选不完整、隐藏对象仍出现在画布或参考虚影，以及图片隐藏 Mask 仍被绘制的问题；视频画布与轨迹清单共用复审显示规则，隐藏条目仍可从列表恢复。

- 文档登录与创建项目的截图随文档主题切换，避免连续展示同一界面的明暗两张图片。

- 分离数据库运行账号与迁移账号时，分区维护、审计归档和统计刷新改由专用维护 worker 执行，避免普通后台任务因缺少所有者权限而失败；工作树启动器同时检查和管理两类 worker，Compose 部署同步提供维护队列消费者。

- 修复独立工作树的媒体链接绕过同源代理、不同临时目录下端口预留冲突，以及 E2E API 误用数据库所有者账号的问题；启动前通过真实连接核对运行账号与迁移账号是否指向同一 PostgreSQL 实例。

- 修复图片和视频问题图钉内的状态符号偏离圆心，缩放后仍保持居中。

- 图片和视频问题图钉统一为屏幕固定大小，并按严重度显示未解决问题：提示蓝色、警告黄色、阻断红色；已解决和搁置分别显示绿色、灰色。

- 工作台反馈入口移到顶栏，避免右下角悬浮按钮在鼠标移向评论「发送」时弹出并遮挡操作。

- 工作台评论发送目标自动跟随选中的正式标注，取消选择时回到当前任务；同类别实例按短 ID 分别列出，支持手动回退到任务并保留各目标草稿；修复切换标注时题图评论绘制入口忽隐忽现。评论总数和未解决问题数改为页签右上角角标，超过 9 条显示 `9+`，悬停显示准确数量。

- 讨论输入区的 Enter 不再同时触发视频多边形提交；发送留言时保留正在绘制的草稿和源帧。

- 修复任务讨论超过一页后遗漏旧留言、标注评论附件使用当前选中对象下载，以及发送、上传或绘图较晚完成时影响其他目标草稿的问题。

- 弹窗绘图在关闭、切换讨论页签或站内离开后保留未发送笔触；与题图直接绘制互斥，避免一个绘图入口覆盖另一个入口的草稿。

- 问题列表按服务端状态筛选，未解决数量使用完整统计；画布图钉未加载完或加载失败时显示提示。任务问题与画布选点入口分开，图片任务问题不再要求坐标。

- 修复 AI 示例选择仅召回框时，因沿用原生 Mask 保存设置而报 HTTP 502 的问题；图片和视频当前帧均可正常召回框，切回掩膜保留原有保存设置。

- 修复 SAM3 示例召回选择“框与掩膜”并保存为原生 Mask 时缺少矩形候选的问题；同一实例的框与 Mask 成对返回，逐条采纳时保留正确的候选签名与视频帧位置。

- 修复标注指引在离开页面时保存失败会丢失草稿、表格单元格内的图片上传无法完成，以及 BUG 评论在表格内快捷提交时可能发送旧值的问题。

- 修复同一事务中创建的任务、标注和预测在导出时顺序不稳定，避免同一份数据集在不同查询计划下产生不一致的导出结果。

- Markdown 空白输入提示使用弱化颜色，修正指引提示中直接显示换行转义字符的问题。

- 标注指引与 BUG 正文统一阅读排版，内部指引图片支持正确解析、加载失败提示与重试；宽表格和代码块可横向滚动，图片可放大、代码可复制。打开旧指引或切换编辑模式不会自动改写原文。

- 修复多个设备同时编辑命名布局预设时，后提交的旧清单覆盖另一设备新预设的问题；冲突操作现在刷新最新清单并提示重试。

- 修复审核员可见并运行二次推理的问题；审核工作台隐藏面板及相关开关，接口拒绝审核员调用，保留人工修改标注的能力。

- 修复桌宠上方提示切换时本体位置跳动的问题；展开图片、视频或点云详情后隐藏简化气泡，收起后恢复，拖动时继续与详情面板联动。

- 修复图片工作台选中旋转框后无法拖动框主体的问题；移动保持尺寸与角度，支持保存及撤销、重做。

- 多工作树并行启动前端开发服务时使用各自的 Vite 预构建缓存，避免共享依赖目录导致 CodeMirror 等模块重复加载并使页面崩溃。

- 修复视频 Issue 定位提示遮挡讨论卡片的问题，等待源帧或超时后仍可切换到其他问题。

- 修复 Mask 保存失败后后台重复发送更新的问题；失败时保留当前像素和笔画历史供重试，离线撤销或重做同步时保留原版本条件。

- 修复隐藏左侧面板或调整画布位置后，选择“审核协作”等预设仍沿用旧排列的问题；切换预设按示意图恢复完整布局，并保留画布和未发送草稿。

- 顶栏性能监控、后台任务和通知面板统一尺寸与位置，点击外部即可关闭，切换入口只保留一个面板；修复后台任务遮罩局限于顶栏、点击正文无法关闭的问题。

- 开工清单依据本人真实访问、有效标注和审核结果更新，指南需明确确认；项目开工准备检查实际非空批次、后台建任务状态和有效分派，避免将空批次或失效成员误报为就绪。

- 工作台区分服务端保存、本机暂存和同步失败，保存或队列读取未完成时阻止提交；退出时可同步或保留本机记录，切换账号后隔离待同步操作和查询缓存。
- 标注创建重试复用持久操作标识，避免网络响应丢失或多标签页同步产生重复对象；账号恢复后仍按当前任务归属校验写入，旧页面不能修改已交接工作。
- 批次分派在服务端复核账号启用状态、项目成员关系和对应职责，阻止向已停用或职责不匹配的账号继续分派工作。
- 单条任务被退回时，即使同批次仍在审核，原标注员也能打开并继续返工，其他待审任务保持原流程。
- 忘记密码现在实际发送重置邮件，并在发送失败时提供管理员协助说明；重置链接仅能使用一次，成功后旧会话失效，注册、重置和个人改密使用一致的密码规则。
- 标注与审核首页区分网络、权限和服务错误，支持重试并在刷新失败时保留已加载内容；任务列表可以继续加载后页，避免大型批次只能访问前 100 条任务。

- 二次推理阻止同一标注的重复运行，切换任务或标注后，旧请求只更新来源任务，避免干扰当前对象的运行状态。

- 点云跨帧任务中心改用无背景模糊的遮罩，避免软件渲染时弹窗叠加点云造成浏览器清理卡顿及后续上下文初始化超时。

- 修复视频首次打开或切换任务时，帧时间表更新可能使精确帧一直等待的问题；帧数据就绪后自动继续显示，无需刷新或手动切帧。

- 生产 Web 代理支持同源媒体上传下载和视频分段读取，API 容器替换后自动重新解析上游地址；API 镜像按 Python 锁文件构建，依赖环境与开发源码挂载隔离。

- 修复生产 CSP 拦截通知组件动态样式的问题，通知样式在插入页面前携带当前请求 nonce。

- 修复拖出浮窗后的实际尺寸与保存尺寸不一致，导致退出紧凑布局或刷新时面板放大的问题。

- 修复窄屏工作台“更多工具”中的视频分段选择器按键被菜单拦截、Enter 可能误触智能切题的问题。

- 修复工作台专注画布布局在空闲时持续触发布局序列化的问题，避免预设状态更新反复切换最大化。

- CI 与本地 Compose 的 MinIO 服务镜像改用 quay.io 官方社区源；Docker Hub 的 `minio/minio` 已停止分发，导致后端测试与端到端工作流全部拉取失败。

- 工作台选择标准标注或重置标准布局时恢复左右侧栏与居中画布，避免沿用旧画布靠边位置后把任务与类别面板挤到右侧。

## [0.25.3] - 2026-09-09

### Added

- Mask 设置收起为左侧胶囊，悬停展开常用工具，点击更多在顶部居中打开完整工具栏；减少图片遮挡，绘制经过时不自动展开；完整工具栏按状态、工具和保存操作分区，展开和收起以胶囊为起终点连贯过渡，并适配减少动态效果设置。

- 图片与视频多边形在加点时实时预览轮廓和内部半透明填充，随光标更新并遵循工作台填充透明度设置。

- 图片 Raster Mask 支持预览后直线切割为两个实例，保留全部像素及来源属性；一次撤销或重做保留相同对象 ID，恢复所需的 Mask 版本在原切割后 30 天内受资源清理保护。

- 图片多边形支持预览后切成两个对象，较大区域保留原 ID，复制业务属性和原父对象关系；支持原子撤销/重做、失败重试与 30 天恢复期限，后续编辑或锁定会阻止覆盖。

- 图片多边形支持沿已保存的简单边界追踪，预览并选择顺逆两条路径后追加到草稿；保留逐点撤销，来源修改或删除时拒绝过期预览，带孔和多外环对象明确提示暂不支持。

- 图片多边形支持 Shift 拖动自动落点，缩放或改变拖动速度后仍按约 8 屏幕像素采样；可继续补点、撤销与连续创建，20,000 点时暂停自动采样并保留草稿。

- 图片矩形框支持按住 Alt 从中心创建，也可在类别面板选择“角点 / 中心”；靠近图像边界时对称限制半径，连续创建沿用该会话选项。

- 视频问题支持用一条记录表达源帧范围，并保存对象版本、画布视图和时间窗；可从项目问题列表跨任务恢复现场，对象变化时保留原帧与位置并提示，权限不足时保留当前任务。

- 视频画布支持落点创建问题并保存源帧；图钉、问题卡片和时间轴共用锚点，等待真实画面就绪后定位，失败保留锚点并可重试，切题隔离未完成的创建与定位。

- 视频工具坞新增单帧与轨迹范围，同几何工具随范围切换；选中轨迹与明确工具快捷键同步更新范围，未完成绘制先确认，避免草稿被改成另一种类型或提前保存。

- 视频画布新增常用当前轨迹条，统一展示矩形框、多边形、折线和 Mask 的源帧、关键帧、插值、保持、outside、遮挡及实际来源；支持相邻关键帧跳转和可编辑时补关键帧，隐藏时间轴后仍可定位当前对象。

- 图片类别面板新增会话级“连续创建”：选定工具单元和类别后，可连续绘制矩形框、旋转框、多边形、折线和模板关键点；逐对象补齐必填属性，保存失败留稿重试，离线持久接收后再继续。

- 文档视频新增跨平台按场景录制入口：手工流程无需 AI 后端，SAM / OCR 可连接远程模型；候选视频独立归档，Linux 营销母版继续保留原有质量门槛。

- 3D Scene 时间轴支持紧凑总览、展开分轨、帧号直达和缩放，以及 1/2/4 帧每秒的只读连续浏览；目标点云就绪后再推进，播放时暂停编辑锁，待保存或未完成绘制会阻止启动。

- 新增 Orca 工作树初始化 hook，共享 `.env` 和依赖一致时的 pnpm 目录，并独立准备 Python 环境与 API 类型，减少重复安装并避免跨分支源码混用。
- 新增 Codex `SessionStart` hook，按 macOS、Windows 和 Linux 注入当前主机硬件摘要，帮助 agent 根据实际运行平台工作。

### Changed

- 移除工作台面板标签的三点菜单和右键布局菜单，直接拖动调整位置，使用标签旁的 × 隐藏面板。

- 文档检查分为 PR 自动校验、每周媒体巡检和阶段手动验收；日常界面迭代不再因旧截图的人工复核状态阻塞 PR，发布前仍检查完整文档和媒体审核证据。

- 视频追踪审阅可明确切换作业，审阅条、轨迹条、时间轴与画布共用目标和帧窗口；切帧或查看参考轨迹保留选区，部分决定后可跳转剩余区间，旧请求不会覆盖新范围或重新弹出过期的人工帧确认。

- 当前题 AI 按准备、运行、审阅和错误突出下一步操作，保留本次输入与真实作业进度；重试复用原请求，取消和切题隔离迟到结果，候选属性草稿可在隐藏面板后继续审阅并随采纳保存。

- 交互 AI 顶栏常驻提示输入、候选序号与接受/取消，将后端、模型、档位和诊断折入高级配置；能力协商与本轮推理分别显示错误和恢复入口，图片与视频均使用已选模型档位。

- Mask 工具栏按当前阶段显示应用区域、提交实例、保存草稿或恢复动作，并与图片和视频 Enter / Esc 共用同一入口；取消预览保留笔画，退出脏稿先确认，低内存只读时仍可保存已有修改。

- 更新视频画框、跨帧续写、Mask 轨迹和时间轴演示，在当前停靠布局中同时展示目标、关键帧与保存结果。

- 更新 SAM 点选、框选、示例与候选审核演示，按当前 AI 审阅布局展示真实推理和保存结果，并补齐文档与首页视频的共同录制来源。

- 更新矩形、旋转框、折线、多边形和 Mask 基础演示，补充布局调整与编辑状态保留教学，并修正文档录像中的异常任务锁倒计时。

- 按自由布局重录面板操作、当前题 OCR、视频工作台与相机种框样板，并更新工作台静态图和首页配图，让文档演示与当前面板位置、标签切换一致。

- 平台侧边栏支持收起为图标、限位拖动宽度与本地记忆；管理员概览改为核心指标带、项目主栏和注册／审计辅助栏，常用管理入口更集中。

- 侧栏为项目管理、训练队列、用户权限和绩效分析等入口使用更明确的图标；工作台区分旋转框、关键点、Mask、智能笔迹与跨帧轨迹，并同步项目工具设置和点云任务队列中的图标，减少相邻选项混淆。

- **文档演示资产全面迁移到 MP4 视频**. README 工作台案例表格与 SAM / Bbox 用户文档中剩余的 4 个 GIF 演示（智能点、智能框、Exemplar、矩形框绘制）替换为 4K 母版派生的 720p MP4（文档站内 DocsVideo 播放，README 用海报帧链接到 MP4），发布文章同步改为海报帧 + 完整演示链接，并删除旧的 GIF 文件。

- 统一文档、首页和宣传文章的视频、GIF、封面派生链：录制先归档标准源或高清母版，派生时校验哈希与质量等级；旧流程 GIF 不再直接覆盖发布文件。

- 点云演示视频统一使用 nuScenes-mini 实景与六相机标定，更新设置窗口、相机种框、跨帧延续和标签演示，避免旧夹具画面与实际操作不符。

- 点云工作台媒体补充三视图与相机整组布局演示，覆盖停靠、浮动、隐藏恢复和相邻帧核对，文档与派生视频共用真实 nuScenes 源录制。

- 工作台设置改按用途统一分类，图片、视频、点云和本机实验均可从任意任务查看、搜索和修改；个人设置页使用同一套分类。

- 工作台设置改为居中悬浮窗口，支持分类搜索、分组说明和窄屏布局；点击外部、返回按钮或 Esc 可关闭，视频开窗自动暂停，编辑时背景画布不响应快捷键。

- 3D 三视图精修加入自由布局，三张视图整体停靠、组成标签、浮动或隐藏；相机可在原有逐路悬浮和整组停靠图库之间切换，悬浮位置与折叠态、停靠位置分别记忆。
- 工作台辅助面板标题栏常驻 ×，可直接隐藏当前活动面板，从“布局”菜单找回并保留业务状态。

- 工作台新建停靠列默认使用工作区宽度的 15%；顶部左右按钮按画布两侧的实际位置收起、展开面板，保留分栏尺寸与标签顺序，并记住收起状态。
- 3D 视角控件收为单行工具栏，框体精修、传感器融合、点级分割与恢复相机排列移到顶部“布局”菜单；切换工作方式保留主画布与其他面板位置。

### Fixed

- 工作台关闭按钮移到每个面板标签旁，合并标签后也能直接隐藏指定面板，保留草稿并可从“布局”找回。

- 工作台底部状态栏保持紧凑单行，窄面板下横向滚动查看完整信息，避免中文标签被压成竖排并挤占画布；光标坐标保留固定位置，进入、移出画布或坐标位数变化不再挤动其他状态。

- 自由布局新列的拖拽示意宽度与落位后的 15% 默认列宽一致，并遵守面板最小宽度。

- 自由布局中拖动两个外围面板上下共用一列时，默认各占一半高度，保留目标列宽，避免尺寸恢复把比例变成 2:1。
- 工作台独立类别面板的列表铺满可用高度，随面板缩放，不再停留在旧侧栏的固定高度。

- 修复工作台顶部栏在窄窗口下项目、任务导航与工具按钮相互重叠的问题，始终保持单行，随宽度省略辅助信息和按钮文字，次要工具收进菜单；前后任务使用带悬停提示的箭头按钮，“提交质检”简写为“提交”。

- 修复 Mask 设置与高级菜单被选中对象浮卡遮挡，导致保存、恢复编辑或保存并传播按钮无法点击。

- Mask 常用工具收起后，焦点停留在胶囊上时仍可按 Esc 执行退出等当前次操作，避免键盘操作被困在工具栏。

- 图片 Mask 普通保存后保留笔刷工具并准备新的空白草稿，不再跳转到矩形框，也不会将下一笔写入刚保存的对象。

- GitHub 仓库首页的 AI 案例现在显示明确的播放入口，并直接打开带浏览器控件的视频。

- 切割、撤销或重做保留 Mask 对象 ID 时，内容读取会校验当前版本，避免浏览器短时显示旧像素和旧面积。

- AI 能力协商失败时，恢复工具条不再遮挡图片或视频 Mask 编辑；切回选择工具后仍可重试协商。

- 视频时间轴操作后调整停靠面板时，布局容器不再被焦点滚动偏移，避免误报恢复失败并锁住布局。

- 视频新建单帧和轨迹 Mask 在保存完成后读取像素，避免保存中的临时标注触发无效内容请求。

- 修复视频 Mask 轨迹在没有未保存像素笔画时按 `Ctrl / Cmd + Z` 无法撤销关键帧删除的问题；撤销 / 重做现在会正确落到关键帧历史。

- 修复 Mask 质量修复计划在持久化可选字段为 `null` 后，后台执行器重新校验 payload 失败的问题。

- 修复工作台停靠分隔条盖住面板菜单、导致隐藏等菜单项无法点击的问题。

- 图片与视频工作台在短窗口、浏览器放大或底部停靠面板挤占空间时，工具不再被裁掉；“更多工具”保留完整入口，当前工具持续可见，菜单按键不会误触画布操作。

- 视频选中当前帧普通 AI 候选时，`A` 采纳、`D` 忽略；按钮与快捷键仅在请求成功后自动前进，失败或补选类别期间保留选择，并避免连按、批量操作与切题迟到响应造成重复提交或误改选择。修复补选类别弹层被对象浮窗遮挡，以及图片多边形备用键说明错误。

- 视频追踪面板切到后台标签页或隐藏后，时间轴不再残留随播放移动的紫色 AI 范围预览；重新显示面板时保留追踪配置。

- 修复视频追踪面板切到后台后仍覆盖章节圈选、强制折叠选中信息卡的问题，保留追踪配置时也能正常创建章节和编辑 Mask。

- 修复图片旋转框拖动手柄时角度随图片长宽比偏移的问题，非正方形图片在缩放后也按实际画面方向旋转。

- 修复新建原生 Mask 保存成功后误报内容加载失败、需要刷新才显示的问题；图片和视频 Mask 等待真实标注 ID 后读取，同内容对象的加载错误互不影响。

- 选中 AI 候选时，讨论面板继续显示任务评论，不再用临时候选 ID 请求正式标注评论；选中已保存标注后仍显示其专属讨论。

- 工作台候选审核演示改用 SAM3 实际推理的车辆轮廓与置信度，并验证逐项采纳、拒绝及自动前进。

- 修复图片工作台拖拽画框时预览停在起点、松手后不生成标注的问题；状态栏更新不再打断手势，快速松手也会保留最后的拖拽位置。

- 平台概览后台刷新失败时保留已有统计和打开的向导，提供刷新提示与重试入口；侧边栏在展开、收起和移动抽屉中均向屏幕阅读器提供待审核、AI 待接管和失败数量。

- 修复浏览器缩放或窗口变窄时，Dashboard 及人员统计中的迷你折线超出卡片边界的问题；曲线随可用宽度完整缩放，并保持描边清晰。
- 修复视觉回归 CI 缺少 `ffmpeg` 导致截图数据初始化失败，以及失败通知缺少 Issue 写权限的问题；通知按测试执行状态说明故障，避免将环境失败误报为 UI 变更。

- 修复在布局防抖保存完成前切换工作台上下文时，最新布局调整未能保存的问题；点云渲染基准现在会实际使用配置的 DPR。
- 设置首次加载失败可重试，保存失败显示未同步提示；关闭窗口前提交文本，避免输入因关闭而丢失。

- 修复从主分支创建 Orca 工作树时，Python 3.12 在 Apple Silicon 上编译旧版 Matplotlib 失败、初始化中断的问题；测试依赖改用兼容版本及预编译包。

- 修复工作台面板停靠、合并标签、拖放、浮动、隐藏或恢复时，侧栏被自动拉宽、画布被挤窄的问题；两列拖成上下排列时保留目标列宽，窗口从紧凑布局恢复时也会保留原有侧栏尺寸。
- 修复画布最大化或切换专注布局后，恢复时侧栏缩窄的问题；保存、重新加载及紧凑布局切换都会保留最大化前的侧栏尺寸。
- 修复面板分成上下布局、移动画布或恢复最大化后，分隔条被临时尺寸约束锁住，无法调整列宽和上下占比的问题。
- 统一管理端各页面的内容区宽度与左右留白：新增共享 `PageContainer` 容器（1480px），修复设置、项目设置、成员绩效页面与概览等页面宽度不一致，以及审核页内容区未居中的问题。
- 将 README 与产品文章中的旧 GIF 引用迁移到现有 MP4 与封面，避免文档显示损坏媒体。

## [0.25.2] - 2026-09-05

### Added

- 当前题 AI 与视频追踪加入工作台停靠布局，可停靠、成组、浮动、隐藏和恢复，并提供图片 AI 审阅、视频追踪预设；隐藏或切换布局不会取消运行或清空草稿。

## [0.25.1] - 2026-09-05

### Added

- 工作台画布可从“布局”菜单移到整个工作区的左、右、上、下边缘，并可最大化或恢复；换位会保留当前 Stage、媒体状态和编辑草稿。

### Fixed

- 点云性能基准从实际导航开始统计渲染耗时，并提前准备 WebGPU 三视图管线，避免交互防抖和首次管线编译误判为持续渲染回退。

## [0.25.0] - 2026-09-05

### Added

- 工作台新增“布局”菜单，任务队列、类别面板、标注详情和讨论可停靠、组成标签、浮动或隐藏，并提供标准标注、专注画布、审核协作预设与一次布局撤销。
- 工作台布局按账号及标注或审核、图片或视频或点云分别保存；窄窗口临时显示单个面板，放宽窗口后恢复桌面布局。

### Changed

- 工作台布局读取完成前暂时锁定布局操作；损坏布局可显式重置，来自新版的布局保持只读，避免旧客户端覆盖已保存的位置。

### Fixed

- 视频导出包的抽帧脚本兼容 FFmpeg 9，修复旧参数导致抽帧失败的问题。
- 录制夹具初始化补齐 nuScenes 地图文件，并按数据集隔离路径解析点云资源，修复全新开发库恢复时地图缺失和任务校验失败。
- 3D 点云工作台的选中信息栏会与桌宠共享位置并一起拖动，不再作为独立浮层停在画布右上角。
- KITTI 点云导出会在相机成员或标定变化后失效旧缓存，并按人工相机框的可见性写入遮挡级别，不再复用过期包或误读 3D 属性。
- 点云质量问题按状态筛选时会先刷新过期状态再分页，首屏不会因已失效问题占位而漏掉仍有效的问题。
- AAP JSON 标注导入的 dry-run 会校验相机角色、主 3D 轨迹关系与重复相机成员，预检结果现在与正式导入一致。- 超限大图现在会同时禁用 Mask 按钮和 `M` 快捷键，并显示同一条尺寸上限原因。
- 批量预标现在按实际归一化后会持久化的重试上下文执行 8 KiB 限制，不再因大量空参数误报 422。

<!--
日常变更（含普通 bug 修复）按 Keep a Changelog 类型分组追加到本段：
Added / Changed / Deprecated / Removed / Fixed / Security（按此顺序，空组省略）。
发版时把「## [Unreleased]」重命名为「## [x.y.z] - 日期」，再在其上方留一个空的
「## [Unreleased]」。0.25.x 版本段累积在本区；进入 0.26.x 后整体移到 docs/changelogs/0.25.x.md。
-->
